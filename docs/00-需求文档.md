# RuT0DataKit 需求文档

## 1. 产品定位

面向数据安全 CTF 的快速分析工具箱。针对竞赛中常见的三类附件——结构化表格（CSV / XLSX）、日志文件（access.log 等通用日志）、网络流量（PCAP / PCAPNG）——提供"选文件 → 自动识别 → 按规则跑 → 出报告"的一站式流程，帮助选手在数据安全方向题目中快速完成数据脱敏、日志取证分析、流量包敏感数据提取。

非目标：

- 不做通用渗透 / 漏扫 / Webshell 管理。
- 不做密码学爆破、隐写通用解密（仅在涉及题目样本时做最小适配，如 blind_watermark 调用留接口，不内置实现）。
- 不做 GUI 之外的 SaaS / 云同步。

## 2. 目标用户与场景

- 主要用户：参加数据安全方向 CTF 的选手、出题人、裁判。
- 场景：
  1. 拿到 csv/xlsx，按脱敏规范输出脱敏后表格（v0.1.0 交付）。
  2. 拿到 access.log，解析 CLF，提取注入 / 越权 / 弱口令 / SQLi 等攻击特征，并对 query 串做敏感数据扫描（v0.2.0 交付）。
  3. 拿到 pcap，自动过滤 HTTP、解码参数、重组 base64 片段、扫描身份证 / 手机号 / MAC / 邮箱 / 银行卡等敏感数据（v0.3.0 交付）。
  4. 数据规范提取（valid/invalid 校验）作为 v0.1.0 的同源能力，与脱敏共用 reader 与 masker/validator 注册表；是否在 v0.1.0 默认交付 GUI 入口由任务细化决定。

## 3. 版本范围

### v0.1.0 — 数据脱敏切片（四功能重构补丁，T0-15 ~ T0-20）

- CSV/XLSX reader + 脱敏 pipeline + masked.csv 导出。
- 内置 maskers：idcard / phone / bankcard / email / name / customer_id / custom。
- 富规则 maskers（v0.1.0 重构新增）：`regex_replace` / `regex_extract` / `delete` / `replace`。
- 行选择 pipeline：`mask_pipeline_selected` 只对勾选行脱敏，未选中行原样输出（保留向后兼容）。
- **列选择 pipeline**（T0-15）：`mask_pipeline_columns(records, rules, selected_columns: &HashSet<String>)` 只对勾选列应用 masker，未勾选列原样；`selected_columns` 与 `rules.maskers[].field` 取交集。
- **校验 pipeline**（T0-15）：`validate_pipeline(records, rules) -> ValidateResult { headers, rows, valid_matrix, summary }`，逐 cell 填合法矩阵；未声明 validator 的列全 true。
- 规则引擎：YAML RuleSet + MaskerRegistry + ValidatorRegistry；规则可在 GUI 直接编辑后以 JSON 传给后端，不再强制走 YAML 文件；`MaskRule` / `FieldRule` 均加 `description: Option<String>`（`#[serde(default)]`）用于规则详细说明。
- **四功能导航**（T0-17 ~ T0-19）：侧边栏 4 active view（数据脱敏 / 数据校验 / 数据导出 / 规则管理）+ 2 disabled（日志 v0.2.0 / 流量 v0.3.0）。
  - 数据脱敏 view：导入 CSV/XLSX → 预览表格 → **列勾选**（表头列 checkbox）→ 规则面板 → 「应用规则到勾选列」。
  - 数据校验 view：选 validator → 「运行校验」→ 逐 cell 合法性矩阵展示；「跳转到数据导出」按钮把当前数据带入导出 view。
  - 数据导出 view：选数据源（原始数据 / 脱敏数据 / 校验数据）→ **列选 + 列序拖拽 + 格式（CSV / XLSX）** → 「导出」；XLSX 由 src-tauri 的 `rust_xlsxwriter` 写出。
  - 规则管理 view：列表 / 编辑（含 `description`）/ 「保存 YAML」/「加载 YAML」/「查看规则说明」（RuleDocPanel，未选中规则时显示占位提示，属预期行为）。
- **跨视图 state 持久化**（T0-17）：前端 `App.jsx` 顶层 `useReducer` 常驻，`SET_VIEW` 只切 view 不重置数据；切回各 view 数据不丢。
- 验收样本：`tests/fixtures/samples/csv/sample_mask.csv` + `default_mask.yaml`。

**UI 重构 + 规则引擎重构补丁（T0-21 ~ T0-26）**：

- 规则引擎重构：引入抽象可扩展算子 `MaskOp`（4 通用算子 `Template` / `SplitTemplate` / `RegexReplace` / `ConstReplace`）+ `ValidateOp`（3 通用算子 `Regex` / `Algorithm` / `RegexWithGuard`）+ 预置库（6 脱敏别名 `idcard_mask` / `phone_mask` / `bankcard_mask` / `customer_id_mask` / `name_mask` / `email_mask` + 7 校验别名 `email` / `username` / `name` / `idcard` / `bankcard` / `phone` / `mac`）+ `apply_mask_op` / `apply_validate_op` 公共 API；`build_masker` / `build_validator` 内部改走 `MaskOp::from_rule` / `ValidateOp::from_rule`，签名与向后兼容保持不变；新增脱敏类型 = 加 `MaskOp` variant + 预置库一行映射。
- 规则管理界面重做：脱敏 + 校验规则合并为**单一列表**渲染；右上角「添加规则」按钮打开 **Drawer 抽屉表单**完成新建/编辑；每条规则带「试运行」按钮，动态读取当前导入数据首行调 `preview_mask_rule` / `preview_validate_rule` 展示 `input → output`（脱敏）或 `input → valid + message`（校验）。
- 数据脱敏 / 数据校验界面改为**四段垂直**布局：原始数据表 → 表头-规则映射表 → 预览表 → 操作面板；「应用」按钮触发预览（非实时计算）；「导出」按钮直接跳转 ExportView（state 常驻不重置）。
- 数据导出界面改为**单一 antd Table 集成**：表头行 checkbox 勾选导出列 + 上下移按钮调列序 + 单元格预览；顶部数据源 Select / 过滤 Select / 格式 Select（CSV / XLSX）；「导出」按钮按勾选列 + 列序 + 格式调 `export_records_csv` / `export_records_xlsx`。

### v0.2.0 — 日志处理切片

- Log 模块：CLF 解析 + 攻击特征识别 + 敏感扫描 + JSON 报告。
- 复用 v0.1.0 的 SensitiveScan（在 v0.1.0 即预留共享 scan trait，避免 v0.2.0 重构）。
- GUI 增加日志入口与报告展示。
- 验收样本：`tests/fixtures/samples/log/access.log`。

### v0.2.1 — 日志处理切片语义增强

- SQLi payload 语义解析（6 类结构化字段提取）：blind_boolean（read_target / char_position / compared_ascii / comparator）/ union（union_columns）/ error（read_target）/ time（sleep_seconds）/ tautology / comment，产出 `ParsedPayload` + 人类可读 `summary`。
- 字段还原（query/path/UA 双重 URL 解码 + `+` 解码）：`parse_query` 按 form-urlencoded 标准 `+`→space 再双重 `%XX` 解码；`LogEntry` 新增 `decoded_path` / `decoded_query` / `decoded_ua` 字段。
- `Finding.extra` 字段透传 `ParsedPayload`，GUI LogView findings 表新增「解析结果」/「读取目标」列；原始日志表新增 decoded 三列。
- 6 类签名 pattern 扩变体：`union all select` / `exp(~...)` / `floor(rand(0)*2)`。
- 跨 entry 盲注聚合（同一 IP 多行盲注二分还原完整 database() 字符串）留 v0.3.0+。

### v0.2.2 — 日志扫描盲注二分序列聚合还原

- 跨 entry 布尔盲注二分序列聚合还原：日志扫描自动从同一 `(read_target, source_ip)` 的多行盲注二分探针按 `char_position` 聚类，基于 HTTP 响应 body 字节数自动判定真假方向，还原出完整字符串（flag），如 `database()` = `"person"`、`table_name` = `"person_data"`、`column_name` 含 `id/username/password`。
- GUI LogView 新增「盲注聚合结果」卡片（段 ③.5）：read_target inner Card + 可复制还原串 + 探针数 + 来源 IP + 位置明细 Tooltip + 空态 Empty。
- `Report.extra` 由 `Value::Null` → `Value::Mapping({blind_aggregation: [...]})` 向后兼容（不破 v0.2.1 parsed_payload 单条语义解析）。
- 时间盲注聚合 / UNION 报错聚合留 v0.3.0+。

### v0.2.3 — 日志扫描盲注三轴优化

- 算法优化（T3-1）：`blind_aggregator` 的 `true_size` 判定由 v0.2.2 的 `min(body_size)`（fixture-specific 假设）改为 `mode_per_position_true_size`——按 `char_position` 分组取每个混合位置真假簇 body_size 的 min/max → 跨位置众数（并列取较小者），无混合位置退化 `min(body_size)`；修复 v0.2.2 R1 第 4 read_target（false 频次高于 true）全 `?` 回归。自带正则从单层扩到两层嵌套 `((?:[^()]|\((?:[^()]|\([^()]*\))*\))*)` 覆盖 `where table_schema=database()`。`AggregatedResult` 新增 `separator_char: Option<char>` 从 `read_target` 首个 `0xNN` 字面量解码（`0x7e`→`~`）。
- 盲注类型扩展（T3-2）：`BlindProbe` 新增 `probe_kind: ProbeKind`（`AsciiBinary` / `Equality` / `Length`）+ `equality_char`；新增 equality / length 两条正则；聚合分组键升级为 `(read_target, source_ip, ProbeKind)` 三元组防串扰；`AggregatedResult` 新增 `kind` 字段；`PositionDetail.status` 新增 `equality_resolved` / `length_resolved`。
- GUI 显示优化（T3-3）：LogView 段 ③.5 inner Card 加 kind Tag 着色（ascii_binary=red / equality=orange / length=blue）+ separator 高亮（volcano Tag + 复制按钮）+ Collapse 内嵌 Table 位置明细（列 位置/字符/ASCII/探针数/状态，status Tag 6 色着色），替代 v0.2.2 的 JSON.stringify Tooltip。
- 向后兼容：新字段 `kind` / `separator_char` 均 `#[serde(skip_serializing_if)]`，不破 v0.2.2 既有消费者。

### v0.3.0 — 流量包处理切片

- Pcap 模块：tshark 后端 + HTTP 字段提取 + 双重 URL 解码 + base64 重组 + 敏感扫描 + JSON 报告。
- GUI 增加 pcap 入口。
- 验收样本：`tests/fixtures/samples/pcap/data.pcapng`。

### 非范围（三版本均不做）

- 磁盘取证 / 盲水印 / ext4 镜像（留接口）。
- xlsx 写回公式。
- 自定义 Rust 规则插件编译。
- CLI 形态（三版本仅交付 GUI，不提供 CLI 入口）。

## 4. v0.1.0 验收标准（端到端）

1. 在 GUI 中选择 `sample_mask.csv` + 加载内置 `default_mask` 脱敏规则 → 输出 `masked.csv`，字段符合：身份证 `110101********1234`、手机 `138****5678`、银行卡 `622588******1098`、姓名 `张*` / `李*海`、邮箱 `z******n@example.com`（本地 2 字 `z*@x.com`）、客户ID `1*******`。
2. 选择 `.xlsx`（由 sample_mask.csv 转出的 fixture）→ 同一规则 → 同一脱敏结果。
3. 文件类型自动识别：`.csv` / `.xlsx` → csv 模块，命中。
4. 表头不脱敏；字段不在 maskers 列表中的原样输出。
5. 短于阈值的输入（如 1 字 email 本地、<10 位银行卡）原样返回，不 panic。
6. 自定义 YAML 规则可加载并应用（keep_prefix/keep_suffix/mask_char/mask_min_len）。
7. `cargo test --workspace` 全绿；`cargo test --test e2e`（脱敏 fixture）全绿。
8. 侧边栏布局可见，四个 active view（数据脱敏 / 数据校验 / 数据导出 / 规则管理）均可切换；日志 / 流量置灰占位。
9. 可逐行勾选（表头行默认不勾选、不可勾选）→ 在规则面板添加/删除规则（含 4 种富规则 `regex_replace`/`regex_extract`/`delete`/`replace`）→ 应用规则到勾选行 → 导出 CSV；导出文件中勾选行被脱敏、未勾选行原样。
10. **按列勾选脱敏**：MaskView 表头列 checkbox 勾选目标列 → 「应用规则到勾选列」→ 仅勾选列被脱敏，未勾选列原样。
11. **数据校验 GUI 独立 view**：ValidateView 选 validator → 「运行校验」→ 逐 cell 合法性矩阵与汇总展示；非法 cell 高亮。
12. **数据导出列选 / 列序 / 格式**：ExportView 选数据源 → 勾选导出列 → 拖拽列序 → 选 CSV 或 XLSX → 导出；XLSX 写出成功且与 CSV 同源数据一致。
13. **规则详细说明面板**：RulesView 中选中规则后 RuleDocPanel 展示该规则说明；未选中规则时显示占位提示（预期行为）。
14. **跨视图数据不丢**：在 MaskView 处理数据后切到 ValidateView/ExportView/RulesView 再切回，原数据 / 脱敏数据 / 校验结果仍保留。
15. **规则管理单一列表 + Drawer + 试运行**：RulesView 以单一列表渲染脱敏+校验全部规则，每条带试运行/编辑/删除按钮；右上角「添加规则」打开 Drawer 抽屉表单完成新建/编辑。
16. **试运行 API 契约**：试运行按钮调 `preview_mask_rule` / `preview_validate_rule` 命令，卡片内展开 `input → output`（脱敏）或 `input → valid + message`（校验）；动态读取当前数据首行。
17. **MaskView / ValidateView 四段垂直**：原始数据 → 表头-规则映射 → 预览 → 操作面板；「应用」按钮触发预览（未点应用前预览表为空）；「导出」按钮跳转 ExportView。
18. **ExportView 单一 Table 集成**：表头 checkbox 勾选列 + 上下移调序 + 单元格预览；导出 CSV+XLSX 列顺序与勾选与预览一致。
19. `cargo test --workspace` 全绿（~157 passed = 134 unit + 23 e2e；以实际 cargo test 输出为准）。

## 4.1 v0.2.0 验收标准（端到端）

1. `tests/fixtures/samples/log/access.log` 全 1860 行 100% 解析成功（`LogReader::read` 无失败行）。
2. SQLi 签名引擎识别 6 类攻击：UNION 注入、报错注入（extractvalue / updatexml）、时间盲注（sleep / benchmark / pg_sleep）、恒真（or/and 数字=数字）、盲注二分（ascii(substr())）、注释符（-- / # / /*）；每类至少 1 个正例命中 + 1 个反例不误报。
3. 弱口令检测：query 同时含 `username=` / `password=` 且值落在 WEAK_KEYWORDS 集合时产 finding；非弱口令不误报。
4. `scan_log` 端到端跑通 access.log 产出 `Report { kind="log_scan" }`，summary 含 total_lines / sqli_hits / weak_password_hits / sensitive_hits / top_attack_ips（按 IP 聚合取前 5）。
5. JSON 报告可序列化（`Report` 实现 `serde::Serialize`），findings 含 type / value / location / context。
6. GUI 日志扫描入口激活，可读 access.log、跑 scan_log、展示 Report JSON 与命中明细。
7. `cargo test --workspace` 全绿（含新增 e2e `log_parse_full` / `log_scan_full` / `log_signatures_6_categories`，整体 ≥ 234 passed）。

## 4.2 v0.2.1 验收标准（端到端）

1. `parse_query("a=b+c")` → `[("a", "b c")]`（form-urlencoded 标准 `+` 解为空格）；`LogEntry.decoded_path` / `decoded_query` / `decoded_ua` 已填充，供签名引擎与 GUI 直接消费已解码文本。
2. `payload_parser::parse_payload` 6 类结构化解析正反例：
   - blind_boolean `ascii(substr((database()),1,1))>79` → read_target=`database()` / char_position=1 / comparator=`>` / compared_ascii=79。
   - union `1 union select 1,2,3` → union_columns=3。
   - error `extractvalue(1,concat(0x7e,user()))` → read_target=`user()`。
   - time `sleep(5)` → sleep_seconds=5。
   - tautology `1 or 1=1` → 命中 tautology_bypass。
   - comment `1'--` → 命中 comment_truncate。
   - 正常 query `q=hello` → `None`。
3. `scan_log_entry` 产出的 sqli hit 携带 `parsed_payload`（`Some`）；`scan_log` 产出的 sqli finding `extra` 非空（含 `ParsedPayload` 序列化）；weak_password finding `extra` 为 `None`（向后兼容，序列化时不出现 `extra` key）。
4. 6 类签名 pattern 扩变体：`union all select 1,2,3` 命中 sqli_union；`exp(~(select * from dual))` / `floor(rand(0)*2)` 命中 sqli_error_based。
5. GUI LogView findings 表新增「解析结果」（ParsedPayload.summary）+「读取目标」（read_target）列；原始日志表新增 `decoded_path` / `decoded_query` / `decoded_ua` 列。
6. `cargo test --workspace` 全绿（含新增 e2e `log_payload_parser_e2e` / `log_decoded_query_plus_decode`，整体 ≥ 236 passed）。
7. 版本号 0.2.0 → 0.2.1（Cargo.toml×3 + tauri.conf.json + frontend/package.json）；`cd src-tauri && npx @tauri-apps/cli@latest build` 产物 .app + .dmg 文件名含 0.2.1。

## 5. 技术约束

- 核心全部 Rust 实现，不引入 Python 运行时（pcap 在 v0.3.0 通过调用系统 `tshark` CLI，不绑 pyshark/scapy）。
- GUI 用 Tauri v2；前端 v0.1.0 采用 React 18 + Vite 5 + Ant Design 5 + @ant-design/icons（纯白主题，无 emoji，下拉使用 antd `Select` 组件）。
- 规则以 YAML 文件表达 + 内置 Rust 校验器/脱敏器注册表；用户可在 GUI 加载自定义 YAML。
- 校验与脱敏是两条独立 pipeline，可分别调用（v0.1.0 交付脱敏 pipeline `mask_pipeline` / `mask_pipeline_selected` / `mask_pipeline_columns` + 校验 pipeline `validate_pipeline`，四功能 view 全部交付：数据脱敏 / 数据校验 / 数据导出 / 规则管理）。
- 前端跨视图 state 持久化：`App.jsx` 顶层 `useReducer` 内存常驻，`SET_VIEW` 只切 view 不重置数据；切回各 view 数据不丢。
- XLSX 导出依赖 `rust-xlsxwriter`（src-tauri 侧），CSV 导出依赖 `csv` crate（src-tauri 侧）；前端规则 YAML 序列化依赖 `js-yaml@5.2.1`。
- 样本放在 `tests/fixtures/`，可在 `cargo test` 中复现。
- 规则引擎基于抽象算子 `MaskOp` / `ValidateOp`：内置 masker / validator 名全部收敛到通用算子枚举 + 预置库映射表；新增脱敏类型 = 加一个 `MaskOp` variant + 在 `rules::presets` 加一行预置映射，`build_masker` 自动 dispatch，无需改 pipeline 或前端调用约定。

## 6. 非功能要求

- macOS（arm64）为主开发平台；CI 跑 Linux。
- 单文件 ≤ 200MB 可在 30s 内完成扫描（v0.1.0 用 fixture 验证基线）。
- 不外发数据：全本地处理；规则与样本不上传。

## 7. 共享能力预留

为避免 v0.2.0/v0.3.0 大规模重构，v0.1.0 在实现脱敏时必须同时落地以下"共享底座"：

- `Validator` / `Masker` trait 与 `ValidatorRegistry` / `MaskerRegistry`。
- `SourceReader` trait + `Records` 抽象（csv/xlsx 实现两种，log/pcap 在后续版本实现自己的 reader，但复用 Records 思路）。
- `SensitiveScan` trait（v0.1.0 可只留 trait + 一个基于 RuleSet 的通用实现占位，具体签名规则在 v0.2.0 实现时绑定）。
- 统一 `Report` / `Finding` serde 结构（v0.1.0 脱敏报告也用该结构，kind="csv_mask"）。
- `CoreError` 含 `DependencyMissing("tshark")` 变体（v0.3.0 用，提前定义）。

这些底座在 v0.1.0 必须有最小可编译实现 + 单测，保证 v0.2.0/v0.3.0 是"扩展"而非"重构"。
