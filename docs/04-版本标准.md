# 版本标准

## 版本号

语义化版本 `MAJOR.MINOR.PATCH`。0.1.x 系列为首个可用迭代。

## 里程碑索引

| 版本 | 范围 | 状态 | 文档 | QA 报告 |
|------|------|------|------|---------|
| 0.1.0 | 数据脱敏切片（csv/xlsx，四功能 GUI + 富规则 + 按列勾选 + 校验 + 导出 + 规则管理 + 抽象算子规则引擎 + 四段垂直 view + 单一 Table 导出 + 动态试运行） | release_complete | docs/versions/0.1.0/ | docs/qa/versions/0.1.0/QA-审计报告.md |
| 0.2.0 | 日志处理切片（CLF 解析 + SQLi 6 类签名 + 弱口令 + 敏感扫描 + scan_log + JSON 报告） | release_complete | docs/versions/0.2.0/ | docs/qa/versions/0.2.0/QA-审计报告.md |
| 0.2.1 | 日志处理切片语义增强（SQLi payload 语义解析 6 类 + 字段还原 query/path/UA + `+` 解码） | release_complete | docs/versions/0.2.1/ | docs/qa/versions/0.2.1/QA-审计报告.md |
| 0.2.2 | 日志扫描盲注二分序列聚合还原（blind_aggregator 模块 + bytes 自动聚类 + 跨 entry 还原完整字符串 + GUI 盲注聚合结果卡片） | release_complete | docs/versions/0.2.2/ | docs/qa/versions/0.2.2/QA-审计报告.md |
| 0.2.3 | 日志扫描盲注三轴优化（true_size 众数算法 + equality/length 盲注类型 + GUI kind Tag/separator 高亮/Collapse 位置明细表） | release_complete | docs/versions/0.2.3/ | docs/qa/versions/0.2.3/QA-审计报告.md |
| 0.2.4 | 日志扫描盲注聚合数据库格式还原（ReconstructedDatabase + reconstruct_database 交叉关联 4 类 read_target + GUI 段 ③.5a 还原数据库视图 antd Table 全量列） | release_complete | docs/versions/0.2.4/ | docs/qa/versions/0.2.4/QA-审计报告.md |
| 0.3.0 | 流量包处理切片（pcap） | planned | docs/versions/0.3.0/ | docs/qa/versions/0.3.0/ |

## 版本状态口径

- `planned`：范围已定，任务 DAG 已拆，尚未全部 verified_complete。
- `in_progress`：已开始派 coder。
- `done_e2e`：所有任务 verified_complete + 端到端集成验收通过；但未通过 Release QA。
- `qa_passed`：Release QA 审计通过，QA 报告落盘且结论 `qa_passed`。
- `release_complete`：QA 通过 + 版本文档（更新日志）同步完成。

## 发布门禁

版本状态写入 `release_complete` 前，必须：

1. 全部任务 `verified_complete`。
2. 端到端集成验收通过（`cargo test --test e2e` 全绿）。
3. Release QA 审计通过，`docs/qa/versions/<ver>/QA-审计报告.md` 结论 `qa_passed`。
4. `docs/versions/<ver>/更新日志.md` 与 TASK-BOARD 状态对齐。

## 跨版本依赖

- v0.2.0 依赖 v0.1.0 的 SensitiveScan trait + Report/Finding + Reader 抽象，须 v0.1.0 `release_complete` 后才派 v0.2.0 coder。
- v0.3.0 依赖 v0.2.0 的攻击特征与 sensitive scan 实现，须 v0.2.0 `release_complete` 后才派 v0.3.0 coder。

## v0.2.0 验收口径

- 任务集：T2-1 ~ T2-6（见 `docs/03-开发任务清单.md` v0.2.0 段）。
- 全部 `verified_complete` + `cargo test --workspace` 全绿（含 e2e `log_parse_full` / `log_scan_full` / `log_signatures_6_categories`）。
- Release QA 审计通过后，`docs/versions/0.2.0/更新日志.md` 版本状态由 `in_progress` → `release_complete`。
- v0.2.0 交付后，`frontend` 侧边栏「日志扫描」由 disabled 切换为 active，pcap 仍置灰（v0.3.0 启用）。

## v0.2.1 验收口径

- 任务集：T2-7 ~ T2-11（见 `docs/03-开发任务清单.md` v0.2.1 段）。
- 全部 `verified_complete` + `cargo test --workspace` 全绿（含 e2e 新增 `log_payload_parser_e2e` / `log_decoded_query_plus_decode`）。
- Release QA 审计通过后，`docs/versions/0.2.1/更新日志.md` 版本状态由 `in_progress` → `release_complete`。
- v0.2.1 在 v0.2.0 基础上增强 SQLi payload 语义解析（6 类结构化字段提取）+ 字段还原（query/path/UA 双重 URL 解码 + `+` 解码），GUI LogView 新增「解析结果」/「读取目标」列 + decoded 三列；无新功能模块，向后兼容 v0.2.0 行为。

## v0.2.2 验收口径

- 任务集：T2-12 ~ T2-15（见 `docs/03-开发任务清单.md` v0.2.2 段）。
- 全部 `verified_complete` + `cargo test --workspace` 全绿（含 e2e `log_scan_full` 盲注聚合三段断言）。
- Release QA 审计通过后，`docs/versions/0.2.2/更新日志.md` 版本状态由 `in_progress` → `release_complete`。
- v0.2.2 在 v0.2.1 基础上新增跨 entry 布尔盲注二分序列聚合还原（不破 v0.2.1 parsed_payload 单条语义解析），Report.extra 由 `Value::Null` → `Value::Mapping({blind_aggregation: [...]})` 向后兼容。

## v0.2.3 验收口径

- 任务集：T3-1 ~ T3-4（见 `docs/03-开发任务清单.md` v0.2.3 段）。
- 全部 `verified_complete` + `cargo test --workspace` 全绿（含 e2e `log_scan_full` 第 4 read_target 四段断言：separator_char==Some('~') / starts_with("1~") / contains("~lisi~") / resolved_chars>0）。
- Release QA 审计通过后，`docs/versions/0.2.3/更新日志.md` 版本状态由 `in_progress` → `release_complete`。
- v0.2.3 在 v0.2.2 基础上对盲注切片做三轴优化：算法（`mode_per_position_true_size` 众数 + 两层嵌套正则 + `separator_char`）、类型（`ProbeKind` 枚举 + equality / length 两类新盲注 + 三元组分组键）、显示（GUI kind Tag + separator 高亮 + Collapse 位置明细表）。新字段 `kind` / `separator_char` 均 `#[serde(skip_serializing_if)]`，向后兼容 v0.2.2。

## v0.2.4 验收口径

- 任务集：T4-1 ~ T4-4（见 `docs/03-开发任务清单.md` v0.2.4 段）。
- 全部 `verified_complete` + `cargo test --workspace` 全绿（含 e2e `log_scan_full` 新增 reconstructed_database 断言：schema=="person" / 1 表 person_data / 7 列 / ≥2 行 / row[0].id=Some("1") / row[0].username=contains zhangsan/lisi）。
- Release QA 审计通过后，`docs/versions/0.2.4/更新日志.md` 版本状态由 `in_progress` → `release_complete`。
- v0.2.4 在 v0.2.3 基础上把盲注聚合结果从「逐 read_target 卡片」升级为「按数据库格式还原」：Rust 后端新增 `ReconstructedDatabase` / `ReconstructedTable` / `ReconstructedRow` 三结构 + `BlindAggregator::reconstruct_database` 关联函数，对 4 类标准 read_target（`database()` / `group_concat(table_name) from information_schema.tables` / `group_concat(column_name) from information_schema.columns where table_name='X'` / `group_concat(col1,0xNN,col2,...) from <table>`）交叉关联拼装结构化数据库（schema → tables → 全量列 columns → rows），未 fetch 列留 `None`；GUI 段 ③.5a 用 antd `Table` 按表渲染（全量列做表头，未 fetch 单元格显示 `-`），段 ③.5 改名「原始聚合明细」保留兜底。`Report.extra` 在 `blind_aggregation` 之外新增 `reconstructed_database` 键，向后兼容 v0.2.3。
