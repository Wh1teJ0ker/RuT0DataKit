# v0.2.2 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。
> T2-12 / T2-13 / T2-14 状态由 reviewer 主会话判定为 `verified_complete`；T2-15 本身
> 状态由主会话 Phase 9 判定，coder 阶段先标 `in_progress`。

| 任务 | 状态 | 备注 |
|------|------|------|
| T2-12 blind_aggregator 模块 + BlindProbe/PositionDetail/AggregatedResult 结构 + bytes 自动聚类 + 嵌套正则 | verified_complete | `crates/core/src/logsign/blind_aggregator.rs`：`BlindAggregator::collect_from_entries` 跑自带正则抽 `ascii(substr((<read_target>),N,1))<cmp><thr>` 探针；`aggregate` 按 `(read_target, source_ip)` 分组 + `char_position` 子分组；`true_size = min(body_size)`（fixture-specific，条件成立响应 body 更小）；首个 `beyond_end` 截断拼接；`read_target` 内层嵌套括号用 `(?:[^()]|\([^()]*\))*` 吃单层 `(...)`；`BlindProbe` / `PositionDetail` / `AggregatedResult` 结构 derive Serialize |
| T2-13 Report.extra.blind_aggregation 透传 + 向后兼容 | verified_complete | `crates/core/src/pipeline/log_scan.rs`：`scan_log` 末尾把 `Vec<AggregatedResult>` 序列化进 `Report.extra` 的 `Value::Mapping({blind_aggregation: [...]})`；无盲注探针时 extra 仍为含空数组的 Mapping（前端统一按 sequence 读）；v0.2.1 既有 `Finding.extra: Option<Value>`（skip_serializing_if）行为不破，weak_password/sensitive finding extra=None |
| T2-14 GUI LogView 盲注聚合结果卡片 | verified_complete | `frontend/src/components/LogView.jsx`：read_target inner Card + Text copyable 还原串 + 探针数 + 来源 IP + 位置明细 Tooltip（position/ascii_val/decoded_char/status）+ 空态 antd `Empty`；读 `Report.extra.blind_aggregation` 渲染 |
| T2-15 收尾：版本号 0.2.1 → 0.2.2 + 文档同步 + Phase 7 端到端验收 + QA 报告骨架 | verified_complete | 5 处版本号 bump（Cargo.toml workspace / src-tauri/Cargo.toml / tauri.conf.json / frontend/package.json；crates/core/Cargo.toml `version.workspace = true` 继承无需改）；docs/00/01/02/03/04 + versions/0.2.2（规划需求 + 更新日志）+ QA 报告 §0-§9 骨架 + README×2 同步；Phase 7 cargo build + cargo test（288 passed）+ npm build + tauri build 验收通过（RuT0DataKit_0.2.2_aarch64.dmg ~4.9MB）；Phase 8 Release QA 报告骨架由 coder 建、主会话填充结论 `qa_passed`；Phase 9 版本状态同步 `release_complete` |

## 版本状态

`release_complete` — T2-12 / T2-13 / T2-14 / T2-15 全部 `verified_complete`；Phase 7 端到端验收通过
（`cargo build` / `cargo test` 288 passed / `npm run build` / `npx @tauri-apps/cli build`
产物 `RuT0DataKit_0.2.2_aarch64.dmg` ~4.9MB，Info.plist CFBundleShortVersionString=0.2.2）；
Phase 8 Release QA 报告 `docs/qa/versions/0.2.2/QA-审计报告.md` 结论 `qa_passed`；
Phase 9 版本状态同步本文件 + `docs/04-版本标准.md` 里程碑索引 0.2.2 行 → `release_complete`，
handoff/ 已清理。

> `git tag v0.2.2 + push GitHub SSH`（`git@github.com:Wh1teJ0ker/RuT0DataKit.git`）由主会话执行。

## T2-15 收尾交付明细

1. 版本号 0.2.1 → 0.2.2：
   - `Cargo.toml`（workspace `[workspace.package] version`）
   - `crates/core/Cargo.toml`（`version.workspace = true` 继承 workspace，无需改）
   - `src-tauri/Cargo.toml`（独立 manifest `version`）
   - `src-tauri/tauri.conf.json`（`version` 字段）
   - `frontend/package.json`（`version` 字段）
2. 文档同步：
   - `docs/00-需求文档.md`：§3 补 v0.2.2 范围段（盲注聚合自动还原 flag）。
   - `docs/01-页面与交互说明.md`：LogView 补段 ③.5「盲注聚合结果」描述。
   - `docs/02-技术设计文档.md`：§2.8 补 blind_aggregator 模块 + AggregatedResult/PositionDetail/BlindProbe 结构 + bytes 自动聚类算法 + Report.extra.blind_aggregation 字段 + 嵌套正则 + 向后兼容。
   - `docs/03-开发任务清单.md`：加 v0.2.2 段（T2-12 ~ T2-15 四行 + 依赖链 T2-12→T2-13→T2-14→T2-15）。
   - `docs/04-版本标准.md`：里程碑索引加 0.2.2 行（初始 `in_progress`）；0.2.1 保持 `release_complete`；文件末尾追加「## v0.2.2 验收口径」。
   - `docs/versions/0.2.2/规划需求.md`：范围 + 目标 + 任务清单引用 + 依赖 + 验收 + 未实现/遗留 + 状态。
   - `docs/versions/0.2.2/更新日志.md`：T2-12 ~ T2-14 `verified_complete` + T2-15 `in_progress`。
   - `docs/qa/versions/0.2.2/QA-审计报告.md`：新建 §0-§9 骨架（内容由主会话 Phase 8 填）。
   - `README.md` / `README_EN.md`：功能列表补「盲注二分序列自动聚合还原 flag」；状态行 v0.2.1 → v0.2.2。
3. Phase 7 端到端验收：`cargo build --workspace` / `cargo test --workspace` /
   `cd frontend && npm run build` / `cd src-tauri && npx @tauri-apps/cli@latest build`
   全部通过（产物 .app + .dmg 文件名含 0.2.2）。
4. Phase 8 Release QA：QA 报告 §0-§9 骨架由 coder 建，结论由主会话 Phase 8 填充。
5. Phase 9 版本同步（主会话执行，不在 coder scope）：T2-15 → `verified_complete`，
   版本状态 → `release_complete`，handoff/ 清理，git tag v0.2.2 + push。
