# v0.3.0 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。
> T3-1 / T3-2 / T3-3 / T3-4 状态由 reviewer 主会话判定为 `verified_complete`；T3-5 本身
> 状态由主会话 Phase 9 判定，coder 阶段先标 `in_progress`。

| 任务 | 状态 | 备注 |
|------|------|------|
| T3-1 tshark 调用 + HttpRequest 提取 + hex→bytes 手写 | verified_complete | `crates/core/src/pcap/mod.rs`：新增 `pub mod decoder; pub mod reader; pub mod scanner;` + re-export `decode_url_twice` / `extract_decoded_fields` / `reassemble_base64` / `try_decode_base64_field` / `HttpRequest` / `PcapReader` / `PcapScanner`。`crates/core/src/lib.rs` 加 `pub mod pcap;` + 头注释补 v0.3.0 pcap 模块。`crates/core/src/pcap/reader.rs`：`HttpRequest { frame_no:u32, src_ip:String, dst_ip:String, method:String, host:String, uri:String, body:Option<String>, user_agent:String }`（`#[derive(Debug, Clone, serde::Serialize)]`）；`PcapReader::read(path: &Path) -> Result<Vec<HttpRequest>, CoreError>`：`Command::new("tshark").arg("--version")` 探测 → 失败返回 `CoreError::DependencyMissing("tshark")`；主命令 `tshark -r <path> -Y "http.request" -T fields -e frame.number -e ip.src -e ip.dst -e http.request.method -e http.host -e http.request.uri -e http.file_data -e http.user_agent -E separator=\t -E occurrence=f`；按行 split，每行按 `\t` split 8 字段，<7 字段跳过；`http.file_data` hex 串经手写 `hex_to_bytes(s: &str) -> Option<Vec<u8>>`（trim → empty 返回 Some(empty Vec) → 奇数长度 None → 非 hex 字符 None → 每 2 char 一字节）→ `String::from_utf8_lossy`。3 单测：`hex_to_bytes_basic`（用 `Vec::<u8>::new()` 解决 E0282 推断）/ `hex_to_bytes_utf8_body`（`7b22...7d` → JSON `{...}`）/ `#[ignore] pcap_reader_reads_fixture`（断言 ≥5000 HttpRequest / ≥5000 POST / 首 POST body 含 "username"）。|
| T3-2 双重 URL 解码 + base64 字段解码 + 规则兜底 | verified_complete | `crates/core/src/pcap/decoder.rs`：`decode_url_twice(input: &str) -> String`（独立实现，不依赖 log 模块，避免循环依赖）；`try_decode_base64_field(value: &str) -> Option<String>`（要求 length≥4 + length%4==0 + charset ⊂ [A-Za-z0-9+/=] + 手写 `b64_decode`（不引入 `base64` crate）+ UTF-8 only（GBK 留 v0.3.1+）+ `printable_ratio ≥ 0.8` 防误伤）；`extract_decoded_fields(body: &str) -> Vec<(String, String)>`：先试 `serde_json::from_str` 解析为 Object → 对每个字段值递归 `try_decode_base64_field`；JSON 解析失败再降级 `form-urlencoded`（按 `&` split → `=` split → `decode_url_twice` → `try_decode_base64_field`）；仍失败则整体当单字段 `_body`。`reassemble_base64(field_values: &[String], _param: &str) -> Option<String>`：规则化兜底（fixture 不需要，保留接口）。`crates/core/Cargo.toml` 新增 `serde_json = "1"` 依赖。8 单测：`url_decode_twice_basic` / `decode_fixture_first_post_body`（断言 username=chenyong / name=付里夏旋 / sex=女 / birth=20010905 / idcard=506051200109055743 / phone=74733385248 / address contains 黑龙江+哈尔滨）/ `non_base64_value_untouched` / `phone_11_digits_not_decoded` / `bankcard_16_may_attempt_but_fail_printable` / `form_urlencoded_body` / `plain_text_body_as_single_field` / `reassemble_base64_empty_returns_none`。 |
| T3-3 PcapScanner::scan + pipeline 集成 + e2e | verified_complete | `crates/core/src/pcap/scanner.rs`：`PcapScanner::scan(path: &Path, scan: &DefaultSensitiveScan, rules: &RuleSet) -> Result<Report, CoreError>`：对每个 `HttpRequest` 拼装 `scan_text = decode_url_twice(uri)` + body 经 `extract_decoded_fields` 解出的非空字段值（累加 `decoded_fragments`）→ `scan.scan(&scan_text, rules)` → 每个 `Finding` 填 `location = Some("frame:{frame_no}")` + `context = Some("{method} {host} -> {src_ip}")` + `extra = None` → 累加 `sensitive_hits` + `ip_counts` HashMap。summary `serde_yml::Mapping`：`total_requests` / `sensitive_hits` / `decoded_fragments` / `top_src_ips`（按命中数倒序取前 5，每项 `{ip, hits}` mapping）。返回 `Report { source, kind: "pcap_scan", summary: Value::Mapping, findings, extra: Value::Null }`。`crates/core/src/pipeline/mod.rs` 加 `pub mod pcap_scan;` + `pub use pcap_scan::scan_pcap;`；`crates/core/src/pipeline/pcap_scan.rs` 薄包装透传 `PcapScanner::scan`。`crates/core/tests/e2e.rs` 新增 `#[ignore] pcap_scan_full`：构造显式 `RuleSet`（idcard/phone/name FieldRule validators，非 `load_default_mask_ruleset` 因其 validators 空），断言 `kind=="pcap_scan"` / `total_requests>=5000` / `sensitive_hits>=4000` / findings 类型含 `idcard` + (`name` 或 `phone`) / 所有 findings `location` 以 `"frame:"` 开头 / `top_src_ips` 非空。 |
| T3-4 Tauri 命令 scan_pcap_file + GUI PcapView 四段 | verified_complete | `src-tauri/src/commands.rs`：加 `use ruT0_data_kit_core::pcap::PcapReader;` + `use ruT0_data_kit_core::pipeline::{..., scan_pcap, ...};`；新增 `#[tauri::command] scan_pcap_file(path: String) -> Result<Value, String>`：`PcapReader::read` → `DefaultSensitiveScan::new()` + `pcap_sensitive_ruleset()`（独立 helper，返回 idcard/phone/name FieldRule validators RuleSet，不污染 mask view 的 `load_default_mask_ruleset`）→ `scan_pcap` → `serde_json::to_value(&report)` + `serde_json::to_value(&requests)` → `Ok(json!({ "entries": ..., "report": ... }))`。`src-tauri/src/main.rs` `generate_handler!` 加 `commands::scan_pcap_file,`（共 21 命令）。`frontend/src/state.js` initialState 加 `pcapEntries: []` / `pcapReport: null` / `pcapLoading: false` + reducer `SET_PCAP_ENTRIES` / `SET_PCAP_REPORT` / `SET_PCAP_LOADING`。`frontend/src/tauri.js` 加 `export async function scanPcapFile(path) { return tauriInvoke("scan_pcap_file", { path }); }`。`frontend/src/components/Sidebar.jsx` 删除 pcap 项 `disabled: true`，6 项全 active。`frontend/src/App.jsx` import `PcapView` + FileToolbar guard 加 `"pcap"` + 路由 `{state.activeView === "pcap" && <PcapView state={state} dispatch={dispatch} />}`。`frontend/src/components/PcapView.jsx` 新文件镜像 LogView 四段布局：① 导入 `.pcap/.pcapng` 按钮（`select_file` → `detect_source_type === "pcap"` 校验 → `scanPcapFile`）；② 原始 HTTP 表（frame_no / src_ip / dst_ip / method / host / uri / body slice 200 / user_agent，分页 50）；③ 扫描按钮 + summary Descriptions（total_requests / sensitive_hits / decoded_fragments / top_src_ips volcano Tag）；④ Findings 表（type Tag 按 `TAG_COLOR_BY_TYPE` 着色 idcard=purple / phone=blue / bankcard=magenta / email=cyan / mac=geekblue / username=gold / name=green / value / location=frame:N / context）。tshark 缺失检测 `msg.includes("tshark") || msg.includes("DependencyMissing")` → `message.error("流量分析需要系统 tshark，请先安装 Wireshark CLI (brew install wireshark)")`。0 emoji / 0 原生 select 保持。 |
| T3-5 收尾：版本号 0.2.4 → 0.3.0 + 文档同步 + Phase 7-9 + git tag v0.3.0 + push | verified_complete | 4 处版本号 bump（`Cargo.toml` workspace `[workspace.package] version` 0.2.4 → 0.3.0 / `src-tauri/Cargo.toml` package version 0.2.4 → 0.3.0 / `src-tauri/tauri.conf.json` version 字段 0.2.4 → 0.3.0 / `frontend/package.json` version 字段 0.2.4 → 0.3.0；`crates/core/Cargo.toml` 用 `version.workspace = true` 继承，无需改）；docs/00/01/02/03/04 + versions/0.3.0（规划需求 + 更新日志）+ qa/0.3.0 同步；README×2 状态行 + 功能列表 v0.3.0；Phase 7 `cargo test --workspace`（319 non-ignored + 2 ignored pcap 全绿）+ `npm run build` + `npx @tauri-apps/cli@latest build`（产物 `RuT0DataKit_0.3.0_aarch64.dmg`）；Phase 8 Release QA 报告 `docs/qa/versions/0.3.0/QA-审计报告.md` 结论 `qa_passed`；Phase 9 版本状态同步本文件 + `docs/04-版本标准.md` 里程碑索引 0.3.0 行 → `release_complete`，handoff/ 清理；git commit + tag v0.3.0 + push GitHub SSH。 |

## 版本状态

`release_complete` — T3-1 / T3-2 / T3-3 / T3-4 / T3-5 全部 `verified_complete`；Phase 7 端到端验收通过
（`cargo build --workspace` / `cargo test --workspace`：319 non-ignored 全绿 + 2 `#[ignore]` pcap 测试
（`pcap_reader_reads_fixture` / `pcap_scan_full`）本机 tshark 在场时手动 `cargo test -- --ignored` 全绿；
`npm run build` 2.23s；`cd src-tauri && npx @tauri-apps/cli@latest build` 18.79s 产出
`RuT0DataKit_0.3.0_aarch64.dmg` + `RuT0DataKit.app`，Info.plist CFBundleShortVersionString=0.3.0）；
Phase 8 Release QA 报告 `docs/qa/versions/0.3.0/QA-审计报告.md` 结论 `qa_passed`；
Phase 9 版本状态同步本文件 + `docs/04-版本标准.md` 里程碑索引 0.3.0 行 → `release_complete`，
handoff/ 已清理。

> `git tag v0.3.0 + push GitHub SSH`（`git@github.com:Wh1teJ0ker/RuT0DataKit.git`）由主会话执行。

## T3-5 收尾交付明细

1. 版本号 0.2.4 → 0.3.0：
   - `Cargo.toml`（workspace `[workspace.package] version`）
   - `crates/core/Cargo.toml`（`version.workspace = true` 继承 workspace，无需改）
   - `src-tauri/Cargo.toml`（独立 manifest `version`）
   - `src-tauri/tauri.conf.json`（`version` 字段）
   - `frontend/package.json`（`version` 字段）
2. 文档同步：
   - `docs/00-需求文档.md`：§3 补 v0.3.0 范围段（流量包 pcap 处理切片）。
   - `docs/01-页面与交互说明.md`：加 PcapView 四段布局说明。
   - `docs/02-技术设计文档.md`：补 pcap 模块结构 + HttpRequest + decoder + PcapScanner + scan_pcap pipeline。
   - `docs/03-开发任务清单.md`：加 v0.3.0 段（T3-1 ~ T3-5 五行 + 依赖链 T3-1→T3-2→T3-3→T3-4→T3-5）。
   - `docs/04-版本标准.md`：里程碑索引 0.3.0 行状态 `planned` → `release_complete`；文件末尾追加「## v0.3.0 验收口径」。
   - `docs/versions/0.3.0/规划需求.md`：状态 `planned` → `release_complete`。
   - `docs/versions/0.3.0/更新日志.md`：T3-1 ~ T3-5 `verified_complete` + 版本状态 `release_complete`。
   - `docs/qa/versions/0.3.0/QA-审计报告.md`：新建 §0-§9（主会话填充）。
   - `README.md` / `README_EN.md`：功能列表补「流量包分析（pcap/pcapng → tshark → HTTP 字段提取 + 双重 URL 解码 + base64 字段解码 + 敏感扫描）」；状态行 v0.2.4 → v0.3.0。
3. Phase 7 端到端验收：`cargo build --workspace` / `cargo test --workspace`（319 non-ignored +
   2 ignored pcap）/ `cd frontend && npm run build` / `cd src-tauri && npx @tauri-apps/cli@latest build`
   全部通过（产物 .app + .dmg 文件名含 0.3.0）。
4. Phase 8 Release QA：QA 报告 §0-§9 填充，结论 `qa_passed`。
5. Phase 9 版本同步（主会话执行）：T3-5 → `verified_complete`，
   版本状态 → `release_complete`，handoff/ 清理，git tag v0.3.0 + push。
