# v1.0.0 更新日志

> 版本：1.0.0
> 主题：纯框架 shell（从零重写新基线）
> 状态：`planned`
> 公开 Release：暂无（未发布）

## 版本摘要

v1.0.0 是 RuT0DataKit 从零重写后的首个里程碑，交付纯框架 shell：四区布局 + Sheet/Tab 工作台 + antd Table + SQLite 全量持久 + Tauri updater 签名 + AI 占位 + 设置入口。历史 v0.8.0 归档于 `release/v0.8.0` 分支。

## 更新内容

### 新增

- 四区布局 shell（上方工具栏：左组 6 项数据操作 + 右组 4 项能力按钮；左侧动态能力面板；中央 Workbench 70%；右侧 AI 面板占位）
- Sheet/Tab 多页工作台 + antd Table（选区 / 列选 / 列序 / 状态高亮骨架）
- SQLite 全量数据持久（5 表 + 3 索引 + 迁移）
- CSV / XLSX 导入流（选文件 → 识别 → 新建 Sheet → 写 DB → 渲染 Table）
- Tauri updater 自动检查更新（Ed25519 签名）
- AI IPC 契约占位（`ai_suggest` / `invoke_ai_op` 空实现）
- 设置页（updater 检查 / tshark 路径占位 / DB 路径展示 / 关于）
- 操作日志（`import` / `column_rename` / `column_reorder` 三类）

### 明确不做（推迟至 v1.1+）

- 脱敏 / 校验 / 提取 / 规则 / 搜索 / Tools / PCAP / 真实 AI
- 状态高亮 `invalid/masked/hit` 触发（v1.0.0 仅 `default` 样式）
- 操作日志全类型扩展

## 任务进度表

> 与 `handoff/TASK-BOARD.md` 状态保持一致，冲突以实际验证证据为准。

| 任务 | 名称 | 状态 | 备注 |
|---|---|---|---|
| T1 | 工作区脚手架 | `planned` | Cargo workspace + frontend + src-tauri 初始化 |
| T2 | 四区布局 shell | `planned` | antd Layout |
| T3 | Sheet/Tab + antd Table | `planned` | 选区/列选/列序/状态高亮骨架 |
| T4 | SQLite 持久层 | `planned` | rusqlite bundled + 5 表 + 3 索引 |
| T5 | 导入流 | `planned` | CSV/XLSX → DB → Table |
| T6 | Tauri updater 插件 | `planned` | 签名密钥 + updater JSON |
| T7 | AI 占位 IPC 契约 | `planned` | ai_suggest / invoke_ai_op 空实现 |
| T8 | 设置页 + 能力面板占位提示 | `planned` | 设置 4 卡片 + 4 项能力面板占位 |
| T9 | 双语 README + docs 收口 + QA | `planned` | 文档收口 + Release QA |

## 当前状态

`planned` — 已规划范围，未开始实现。待任务进入分派后逐项推进至 `verified_complete`，端到端验收后推进至 `done_e2e`，经 Release QA 审计后推进至 `qa_passed`，finalize 成功后为 `release_complete`。

## 公开 Release

暂无。v1.0.0 尚未发布，待 QA 通过后打 `v1.0.0` tag 并走 draft → finalize 流程。
