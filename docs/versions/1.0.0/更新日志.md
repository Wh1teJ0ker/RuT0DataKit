# v1.0.0 更新日志

> 版本：1.0.0
> 主题：纯框架 shell（从零重写新基线）
> 状态：`planned`（T1~T9 全部 `verified_complete`，待端到端 + Release QA）
> 公开 Release：暂无（未发布）

## 版本摘要

v1.0.0 是 RuT0DataKit 从零重写后的首个里程碑，交付纯框架 shell：四区布局 + Sheet/Tab 工作台 + antd Table + SQLite 全量持久 + Tauri updater 签名 + AI 占位 + 设置入口。历史 v0.8.0 归档于 `release/v0.8.0` 分支。T4-hotfix（edb3ad6）修复了 SQLite 多列分页语义缺陷；T5 实际依赖 calamine 0.26（非 0.27）进行 XLSX 解析。

## 更新内容

### 新增

- 四区布局 shell（上方工具栏：左组 6 项数据操作 + 右组 4 项能力按钮；左侧动态能力面板；中央 Workbench 70%；右侧 AI 面板占位）
- Sheet/Tab 多页工作台 + antd Table（选区 / 列选 / 列序 / 状态高亮骨架）
- SQLite 全量数据持久（5 表 + 3 索引 + 迁移）
- CSV / XLSX 导入流（选文件 → 识别 → 新建 Sheet → 写 DB → 渲染 Table）
- Tauri updater 自动检查更新（Ed25519 签名）
- AI IPC 契约占位（`ai_suggest` / `invoke_ai_op` 空实现）
- 设置页（updater 检查 / tshark 路径占位 / DB 路径展示 / 关于）
- 操作日志（`import` / `column_rename` / `column_reorder` 三类）

### 明确不做（推迟至 v1.1+）

- 脱敏 / 校验 / 提取 / 规则 / 搜索 / Tools / PCAP / 真实 AI
- 状态高亮 `invalid/masked/hit` 触发（v1.0.0 仅 `default` 样式）
- 操作日志全类型扩展

## 任务进度表

> 与 `handoff/TASK-BOARD.md` 状态保持一致，冲突以实际验证证据为准。

| 任务 | 名称 | 状态 | 备注 |
|---|---|---|---|
| T1 | 工作区脚手架 | `verified_complete` | Cargo workspace + frontend + src-tauri 初始化；cargo check + vite build + cargo tauri dev 链路全通 |
| T2 | 四区布局 shell | `verified_complete` | antd Layout 四区 + TopToolbar 左6/右4 + SidePanel 动态面板 + AiPanel 折叠（collapsedWidth=48）+ Workbench 空态；pnpm build 通过 |
| T3 | Sheet/Tab + antd Table | `verified_complete` | SheetTabs(新建/切换/关闭/双击重命名) + DataTable(行复选+Shift区间/列显隐Dropdown/@dnd-kit列拖拽/rowClassName枚举/pageSize=50) + state.js 8 action; mock 100 行; pnpm build 3060 modules 通过; review_passed |
| T4 | SQLite 持久层 | `verified_complete` | rusqlite bundled + DbManager(Mutex<Connection>, Send+Sync) + 5 表 + 3 索引 + 迁移框架；cargo test db:: 8/8 pass；review_passed；**T4-hotfix**（edb3ad6）：count_rows→COUNT(DISTINCT row_idx) 返回行数、query_cells→子查询行级分页、新增多列分页单测，修复 T5 review 暴露的多列分页语义缺陷；9/9 db 测试全绿 |
| T5 | 导入流 | `verified_complete` | CSV/XLSX(detect_format) → import_file(create_session+create_sheet+分批 write_cells 5000/批+log_operation kind=import) → get_sheet_data(行级分页+BTreeMap 分组+跳过表头) → 前端(IMPORT_SUCCESS/SET_SHEET_DATA)；ImportResult/PageData camelCase；T4-hotfix 后分页语义正确；cargo check + cargo test(9 db+3 datasource) + pnpm build 全过；T5 review 初判 rejected（T4 DB 层缺陷），T4-hotfix 修复后重判 passed；review_passed |
| T6 | Tauri updater 插件 | `verified_complete` | Ed25519 密钥对生成（pubkey 入 tauri.conf.json，私钥 ~/.tauri 不入库 + .gitignore *.key）+ updater 插件注册 + check_update/install_update 命令（无网静默降级 available=false）+ updater-密钥.md 文档；cargo check 通过；review_passed |
| T7 | AI 占位 IPC 契约 | `verified_complete` | AiContext/AiSuggestion struct(camelCase) + ai_suggest/invoke_ai_op 空实现(v1.1+ 错误) + generate_handler 注册 + tauri.js 封装 + AiPanel 手动触发按钮(.catch 降级)；cargo check + pnpm build 通过；review_passed |
| T8 | 设置页 + 能力面板占位提示 | `verified_complete` | SettingsView + 4 卡片(UpdateCard 内联 invoke check_update/install_update 降级不崩溃 / TsharkPathCard v1.3+ 占位 / DbPathCard 硬编码路径+alert / AboutCard v1.0.0+链接) + Workbench settings 分支集成 + 能力面板文案核对(Mask/Validate v1.1+，Extract/Rules v1.2+ 与文档一致)；pnpm build 通过；review_passed |
| T9 | 双语 README + docs 收口 + QA | `verified_complete` | 双语 README 收口（去「规划阶段」免责声明 + 项目结构 + v1.0.0 能力边界简表 + Quick Start 已验证可复现）+ docs 一致性校验 + QA 审计静态项预填；T9 review_passed（2 info 级非阻塞）；待端到端 + Release QA 推进 `qa_passed` |

## 当前状态

`planned` — 框架阶段 T1~T9 全部 `verified_complete`。待端到端验收推进至 `done_e2e`，经 Release QA 审计后推进至 `qa_passed`，finalize 成功后为 `release_complete`。

## 公开 Release

暂无。v1.0.0 尚未发布，待 QA 通过后打 `v1.0.0` tag 并走 draft → finalize 流程。
