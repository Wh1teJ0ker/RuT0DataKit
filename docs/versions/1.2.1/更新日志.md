# v1.2.1 更新日志

> 版本状态：`已通过端到端验收` — 大 TXT 文件性能修复 + 手机号规则一致性修复 + 架构优化，待用户显式 `/tag v1.2.1` 后进入 Release QA。需求见 [`规划需求.md`](./规划需求.md)。

## 端到端验收记录（2026-08-15）

### 阶段一：大 TXT 文件性能修复（T1–T4）

| # | 验收项 | 结果 |
|---|---|---|
| 1 | `npx vite build` 通过 | ✓ built in 2.47s |
| 2 | `cargo check` 通过 | ✓ Finished dev profile, 0 error |
| 3 | CSV/JSON/XLSX/LOG 行为不变，SCHEMA_VERSION=5 | ✓ SCHEMA_VERSION=5 未变，datasource 模块文件未增减 |
| 4 | 8.5 MB 单行 TXT 经 T1 修复产出多行 | ✓ `chunk_string`+`lines()` 分块逻辑就位，48 datasource tests passed |
| 5 | DataTable 对超长单元格截断 + highlightCell 防护 | ✓ `HIGHLIGHT_BYTE_LIMIT=50000` + `ellipsis:true` 就位 |
| 6 | ExtractPanel 超阈值输入跳过主线程同步正则 | ✓ `EXTRACT_INPUT_LIMIT=50000` + `skippedLargeCells` 提示就位 |

### 阶段二：手机号规则修复 + 架构优化（T5–T10）

| # | 验收项 | 结果 |
|---|---|---|
| 7 | `check_phone_prefix("799…", &[])` → `true` | ✓ 空名单=不过滤 |
| 8 | `is_valid_phone("799…", &[])` → `true`；`…, &["134"])` → `false` | ✓ 完整校验（11 位+全数字+前缀） |
| 9 | `validate_extracted_with_params(PhonePrefix{[]}, "799…")` → `(true, "")` | ✓ PhonePrefix 分支统一走 `is_valid_phone` |
| 10 | UI 标签统一「留空=不限」 | ✓ RulesPanel/ExtractPanel/ValidatePanel/MaskPanel 一致 |
| 11 | `func_validator.rs` 拆为 `validators/` 子模块 | ✓ 8 文件（mod/luhn/ip/idcard/personal/datetime/address/email/generic） |
| 12 | `rules/mod.rs` 拆为 `rule.rs` + `registry.rs` + `builtins.rs` | ✓ mod.rs ≤ 50 行纯 re-export |
| 13 | `extract_ops.rs` 共享 helper 消除重复 | ✓ `validate_single`/`query_all_data_cells`/`read_headers`/`write_sheet` 被 extract_ops+mask_ops 共用 |
| 14 | `RulesPanel.jsx` ≤ 500 行 + `rule/` 子目录 | ✓ 454 行容器 + 7 子文件 |
| 15 | 死代码删除 | ✓ `grep -r "extractColumn\|validateColumn\|PiiExtractor" frontend/src` 无结果 |
| 16 | `npx vite build` + `cargo check` + `cargo test` 通过 | ✓ 3109 modules, 0 error, 180 tests + 15 doc-tests passed |

结论：16/16 端到端验收项全部通过 → `done_e2e`。

## 版本主题

两阶段交付：
1. **性能修复**：解决 8.5 MB 单行无换行 TXT 文件导致前端卡顿/冻结的问题。
2. **一致性修复 + 架构优化**：修复手机号规则管理与实际功能数据不一致（空名单误过滤）；
   拆分 4 个 god-file（extract_ops.rs / func_validator.rs / rules/mod.rs / RulesPanel.jsx）；
   提取共享 helper 消除 ~150 行重复；删除 ~850 行死代码。

## 任务进度表

| 任务 ID | 标题 | 状态 |
|---|---|---|
| T1 | TxtReader 按行/定长分块解析 | verified_complete |
| T2 | DataTable 大单元格截断 + highlightCell 防护 | verified_complete |
| T3 | ExtractPanel 超大输入不再主线程同步冻结 | verified_complete |
| T4 | 构建验证 + 版本号升级 + 提交 | verified_complete |
| T5 | 手机号校验一致性修复 | verified_complete |
| T6 | 删除死代码 | verified_complete |
| T7 | 拆分 func_validator.rs + rules/mod.rs | verified_complete |
| T8 | extract_ops 提取复用 helper | verified_complete |
| T9 | 拆分 RulesPanel.jsx | verified_complete |
| T10 | 构建验证 + 版本文档 | verified_complete |
