# v0.1.1 规划需求

## 范围

日志处理切片：access.log 等 CLF 日志的解析、攻击特征识别、敏感数据扫描、JSON 报告。

## 目标

- Log reader（CLF 正则解析）→ LogEntry。
- 攻击特征签名：SQLi / 路径穿越 / 命件注入 / 弱口令 / 越权。
- 复用 v0.1.0 的 SensitiveScan + Report/Finding，产出 Report(kind=log)。
- GUI log 入口启用 + 报告展示。
- 补齐 validators：birth / address / password / ip（v0.1.0 只落地了最小子集）。

## 任务清单

见 `docs/03-开发任务清单.md` v0.1.1 表（T1-1 ~ T1-6）。

## 依赖

- 须 v0.1.0 `release_complete`。

## 验收（规划，派 coder 时细化）

- 选 `tests/fixtures/samples/log/access.log` → 100% 解析成功。
- 识别 SQLi（`1'%20or%20ascii(substr(...))`）、弱口令（guest/123456、admin/admin）。
- JSON 报告含命中明细。

## 状态

`planned` — 待 v0.1.0 发布后启动。
