# v0.2.3 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。
> T3-1 / T3-2 / T3-3 状态由 reviewer 主会话判定为 `verified_complete`；T3-4 本身
> 状态由主会话 Phase 9 判定，coder 阶段先标 `in_progress`。

| 任务 | 状态 | 备注 |
|------|------|------|
| T3-1 blind_aggregator 算法 + 正则优化（true_size 众数 + 两层嵌套 + separator_char） | verified_complete | `crates/core/src/logsign/blind_aggregator.rs`：把 v0.2.2 的 `true_size = min(body_size)`（fixture-specific 假设，反向场景会误判）改为 `mode_per_position_true_size`——按 `char_position` 分组 → 每个「混合位置」取真假两簇 `body_size` 中较小者（fixture：true 簇更小）/ 较大者（反向场景：true 簇更大）作为该位置 true_size → 跨位置众数（并列取较小者）→ 无混合位置退化 `min(body_size)`；fixture 第 4 read_target（`group_concat(id,0x7e,username,0x7e,idcard)`）false 频次 737 > true 669，旧全局频次法误判为 875 导致全 `?`，新算法每位置 true 簇 = 862 → 众数 862 正确解出 `1~zhangsan~...~lisi~...`；反向场景（true body 更大）每位置 max=true 簇，众数仍正确。自带正则从单层 `(?:[^()]\|\([^()]*\))*` 扩到两层 `((?:[^()]\|\((?:[^()]\|\([^()]*\))*\))*)` 吃掉 `where table_schema=database()`。`AggregatedResult` 新增 `separator_char: Option<char>`（`#[serde(skip_serializing_if)]`）：从 `read_target` 首个 `0x([0-9a-fA-F]{2})` 字面量解码（`0x7e`→`~`），`0xff+` 返回 `None`。模块注释删除 v0.2.2「true 探针频次显著高于 false」错误前提。e2e `log_scan_full` 新增第 4 read_target 三段断言（`separator_char==Some('~')` / `decoded_string.starts_with("1~")` / `contains("~lisi~")` / `resolved_chars>0`）防回归。 |
| T3-2 盲注类型扩展（equality / length + ProbeKind 枚举 + 三元组分组键） | verified_complete | `crates/core/src/logsign/blind_aggregator.rs`：`BlindProbe` 新增 `probe_kind: ProbeKind`（`AsciiBinary` / `Equality` / `Length`）+ `equality_char: Option<char>`；新增 2 条正则：equality `substr\s*\(\s*\(\s*((?:[^()]\|\((?:[^()]\|\([^()]*\))*\))*)\s*\)\s*,\s*(\d+)\s*,\s*\d+\s*\)\s*=\s*(?:'([^'])'\|char\s*\(\s*(\d+)\s*\))` 捕获 read_target / position / 字符字面量或 char() 码点，直接解出单字符；length `length\s*\(\s*\(\s*((?:[^()]\|\((?:[^()]\|\([^()]*\))*\))*)\s*\)\s*\)\s*(>=?\|<=?\|=)\s*(\d+)` 捕获 read_target / 比较符 / 阈值，取最小 `=` 或最大 `<` 阈值作为字符串长度。聚合分组键由 `(read_target, source_ip)` 升级为 `(read_target, source_ip, ProbeKind)` 三元组，避免同一 read_target 的 ascii_binary 与 length 探针串扰。`AggregatedResult` 新增 `kind: Option<String>`（序列化为 `ascii_binary` / `equality` / `length`，`#[serde(skip_serializing_if)]`）。`PositionDetail.status` 新增 `equality_resolved` / `length_resolved`。`crates/core/src/logsign/mod.rs` re-export `ProbeKind`。单测 4 条：`equality_probe_resolves_single_char` / `equality_probe_char_form` / `length_probe_resolves_numeric` / `mixed_kinds_no_cross_contamination`。 |
| T3-3 GUI 段 ③.5 显示优化（kind Tag + separator 高亮 + Collapse 位置明细表） | verified_complete | `frontend/src/components/LogView.jsx`：antd import 加 `Collapse`；新增 `inferKind(r)` / `KIND_TAG_COLOR`（ascii_binary=red / equality=orange / length=blue）/ `STATUS_TAG_COLOR`（resolved=green / unresolved_all_true=orange / beyond_end=default / insufficient_probes=red / equality_resolved=blue / length_resolved=purple）/ `renderDecodedString(r)`（separator_char 存在→`Text strong` + volcano Tag 包裹分隔符高亮分段 + 独立 Button type=link「复制」调 `navigator.clipboard.writeText`；否则 `Text strong copyable`）。段 ③.5 inner Card：extra = kind Tag + 已解 N/M Tag；还原结果用 `renderDecodedString`；删除 v0.2.2 的 JSON.stringify Tooltip；Descriptions 下方加 `Collapse`（ghost / size=small / 默认折叠 / label=`位置明细（N 项）`），展开后内嵌 `Table`（size=small / pagination=false / scroll y=200 / rowKey=position），列 位置 / 字符（resolved 显示字符本体 `Text code`，unresolved/beyond_end/insufficient 显示 `-`，length_resolved 显示 `ascii_val` 数值）/ ASCII / 探针数 / 状态 Tag。空态保持 antd `Empty`。0 emoji / 0 原生 select 保持。 |
| T3-4 收尾：版本号 0.2.2 → 0.2.3 + 文档同步 + Phase 7-9 + git tag v0.2.3 + push | verified_complete | 5 处版本号 bump（Cargo.toml workspace / src-tauri/Cargo.toml / tauri.conf.json / frontend/package.json → 0.2.3；crates/core/Cargo.toml `version.workspace = true` 继承无需改）；docs/00/01/02/03/04 + versions/0.2.3（规划需求 + 更新日志）+ QA 报告 §0-§9 同步；README×2 状态行 + 功能列表 v0.2.3；Phase 7 cargo build + cargo test（301 passed）+ npm build + tauri build 验收通过（RuT0DataKit_0.2.3_aarch64.dmg ~5.0MB，Info.plist CFBundleShortVersionString=0.2.3）；Phase 8 Release QA 报告结论 `qa_passed`；Phase 9 版本状态同步 `release_complete`；handoff/ 清理；git commit + tag v0.2.3 + push GitHub SSH。 |

## 版本状态

`release_complete` — T3-1 / T3-2 / T3-3 / T3-4 全部 `verified_complete`；Phase 7 端到端验收通过
（`cargo build` / `cargo test` 301 passed / `npm run build` / `npx @tauri-apps/cli build`
产物 `RuT0DataKit_0.2.3_aarch64.dmg` ~5.0MB，Info.plist CFBundleShortVersionString=0.2.3）；
Phase 8 Release QA 报告 `docs/qa/versions/0.2.3/QA-审计报告.md` 结论 `qa_passed`；
Phase 9 版本状态同步本文件 + `docs/04-版本标准.md` 里程碑索引 0.2.3 行 → `release_complete`，
handoff/ 已清理。

> `git tag v0.2.3 + push GitHub SSH`（`git@github.com:Wh1teJ0ker/RuT0DataKit.git`）由主会话执行。

## T3-4 收尾交付明细

1. 版本号 0.2.2 → 0.2.3：
   - `Cargo.toml`（workspace `[workspace.package] version`）
   - `crates/core/Cargo.toml`（`version.workspace = true` 继承 workspace，无需改）
   - `src-tauri/Cargo.toml`（独立 manifest `version`）
   - `src-tauri/tauri.conf.json`（`version` 字段）
   - `frontend/package.json`（`version` 字段）
2. 文档同步：
   - `docs/00-需求文档.md`：§3 补 v0.2.3 范围段（盲注算法/类型/显示三轴优化）。
   - `docs/01-页面与交互说明.md`：LogView 段 ③.5 描述补 v0.2.3 三行为（kind Tag / separator 高亮 / Collapse 位置明细表）。
   - `docs/02-技术设计文档.md`：§2.8.4 补 `mode_per_position_true_size` 算法 + 两层嵌套正则 + `separator_char` + ProbeKind 枚举 + equality/length 正则 + 三元组分组键 + `kind` 字段 + `equality_resolved`/`length_resolved` status。
   - `docs/03-开发任务清单.md`：加 v0.2.3 段（T3-1 ~ T3-4 四行 + 依赖链 T3-1→T3-2→T3-3→T3-4）。
   - `docs/04-版本标准.md`：里程碑索引加 0.2.3 行（`release_complete`）；0.2.2 保持 `release_complete`；文件末尾追加「## v0.2.3 验收口径」。
   - `docs/versions/0.2.3/规划需求.md`：范围 + 目标 + 任务清单引用 + 依赖 + 验收 + 未实现/遗留 + 状态。
   - `docs/versions/0.2.3/更新日志.md`：T3-1 ~ T3-3 `verified_complete` + T3-4 `verified_complete`。
   - `docs/qa/versions/0.2.3/QA-审计报告.md`：新建 §0-§9（主会话填充）。
   - `README.md` / `README_EN.md`：功能列表补「盲注三轴优化（true_size 众数 + equality/length 类型 + GUI 高亮）」；状态行 v0.2.2 → v0.2.3。
3. Phase 7 端到端验收：`cargo build --workspace` / `cargo test --workspace` /
   `cd frontend && npm run build` / `cd src-tauri && npx @tauri-apps/cli@latest build`
   全部通过（产物 .app + .dmg 文件名含 0.2.3）。
4. Phase 8 Release QA：QA 报告 §0-§9 填充，结论 `qa_passed`。
5. Phase 9 版本同步（主会话执行）：T3-4 → `verified_complete`，
   版本状态 → `release_complete`，handoff/ 清理，git tag v0.2.3 + push。
