# v1.1.1 更新日志

> 版本：1.1.1
> 状态：🔲 待发布
> 前置：v1.1.0 已 qa_passed 并发布 tag `v1.1.0`

## 本期目标

在 v1.1.0 基础上完成四大功能：

1. **撤销/重做**：mask / replace_in_column / replace_all 操作可撤销可重做；优化 DB 交互，避免纯内存阶段，保证数据可保存。
2. **列操作**：(1) 将某列以 JSON 格式解析，展开为新 Tab；(2) 某列批量字段替换。
3. **搜索**：关键字 + 正则搜索，全局搜索替换，优化索引算法避免大文件卡顿。
4. **测试优化**：commands 层新增测试，更全面详细。

## 进度表

| ID | 任务 | 状态 | 备注 |
|---|---|---|---|
| T29 | DB schema v2→v3（before_snapshot + idx_cells_sheet_col + 7+1 个新方法） | ✅ 已完成 | 2026-08-07 verified_complete；review_passed；39 tests 全绿；schema v3 + 8 方法 + LIKE 转义 + 单事务 replace |
| T30 | undo/redo 后端命令 | ✅ 已完成 | 2026-08-07 verified_complete；review_passed；5 commands 层 tests 全绿；undo/redo/list_undoable_operations + mask_column 改用 log_operation_with_snapshot（before/after 快照） |
| T31 | 搜索后端命令（search_cells + replace_all） | ✅ 已完成 | 2026-08-07 verified_complete；review_rejected→修复重复 §4.7 + v1.2.0→v1.1.1 版本标注→review_passed；15 tests 全绿；search_cells 关键字/正则分页 + replace_all 单事务 + before/after 快照撤销 |
| T32 | 列操作后端命令（parse_column_as_json + replace_in_column） | ✅ 已完成 | 2026-08-07 verified_complete；review_passed；7 tests 全绿；parse_column_as_json JSON 列展开为新 sheet（不入撤销栈）+ replace_in_column 单事务列内替换 + before/after 快照撤销 |
| T33 | 前端 IPC + state 扩展 | ✅ 已完成 | 2026-08-08 verified_complete；review_passed；pnpm build 通过（vite 3078 modules 2.17s）；7 IPC（undo/redo 不传 sheetId，searchCells colIdx 可 null）+ 5 ACTION + factory searchHits + createSheetFromParse + APPLY_SEARCH_HITS rowKey 构造（pageInnerIdx=rowIdx-1-pageBase，越界跳过） |
| T34 | 前端 UI（撤销工具栏 + 搜索栏 + 列操作面板） | ✅ 已完成 | 2026-08-08 verified_complete；review_passed（3 minor：全局替换未刷 undoStack / 空表 listUndoable 噪音 / ColumnOpsPanel 空表守卫，均非阻塞）；pnpm build 通过（vite 3079 modules 2.13s）；DataTable 搜索栏+mark 高亮+全局替换 Modal / Workbench 撤销重做工具栏（redoStack 本地维护）/ ColumnOpsPanel JSON 解析新 Tab + 列内替换 / SidePanel+TopToolbar 注册 columnOps / AppContext 5 dispatcher / state.js barrel createSheetFromParse |
| T35 | 版本号升级 1.1.0→1.1.1 | ✅ 已完成 | 2026-08-08 verified_complete；主会话直接实施（无三件套）；4 文件改动（Cargo.toml workspace.package.version=1.1.1 + 加 v1.1.1 注释行、tauri.conf.json version=1.1.1、frontend/package.json version=1.1.1、frontend/src/constants.js APP_VERSION="v1.1.1"）；core/src-tauri 用 workspace=true 自动继承；cargo check --workspace 通过 + pnpm --prefix frontend build 通过（vite 2.17s）；commit a3cf70b |
| T36 | 文档收口 + 全量验证 + Release QA | 🔲 待启动 | 依赖 T35 |

## 关键设计决策

- 搜索索引：LIKE + 复合索引（idx_cells_sheet_col）+ 分页，不引入 FTS5。
- 撤销范围：仅就地变更操作（mask / replace_in_column / replace_all）；import / parse_json / validate / extract 不入撤销栈。
- 安全约束：所有 SQL 用 `?N` + `params![]` 参数绑定；LIKE 转义 `%`/`_`/`\`；正则编译失败返回错误不 panic；快照仅存变更列 cells。
