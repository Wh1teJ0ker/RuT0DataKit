# v0.2.4 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。
> T4-1 / T4-2 / T4-3 状态由 reviewer 主会话判定为 `verified_complete`；T4-4 本身
> 状态由主会话 Phase 9 判定，coder 阶段先标 `in_progress`。

| 任务 | 状态 | 备注 |
|------|------|------|
| T4-1 ReconstructedDatabase 结构 + reconstruct_database 算法 + 单测 | verified_complete | `crates/core/src/logsign/blind_aggregator.rs`：新增 3 结构 `ReconstructedDatabase { schema: Option<String>, tables: Vec<ReconstructedTable>, unmatched_results: Vec<AggregatedResult> }` / `ReconstructedTable { name, columns: Vec<String>, rows: Vec<ReconstructedRow>, row_data_columns: Vec<String>, column_separator: Option<char>, source_probe_count: u32 }` / `ReconstructedRow { cells: Vec<Option<String>> }`（全部 `serde::Serialize`）；新增关联函数 `BlindAggregator::reconstruct_database(&[AggregatedResult]) -> ReconstructedDatabase` 三步算法：Step1 分类（`schema` / `table_list` / `column_list` / `row_data` 4 类，按 `read_target` contains + 正则判别；未匹配进 `unmatched_results`）；Step2 拼装（schema 直填 → 对 table_list 每个表名按 `table_name='T'` 谓词匹配 column_list（无谓词按索引对齐）→ 全量 columns（无列清单兜底为 row_data_columns）→ 按 `from T` 匹配 row_data → 解析 rows：`decoded_string` 按 `,` split 行 + 每行按 `column_separator`（`0xNN` 解码）split 列 → 映射到 `row_data_columns → value` → 投影到 `columns` 全量列，未 fetch 列 `None`）；Step3 unmatched 原样塞回。分类正则：`table_name\s*=\s*'([^']+)'` / `from\s+(\w+)` / `group_concat\s*\(([^)]*)\)` / `0x([0-9a-fA-F]{2})`（复用 `parse_separator_char`）。`crates/core/src/logsign/mod.rs` re-export 3 新结构。8 条单测：`reconstruct_full_fixture_database_view`（4 RT → 1 表 person_data / 7 列 / 2 行 / row[0].cells=[Some("1"),Some("zhangsan"),None,None,None,Some("IDCARD1"),None]）/ `reconstruct_unmatched_results_collected` / `reconstruct_partial_no_column_list_fallback` / `reconstruct_empty_results` / `reconstruct_no_separator_single_column_row` / `parse_table_name_predicate_extracts_name` / `parse_from_table_extracts_name` / `parse_row_data_columns_filters_hex_literals`；helper `mk_result` 构造 AggregatedResult。 |
| T4-2 pipeline 集成 + Report.extra.reconstructed_database 透传 + e2e 断言 | verified_complete | `crates/core/src/pipeline/log_scan.rs`：`scan_log` 末尾调 `let aggregator = BlindAggregator::collect_from_entries(entries); let blind_results = aggregator.aggregate(); let reconstructed = BlindAggregator::reconstruct_database(&blind_results);`，在 `Report.extra` 的 `serde_yml::Mapping` 中并列插入 `blind_aggregation` 与 `reconstructed_database` 两键（v0.2.3 `blind_aggregation` 数组保留不动，新增 `reconstructed_database` 键，向后兼容）。`crates/core/tests/e2e.rs` `log_scan_full` 末尾新增 reconstructed_database 五段断言：`report.extra` 含 `reconstructed_database` mapping；`schema == "person"`；`tables` 含 `name == "person_data"` 且 `columns` 含全 7 列 (id/username/password/sex/birth/idcard/phone)；该表 `rows` ≥2 行；首行 `cells[id] == Some("1")`、`cells[username]` contains "zhangsan" 或 "lisi"；`row_data_columns` 含 id/username/idcard；`column_separator == "~"`。 |
| T4-3 GUI LogView 段 ③.5a 还原数据库视图 + ③.5 改名原始聚合明细 | verified_complete | `frontend/src/components/LogView.jsx`：antd import 加 `Divider`；新增派生 `const reconstructedDb = state.logReport?.extra?.reconstructed_database || null;`；新增 `buildTableColumns(table)`（`table.columns` → antd Table columns，title=列名，dataIndex=列名，未 fetch 单元格 `render: v => v ?? <Text type="secondary">-</Text>`）+ `buildTableDataSource(table)`（`table.rows` → dataSource，每行 object 键=列名，未 fetch 列 undefined，key 用 row index）。在段 ③.5 之前插入段 ③.5a Card「还原数据库视图」：顶部 `Descriptions`（数据库名 `<Text code>` + 表数 Tag + unmatched 数）；`reconstructedDb == null \|\| tables.length === 0` → `<Empty description="无法还原为数据库结构（缺少 database()/table_name/column_name/row data 四类探针）" />`；每张 `ReconstructedTable` 一个 inner Card（title=`<Text code strong>{table.name}</Text>` + 列/行数 Tag；body=antd Table；extra=`column_separator` volcano Tag + `row_data_columns` Tooltip「已 fetch N/M 列」+ 探针 Tag）；unmatched_results 非空 → `Divider` + 简版列表（read_target + decoded_string copyable）。段 ③.5 既有 Card title 由「盲注聚合结果」改为「原始聚合明细」（kind Tag / renderDecodedString / Collapse+Table 位置明细全部保留作兜底），数据源 `blindAggregation` 不动。0 emoji / 0 原生 select 保持。 |
| T4-4 收尾：版本号 0.2.3 → 0.2.4 + 文档同步 + Phase 7-9 + git tag v0.2.4 + push | verified_complete | 4 处版本号 bump（Cargo.toml workspace / src-tauri/Cargo.toml / tauri.conf.json / frontend/package.json → 0.2.4；crates/core/Cargo.toml `version.workspace = true` 继承无需改）；docs/00/01/02/03/04 + versions/0.2.4（规划需求 + 更新日志）+ QA 报告 §0-§9 同步；README×2 状态行 + 功能列表 v0.2.4；Phase 7 cargo build + cargo test + npm build + tauri build 验收通过；Phase 8 Release QA 报告结论 `qa_passed`；Phase 9 版本状态同步 `release_complete`；handoff/ 清理；git commit + tag v0.2.4 + push GitHub SSH。 |

## 版本状态

`release_complete` — T4-1 / T4-2 / T4-3 / T4-4 全部 `verified_complete`；Phase 7 端到端验收通过
（`cargo build` / `cargo test` 全绿 / `npm run build` / `npx @tauri-apps/cli build`
产物 `RuT0DataKit_0.2.4_aarch64.dmg`，Info.plist CFBundleShortVersionString=0.2.4）；
Phase 8 Release QA 报告 `docs/qa/versions/0.2.4/QA-审计报告.md` 结论 `qa_passed`；
Phase 9 版本状态同步本文件 + `docs/04-版本标准.md` 里程碑索引 0.2.4 行 → `release_complete`，
handoff/ 已清理。

> `git tag v0.2.4 + push GitHub SSH`（`git@github.com:Wh1teJ0ker/RuT0DataKit.git`）由主会话执行。

## T4-4 收尾交付明细

1. 版本号 0.2.3 → 0.2.4：
   - `Cargo.toml`（workspace `[workspace.package] version`）
   - `crates/core/Cargo.toml`（`version.workspace = true` 继承 workspace，无需改）
   - `src-tauri/Cargo.toml`（独立 manifest `version`）
   - `src-tauri/tauri.conf.json`（`version` 字段）
   - `frontend/package.json`（`version` 字段）
2. 文档同步：
   - `docs/00-需求文档.md`：§3 补 v0.2.4 范围段（盲注聚合数据库格式还原）。
   - `docs/01-页面与交互说明.md`：LogView 段 ③.5a 还原数据库视图 + 段 ③.5 改名「原始聚合明细」。
   - `docs/02-技术设计文档.md`：§2.8 补 `ReconstructedDatabase` / `ReconstructedTable` /
     `ReconstructedRow` 结构 + `reconstruct_database` 算法 + `Report.extra.reconstructed_database` 字段。
   - `docs/03-开发任务清单.md`：加 v0.2.4 段（T4-1 ~ T4-4 四行 + 依赖链 T4-1→T4-2→T4-3→T4-4）。
   - `docs/04-版本标准.md`：里程碑索引加 0.2.4 行（`release_complete`）；0.2.3 保持
     `release_complete`；文件末尾追加「## v0.2.4 验收口径」。
   - `docs/versions/0.2.4/规划需求.md`：范围 + 目标 + 任务清单引用 + 依赖 + 验收 + 未实现/遗留 + 状态。
   - `docs/versions/0.2.4/更新日志.md`：T4-1 ~ T4-3 `verified_complete` + T4-4 `verified_complete`。
   - `docs/qa/versions/0.2.4/QA-审计报告.md`：新建 §0-§9（主会话填充）。
   - `README.md` / `README_EN.md`：功能列表补「盲注聚合数据库格式还原（schema→tables→columns→rows）」；
     状态行 v0.2.3 → v0.2.4。
3. Phase 7 端到端验收：`cargo build --workspace` / `cargo test --workspace` /
   `cd frontend && npm run build` / `cd src-tauri && npx @tauri-apps/cli@latest build`
   全部通过（产物 .app + .dmg 文件名含 0.2.4）。
4. Phase 8 Release QA：QA 报告 §0-§9 填充，结论 `qa_passed`。
5. Phase 9 版本同步（主会话执行）：T4-4 → `verified_complete`，
   版本状态 → `release_complete`，handoff/ 清理，git tag v0.2.4 + push。
