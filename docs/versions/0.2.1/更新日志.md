# v0.2.1 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。
> T2-7 ~ T2-10 状态由 reviewer 主会话判定为 `verified_complete`；T2-11 本身状态由主会话
> Phase 9 判定，coder 阶段先标 `in_progress`。

| 任务 | 状态 | 备注 |
|------|------|------|
| T2-7 parse_query 解 `+` + LogEntry decoded_* 字段 | verified_complete | `crates/core/src/log/mod.rs`：`parse_query` 按 form-urlencoded 标准先 `+`→space 再双重 `%XX` 解码；`LogEntry` 新增 `decoded_path` / `decoded_query` / `decoded_ua` 字段，`parse_line` 一次性填充；`join_decoded_query` 拼回 `k=v&k2=v2` 形态供签名引擎与 GUI 消费 |
| T2-8 payload_parser 模块 + SignatureHit.parsed_payload | verified_complete | `crates/core/src/logsign/payload_parser.rs`：`ParsedPayload { attack_type, technique, read_target, char_position, compared_ascii, comparator, union_columns, sleep_seconds, summary }` + `parse_payload(decoded_text) -> Option<ParsedPayload>`（6 类正则首命中返回）；`SignatureHit` 新增 `parsed_payload: Option<ParsedPayload>`，`scan_log_entry` 产 hit 时填充 |
| T2-9 Finding.extra + pipeline 透传 + 6 类 pattern 扩变体 | verified_complete | `crates/core/src/report.rs`：`Finding.extra: Option<serde_yml::Value>`（`skip_serializing_if = "Option::is_none"`，向后兼容）；`pipeline/log_scan.rs` 把 sqli hit 的 `parsed_payload` 序列化透传进 `Finding.extra`；weak_password / sensitive / csv_mask 场景 extra=None；`sqli_signatures.yaml` 扩 `union all select` / `exp(~...)` / `floor(rand(0)*2)` 变体 |
| T2-10 GUI LogView decoded 列 + 解析结果/读取目标列 | verified_complete | `frontend/src/components/LogView.jsx`：原始日志表新增 `decoded_path` / `decoded_query` / `decoded_ua` 列；findings 表新增「解析结果」（ParsedPayload.summary）+「读取目标」（read_target）列，读 `Finding.extra` 序列化字段渲染 |
| T2-11 收尾：版本号 0.2.0 → 0.2.1 + 文档同步 + e2e 扩展 + Phase 7 端到端验收 | verified_complete | 5 处版本号 bump（Cargo.toml workspace / crates/core / src-tauri/Cargo.toml / tauri.conf.json / frontend/package.json）；docs/00/01/02/03/04 + versions/0.2.1（规划需求 + 更新日志）+ QA 报告 §0-§9 + README×2 同步；e2e 新增 2 条（payload_parser 端到端 + decoded_query 还原）；Phase 7 cargo build + cargo test（275 passed）+ npm build + tauri build 验收通过（RuT0DataKit_0.2.1_aarch64.dmg 4.9MB）；Phase 8 Release QA 结论 `qa_passed`；Phase 9 版本状态同步 `release_complete` |

## 版本状态

`release_complete` — T2-7 ~ T2-11 全部 `verified_complete`；Phase 7 端到端验收通过
（`cargo build` / `cargo test` 275 passed / `npm run build` / `npx @tauri-apps/cli build`
产物 `RuT0DataKit_0.2.1_aarch64.dmg` 4.9MB）；Phase 8 Release QA 报告
`docs/qa/versions/0.2.1/QA-审计报告.md` 结论 `qa_passed`；Phase 9 版本状态同步本文件 +
`docs/04-版本标准.md` 里程碑索引 0.2.1 行 → `release_complete`，handoff/ 已清理。

> 本项目当前非 git 仓库（环境 `Is a git repository: no`）；`git tag v0.2.1 + push GitHub`
> 由用户按既有外部流程处理，不在本工作区内执行。

## T2-11 收尾交付明细

1. 版本号 0.2.0 → 0.2.1：
   - `Cargo.toml`（workspace `[workspace.package] version`）
   - `crates/core/Cargo.toml`（`version.workspace = true` 继承 workspace）
   - `src-tauri/Cargo.toml`（独立 manifest `version`）
   - `src-tauri/tauri.conf.json`（`version` 字段）
   - `frontend/package.json`（`version` 字段）
2. 文档同步：
   - `docs/00-需求文档.md`：§3 补 v0.2.1 范围段；§4.1 补 v0.2.1 验收项。
   - `docs/01-页面与交互说明.md`：LogView findings 表 ASCII 补「解析结果/读取目标」列 + 原始日志表 decoded_path/decoded_query/decoded_ua 列。
   - `docs/02-技术设计文档.md`：§2.8 补 payload_parser 模块 + ParsedPayload 结构 + Finding.extra 字段 + parse_query `+` 解码语义 + 6 类 pattern 扩变体说明。
   - `docs/03-开发任务清单.md`：加 v0.2.1 段（T2-7 ~ T2-11 五行 + 依赖链）。
   - `docs/04-版本标准.md`：里程碑索引加 0.2.1 行（初始 `in_progress`）；0.2.0 保持 `release_complete`。
   - `docs/versions/0.2.1/规划需求.md`：范围 + 目标 + 任务清单引用 + 未实现/遗留 + 状态。
   - `docs/versions/0.2.1/更新日志.md`：T2-7 ~ T2-11 五条变更摘要 + status。
   - `docs/qa/versions/0.2.1/QA-审计报告.md`：新建空文件占位 + 标题（内容由主会话 Phase 8 填）。
   - `README.md` / `README_EN.md`：功能列表补「SQLi payload 语义解析（6 类）+ 字段还原（query/path/UA）」；状态行 v0.2.0 → v0.2.1。
3. e2e 扩展（`crates/core/tests/e2e.rs`）：
   - 新增 `log_payload_parser_e2e`：构造盲注 entry → `scan_log_entry` → 断言 hit.parsed_payload.read_target == Some("database()")。
   - 新增 `log_decoded_query_plus_decode`：构造 entry query 含 `+` → `parse_line` → 断言 `decoded_query` 含空格而非 `+`。
4. Phase 7 端到端验收：`cargo build --workspace` / `cargo test --workspace` / `cd frontend && npm run build` / `cd src-tauri && npx @tauri-apps/cli@latest build` 全部通过（产物 .app + .dmg 文件名含 0.2.1）。
