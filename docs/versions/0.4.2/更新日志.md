# v0.4.2 更新日志

> 设置模块首期：Sidebar 底部「设置」入口 + tshark 多平台自动检测 + 路径配置持久化 + SettingsView UI；附带 v0.4.1 搜索子串匹配缺陷修正。

## 版本范围

4 项（3 主任务 + 1 附带修正）：

1. **tshark 多平台自动检测**（T7-1）：core 新增 `pcap/detect.rs`，进程级全局覆盖路径 + `detect_tshark` 多平台候选探测；`PcapReader::read` 注入覆盖路径（None 时零回归）。
2. **Tauri 命令 + settings.json 持久化**（T7-2）：`detect_tshark` / `load_tshark_path` / `save_tshark_path` 三命令 + `app_config_dir/settings.json` 持久化 + 运行时注入；版本号 0.4.1 → 0.4.2。
3. **Sidebar 底部「设置」按钮 + SettingsView UI**（T7-3）：Sidebar `flex:1` + 贴底 Button + 状态 Tag；SettingsView Card + Descriptions + 4 操作按钮 + 平台路径 Alert；挂载自动探测。
4. **附带搜索子串匹配修正**（T7 附带）：`search_keyword` 精确匹配 → 子串匹配 `key.contains(term)`，搜「张」命中「张三」；新增 CJK + ASCII 子串测试。

## 版本状态

`release_complete` — 全部任务 `verified_complete` + 端到端集成验收通过 + Release QA 审计 `qa_passed` + 版本文档同步完成。

## 任务进度表

| ID | 标题 | 依赖 | 状态 |
|----|------|------|------|
| T7-1 | core: tshark 路径解析器（`Mutex<Option<String>>` 覆盖路径）+ `detect_tshark` 多平台候选探测 + `PcapReader::read` 注入 + 6 单测 | — | verified_complete |
| T7-2 | tauri: `detect_tshark` / `load_tshark_path` / `save_tshark_path` 三命令 + main.rs 注册 + 版本号 0.4.1 → 0.4.2（4 处） | T7-1 | verified_complete |
| T7-3 | frontend: state 3 字段 + tauri.js 3 封装 + Sidebar 底部「设置」Button + App 路由 + SettingsView + 附带 search 子串匹配修正 + 2 单测 | T7-2 | verified_complete |
| T7-4 | docs + README + 版本日志 + QA 报告 | T7-1~T7-3 | verified_complete |

## 端到端验收项

- tshark 多平台自动检测：`detect_tshark` 6 单测（candidate_paths_covers_three_platforms / set_and_get_roundtrip / ignores_empty / resolve_falls_back / probe_nonexistent / `#[ignore] detect_local`）全绿；本机 tshark v4.4.9 手动 `cargo test -- --ignored` 全绿。
- `PcapReader::read` 注入：覆盖为 None 时 `resolve_tshark_cmd()` 返回 `"tshark"` 字面量，行为与 v0.4.1 完全一致（零回归）。
- Tauri 命令：`cargo build --release` 通过；三个命令在 invoke handler 列表。
- 前端构建：`npm run build` 通过；Sidebar 底部「设置」Button 渲染；SettingsView 挂载自动 `loadTsharkPath` + `detectTshark`。
- settings.json 持久化：手选路径 → 保存 → 重启 → SettingsView 仍显示该路径；清除 → 重启 → 回退 PATH。
- 搜索子串匹配：搜「张」命中「张三」（CJK 子串测试 + ASCII 子串测试全绿）。
- （回归）v0.4.1 已修复行为不退化：Sidebar「Tools」下拉、SQL 盲注自动跳转、RegexTool 构造 Tab。
- `cargo test --workspace` 全绿（v0.4.1 基线 419 non-ignored + 本版本新增 8 单测：6 detect + 2 search 子串）。
- 安全约束（docs/00 §6）保持：tshark 探测/路径配置全本地，settings.json 仅写本地 `app_config_dir`，不调用网络。

## Release QA 门禁

- 报告：`docs/qa/versions/0.4.2/QA-审计报告.md`
- 范围：需求覆盖 / 端到端流程 / 构建与测试 / 代码质量 / 安全与隐私 / 数据与迁移 / 依赖与配置 / 文档一致性。
- 结论 `qa_passed` 后版本状态 `release_complete`。
