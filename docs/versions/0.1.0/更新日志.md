# v0.1.0 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。

| 任务 | 状态 | 备注 |
|------|------|------|
| T0-1 Workspace + core 骨架 + CoreError | verified_complete | — |
| T0-2 规则引擎（YAML RuleSet + 注册表） | verified_complete | — |
| T0-3 内置 maskers | verified_complete | 7 个 masker + register_builtin_maskers + default_masker_registry，37 tests pass |
| T0-4 内置 validators 最小子集 | verified_complete | 7 个 validator + register_builtin_validators + default_validator_registry，39 tests pass；idcard 样本矛盾由主会话修复（...768→...769 正例，原样本作反例） |
| T0-5 CSV/Xlsx reader + 脱敏 pipeline | verified_complete | readers/csv_reader/xlsx_reader + mask_pipeline + Report(kind=csv_mask)，e2e 全字段断言通过；xlsx fixture 5043B |
| T0-6 SensitiveScan trait + DefaultSensitiveScan + 统一 Report | verified_complete | scan/mod.rs，5 tests pass；提取 pattern 表覆盖 7 validator；report/mod.rs 复用未改 |
| T0-7 内置规则 YAML default_mask | verified_complete | rules/default_mask.yaml + custom_example.yaml + include_str! 内嵌 + load_default_mask_ruleset() |
| T0-8 Tauri GUI（csv/xlsx 脱敏入口，pcap/log 置灰） | verified_complete | Tauri v2（环境无 v1），src-tauri + frontend 编译通过；cargo tauri dev 待人工验收 |
| T0-9 E2E + README | verified_complete | crates/core/tests/e2e.rs 6 tests pass；README.md + README_EN.md；状态 in_progress 非已发布 |
| T0-10 Core 富规则 + 行选择 pipeline + 测试 | verified_complete | 4 个新 masker（regex_replace/regex_extract/delete/replace）+ mask_pipeline_selected；e2e +3 tests（富规则契约 + 行选择 + 越界忽略）|
| T0-11 src-tauri load_preview / apply_rules 命令 | verified_complete | 新增 load_preview / apply_rules / export_selected_csv 三个命令（src-tauri 共 7 个 command）|
| T0-12 frontend 侧边栏重写 | verified_complete | 文件处理视图：导入/预览/行勾选/规则编辑/应用/导出；日志扫描/流量分析置灰占位 |
| T0-13 E2E 扩展 + 文档同步 | verified_complete | e2e.rs +2 tests（富规则综合 + 行选择 sample）；docs/00 §3/§4、docs/02 §3.1、规划需求、更新日志、QA 报告补丁、README/README_EN 同步 |
| T0-14 前端 React + Ant Design 重写 | verified_complete | vanilla JS → React 18 + Vite 5 + antd 5 + @ant-design/icons 5；纯白主题；无 emoji；下拉 antd Select；tauri.conf.json build 段接 Vite；npm run build + cargo build --release + cargo test 139 passed + GUI smoke PID 85059 通过 |
| Phase C 目录治理 + 文档-代码一致性审查 | verified_complete | 删空目录 references/ + tests/fixtures/rules/；.gitignore 加 .zcode/；docs/00 §5 补 React+antd 技术栈；docs/01 加版本覆盖说明 + §1.1 v0.1.0 实际形态；docs/02 §3/§4 修前端栈 + Tauri 版本；QA 报告 §0 加基线更新提示 + §4.4 失效项标记 + §5 隐私契约复注；Explore 子 agent 7 项复验全 PASS |
| T0-15 core 列选择 pipeline + validate_pipeline + RuleSet.description | verified_complete | 新建 mask_pipeline_columns + validate_pipeline + MaskRule/FieldRule description 字段；e2e +5（含 columns/validate）|
| T0-16 src-tauri 命令扩展（列脱敏 + 校验 + 导出 + 规则存取 + xlsx writer） | verified_complete | apply_rules_cols / run_validate / export_records_csv / export_records_xlsx / save_ruleset / read_ruleset 六新命令；rust-xlsxwriter + csv 依赖；src-tauri 共 13 个 command |
| T0-17 frontend 全局 state + 4-tab 导航骨架 | verified_complete | useReducer + Layout + Sidebar 4 active + 2 disabled + view 占位骨架 |
| T0-18 MaskView 列勾选 + ValidateView 完整 + RuleForm 编辑 | verified_complete | PreviewTable 列头部 checkbox + ValidateView 完整 + RuleForm editMode/description |
| T0-19 ExportView + RulesView 完整 + 规则详细说明面板 | verified_complete | ExportView 列选/序/格式 + RulesView YAML 存取 + RuleDocPanel + maskerDefs/validatorDefs 扩充 + js-yaml@5.2.1 |
| T0-20 文档同步 + e2e 扩展 + Phase 7 E2E | verified_complete | docs/00/01/02/规划需求/README 同步四功能范围突破 + e2e +2~3 + Phase 7 端到端验收（cargo test + npm build + tauri build .app/.dmg + GUI smoke） |
| T0-21 core 规则引擎重构（抽象算子 + 预置库 + 试运行 API） | verified_complete | MaskOp（Template/SplitTemplate/RegexReplace/ConstReplace）+ ValidateOp（Regex/Algorithm/RegexWithGuard）+ AlgoKind/GuardKind + MatchMode；预置库 lookup_mask_preset/lookup_validate_preset/list_mask_op_types/list_validate_op_types（10+10 项）；apply_mask_op/apply_validate_op + MaskOp/ValidateOp impl Masker/Validator；build_masker/build_validator 内部改走 from_rule 路径，签名行为不变；e2e +3（mask_op_template_preset_equivalence / mask_op_regex_replace_dispatch / validate_op_regex_preset_equivalence） |
| T0-22 src-tauri 试运行命令 + 算子元信息命令 | verified_complete | preview_mask_rule / preview_validate_rule / list_mask_op_types / list_validate_op_types 四新命令（src-tauri 共 17 个 command）；commands.rs:8 模块注释刷新分组说明 |
| T0-23 RulesView 重构（单一列表 + Drawer + 动态试运行） | verified_complete | RulesView.jsx 重写为单一列表（脱敏+校验合并）+ 右上角 Drawer 添加/编辑（RuleDrawer.jsx）+ 每条规则试运行按钮动态跑首行；state.js 合并 editingRule；tauri.js 加 4 封装；maskerDefs.js/validatorDefs.js 扩充至 10+10 op 类型（共 6 文件） |
| T0-24 MaskView + ValidateView 四段垂直重构 | planned → verified_complete | MaskView.jsx/ValidateView.jsx 重写为四段垂直（原始数据→表头-规则映射→预览→操作面板）；MaskView 删内嵌 RuleForm（grep 0 命中）；应用按钮触发预览；导出按钮跳转 ExportView（共 2 文件） |
| T0-25 ExportView 单一 Table 集成 | verified_complete | ExportView.jsx 重写为单一 antd Table（表头 checkbox + 上下移 + 单元格预览）+ 顶部源/过滤/格式 Select + 导出按钮 CSV+XLSX（共 1 文件） |
| T0-26 文档同步 + e2e 扩展 + Phase 7-9 | verified_complete | docs/00/01/02/04/更新日志/QA/README+README_EN 同步 + commands.rs:8 共 17 + e2e +2（mask_op_split_template_and_const_replace / validate_op_algorithm_and_guard_equivalence）+ Phase 7 E2E（cargo test 168 passed = 145 unit + 23 e2e + npm build + tauri build .app+.dmg + GUI smoke）+ Phase 8 QA §4.4 补丁段 + Phase 9 版本回 release_complete |
| T0-27 规则管理彻底独立 + 双 state 分离 | verified_complete | RulesView 完全去除 YAML 保存/加载按钮（仅留添加/编辑/删除）+ 移除 App.jsx FileToolbar 在规则视图的渲染（rules 视图不再有「导入文件」按钮）+ 试运行改为用户独立输入样例值（新增 `preview_mask_rule_value` / `preview_validate_rule_value` 两个 src-tauri 命令，共 19 个 command）+ state.js 拆 `maskOverrides` / `validateOverrides` 会话级临时映射（SET_FILE 时清空，6 个新 action：SET/CLEAR/CLEAR_ALL × mask/validate）+ MaskView/ValidateView 切到 override 优先 + 全局 rules 兜底（resolveMask/resolveValidator + effectiveMaskers/effectiveValidators 合并不回写）+ 列操作 Tooltip 改「清除该列映射（不影响规则库）」 |
| T0-28 `MaskOp::Template` 边界 BUG 修复 | verified_complete | `apply_template` 重叠判断 `tail_start < head_end` 改为 `<=`，让 `keep_prefix + keep_suffix == 值长度`（中间段长度为 0）的边界也走重叠分支，仅输出 `mask_min_len` 个 `mask_char`；同步修复 `crates/core/src/maskers/custom.rs::CustomMask::mask`。修复前对 `李四海`（n=3, kp=3, ks=0）错误输出 `李四海*`；修复后输出 `*`。新增 2 回归测试（穷举参数组合断言不再输出 `李四海*` + 边界等价测试）；`cargo test --workspace` 151 unit + 23 e2e = 174 passed 全绿；`npm run build` + `npx @tauri-apps/cli@latest build` 重打包 .app + .dmg 通过 |

## 版本状态

`release_complete`（UI 重构 + 规则引擎重构补丁 + 后续双 state 分离与边界 BUG 修复补丁完成，2026-07-18）— v0.1.0 已完成全部三轮重构与两次后续补丁（T0-1 ~ T0-14 + Phase C + T0-15 ~ T0-20 四功能重构 + T0-21 ~ T0-26 UI 重构 + 规则引擎重构 + T0-27 规则管理彻底独立/双 state 分离 + T0-28 Template 边界 BUG 修复）。规则引擎收敛到 `MaskOp`（4 通用算子 Template/SplitTemplate/RegexReplace/ConstReplace）+ `ValidateOp`（3 通用算子 Regex/Algorithm/RegexWithGuard）+ 预置库 + `apply_mask_op`/`apply_validate_op` 公共 API；`build_masker`/`build_validator` 向后兼容；GUI 改四段垂直 view + 单一列表 RulesView（彻底独立、无文件导入按钮、无 YAML 按钮、试运行独立输入）+ 单一 Table ExportView + 规则/映射双 state 分离（临时映射不污染全局规则库）+ `MaskOp::Template` 边界 BUG 修复。Phase 7-9 全部通过。

**UI 重构 + 规则引擎重构补丁完成（T0-21~T0-26，Phase 7-9 已通过）** 6 项 deliverables：
1. core 抽象算子模型：`MaskOp`（Template/SplitTemplate/RegexReplace/ConstReplace）+ `MatchMode` + `apply_mask_op` + `MaskOp::from_rule` + impl `Masker`；`ValidateOp`（Regex/Algorithm/RegexWithGuard）+ `AlgoKind`/`GuardKind` + `apply_validate_op` + `ValidateOp::from_rule` + impl `Validator`；预置库 `lookup_mask_preset`/`lookup_validate_preset`/`list_mask_op_types`/`list_validate_op_types`（10+10 项）。
2. `build_masker`/`build_validator` 签名行为不变，内部改走 `Op::from_rule → apply_*_op` 路径；旧 `MaskRule`/`FieldRule`/`RuleSet`/`mask_pipeline*`/`validate_pipeline` 全保留。
3. src-tauri 4 新命令（`preview_mask_rule`/`preview_validate_rule`/`list_mask_op_types`/`list_validate_op_types`），共 17 个 command；`commands.rs:8` 模块注释刷新为 17 + T0-16/T0-22 分组。
4. frontend RulesView 单一列表（脱敏+校验合并）+ 右上角 Drawer 添加/编辑（`RuleDrawer.jsx`）+ 每条规则「试运行」动态跑首行；`maskerDefs.js`/`validatorDefs.js` 扩充至 10+10 op 类型。
5. MaskView/ValidateView 重写为四段垂直（原始数据 → 表头-规则映射 → 预览 → 操作面板）；MaskView 删内嵌 RuleForm（grep 0 命中）；应用按钮触发预览；导出按钮跳转 ExportView。
6. ExportView 重写为单一 antd Table（表头 checkbox + 上下移 + 单元格预览）+ 顶部源/过滤/格式 Select + 导出 CSV+XLSX。

Phase 7 端到端验收实测：
- `cargo build --workspace` ✅（仅 `non_snake_case` 已知 warning）
- `cargo test --workspace` ✅ 168 passed = 145 unit + 23 e2e，0 failed
- `cd frontend && npm run build` ✅（dist 产出，仅 antd chunk >500kB 警告，非缺陷）
- `cd src-tauri && npx @tauri-apps/cli@latest build` ✅ 产出 `RuT0DataKit.app` + `RuT0DataKit_0.1.0_aarch64.dmg`
- GUI smoke ✅ `open` 后 `pgrep -f RuT0DataKit` 返回 PID 存活，`kill` 退出干净
- grep 静态检查 ✅ RuleForm 0 / 原生 `<select>` 0 / emoji 0 / python/pyshark/scapy/pyo3 0（仅 docs/00 「不引入」说明命中）

Phase 8 Release QA 审计通过（`docs/qa/versions/0.1.0/QA-审计报告.md` §4.4 追加 UI 重构 + 规则引擎重构补丁段，结论维持 `qa_passed`，带条件：GUI 四 view 端到端交互待用户在自有环境核验）。Phase 9 版本同步完成。

**四功能重构补丁（T0-15 ~ T0-20）完成**：四功能导航 + 按列勾选脱敏 + 校验 GUI（独立 ValidateView）+ 导出列选/列序/CSV+XLSX + 规则详细说明/编辑/YAML 存取 + 跨视图 state 持久化。Phase 7 端到端验收通过（`cargo test --workspace` 152 passed = 134 unit + 18 e2e + `npm run build` + `tauri build` 产出 `RuT0DataKit.app` 4.8MB + `RuT0DataKit_0.1.0_aarch64.dmg` 4.8MB + GUI smoke PID 94524 存活；icon.png 16-bit RGBA 转 8-bit 修复 bundling 失败）。Phase 8 Release QA 审计通过（`docs/qa/versions/0.1.0/QA-审计报告.md` §4.4 追加四功能重构补丁段，结论维持 `qa_passed`，带条件：GUI 四 view 端到端交互待用户在自有环境核验）。Phase 9 版本同步完成。

> **历史状态轨迹（作废保留）**

`in_progress（UI 重构 + 规则引擎重构补丁）`（已被 Phase 9 同步为 release_complete，2026-07-18）— 见上文版本状态段。v0.1.0 此前已 `release_complete`（T0-1 ~ T0-14 + Phase C + T0-15 ~ T0-20 四功能重构补丁）。用户要求进一步重构：规则管理界面重做（单一列表 + Drawer + 动态试运行）+ 数据导出预览（单一 Table 集成 + 列勾选/调序）+ 数据脱敏/校验界面改四段垂直布局 + 规则引擎重构（抽象可扩展算子 MaskOp/ValidateOp + 预置库，基于正则替换这类通用算子便于扩展）。重开为 `in_progress`，派发 T0-21 ~ T0-26。原 T0-1 ~ T0-20 保留为底座，旧 `mask_pipeline` / `mask_pipeline_selected` / `mask_pipeline_columns` / `validate_pipeline` 与 13 个旧 src-tauri 命令保留向后兼容，18 个旧 e2e 不删。

---

### 历史状态轨迹（作废保留）

`in_progress（四功能重构补丁完成，待 Phase 8/9）`（已被 Phase 9 同步为 release_complete）— 见上文版本状态段。

---

### 历史状态轨迹（作废保留）

`release_complete`（T0-1 ~ T0-14 + Phase C，已被四功能重构补丁重开）— v0.1.0 重构（T0-10 ~ T0-14）完成：侧边栏布局 + 文件处理视图 + 4 种富规则 + 行选择 pipeline + src-tauri 命令扩展 + e2e 扩展 + 前端 React + Ant Design 重写 + 全部受影响文档同步。Phase 7 端到端验收通过（139 passed）。Phase 8 Release QA 审计通过。Phase 9 版本同步完成。Phase C 目录治理 + 文档-代码一致性审查补丁完成，版本状态维持 `release_complete`。

---

### 历史状态轨迹（作废保留）

`in_progress（重构）` — 原 v0.1.0 标 `release_complete` 后用户反馈 GUI 不符合预期：缺少侧边栏布局、文件预览、行勾选、富规则能力（regex_replace / regex_extract / delete / replace）。已回退状态为 `in_progress` 并启动 v0.1.0 重构（新任务 T0-10 ~ T0-13，见 TASK-BOARD）。原 T0-1 ~ T0-9 保留为底座。

T0-10 / T0-11 / T0-12 / T0-13 已 `verified_complete`：侧边栏布局 + 文件处理视图 + 4 种富规则 + 行选择 pipeline + src-tauri 命令扩展 + e2e 扩展 + 全部受影响文档同步完成；`cargo build --workspace` / `cargo test --workspace`（e2e 11 passed）/ `cargo build --manifest-path src-tauri/Cargo.toml` 全绿。最终 `release_complete` 由主会话 Phase 9 在 Release QA 审计通过后写入，本任务不自行写入。

`release_complete` — Phase 7 端到端验收通过（`cargo test --workspace` 117 passed）；Phase 8 Release QA 审计通过（`docs/qa/versions/0.1.0/QA-审计报告.md` 结论 `qa_passed`，带条件：GUI 运行时 smoke 待人工，见报告 §8）；Phase 9 版本同步完成。（初版，已作废）
