# v0.2.0 更新日志

> 进度表。与 `handoff/TASK-BOARD.md` 状态保持一致。

| 任务 | 状态 | 备注 |
|------|------|------|
| T2-1 Log reader + CLF 解析 + LogEntry + 双重 URL 解码 | verified_complete | `crates/core/src/log/mod.rs`：CLF 正则解析 + `LogReader::read` + `parse_query` 双重 URL 解码；`access.log` 全 1860 行 100% 解析；6 unit tests |
| T2-2 攻击特征签名引擎（SQLi 6 类） | verified_complete | `crates/core/src/logsign/`：`SignatureEngine` + `sqli_signatures.yaml` 内嵌 6 类规则（blind_binary / union / error_based / time_based / tautology / comment）+ `context_anchor` 锚点抑制误报；16 unit tests |
| T2-3 日志扫描 pipeline（scan_log + 弱口令 + 敏感扫描） | verified_complete | `crates/core/src/pipeline/log_scan.rs`：`scan_log` 组合签名 + 弱口令 grep + `DefaultSensitiveScan`，产出 `Report(kind=log_scan)`；summary 含 total_lines / sqli_hits / weak_password_hits / sensitive_hits / top_attack_ips；6 unit tests |
| T2-4 GUI log 入口启用 + 报告展示 | verified_complete | src-tauri `scan_log` 命令 + frontend LogView 激活（侧边栏「日志扫描」由 disabled 切 active）；pcap 仍置灰（v0.3.0） |
| T2-5 签名引擎装载/重载 | verified_complete | `logsign/loader.rs`：`load_builtin_signatures` + `load_signatures_from_str`；`SignatureEngine::from_rules` 支持运行时重载 + `enabled` 开关 |
| T2-6 收尾：fixture 追加 4 行 SQLi 变体 + e2e 扩展 + 文档/版本号同步到 v0.2.0 | verified_complete | `access.log` 末尾追加 4 行（union / extractvalue / sleep / tautology，总 1860 行）；e2e +3（`log_parse_full` / `log_scan_full` / `log_signatures_6_categories`）；log_test 断言 1856→1860；docs/00 §3/§4.1 + docs/01 + docs/03（任务 ID T1-N→T2-N，T2-N→T3-N）+ docs/04 + docs/versions/0.2.0/规划需求 + README/README_EN 同步；版本号 0.1.0→0.2.0（Cargo.toml×3 / tauri.conf.json / frontend/package.json）；QA 报告填充 qa_passed |

## 版本状态

`release_complete` — T2-1 ~ T2-6 全部 `verified_complete`，Phase 7 端到端集成验收通过（cargo test 234 passed / npm build / tauri 0.2.0 .app+.dmg / GUI smoke），Phase 8 Release QA 审计 `qa_passed`（见 `docs/qa/versions/0.2.0/QA-审计报告.md`）。Phase 9 git tag v0.2.0 + push GitHub。

**T2-6 收尾交付明细**：
1. `tests/fixtures/samples/log/access.log`：末尾追加 4 行 SQLi 变体（UNION `1+union+select+1,2,3--+-` / 报错 `1'+and+extractvalue(1,concat(0x7e,user()))--+-` / 时间盲注 `1'+and+sleep(5)--+-` / 恒真 `1%20or%201=1`，恒真用 `%20` 而非 `+` 以匹配 `parse_query` 不解 `+` 的解码口径），总行数 1856 → 1860。
2. `crates/core/tests/log_test.rs`：`access_log_fixture_all_lines` 断言 1856 → 1860（line_no 连续性断言保持）。
3. `crates/core/tests/e2e.rs`：新增 3 个 v0.2.0 e2e：
   - `log_parse_full`：LogReader 读 access.log，断言 1860 行全解析 + line_no 1..=1860 连续 + 每行 ip 非空。
   - `log_scan_full`：SignatureEngine + DefaultSensitiveScan + 默认 RuleSet 跑 `scan_log`，断言 `Report.kind=="log_scan"`、`total_lines==1860`、`sqli_hits > 1000`、`weak_password_hits >= 4`、4 类新签名（union / error_based / time_based / tautology）各至少 1 次命中、`top_attack_ips` 截断到 5。
   - `log_signatures_6_categories`：6 条 LogEntry 各触发 1 类签名，断言 `SignatureEngine` 各产 ≥1 hit；配 1 条全反例 + 2 条单类反例（`select` 关键字不触发 union、`or 1` 不触发 tautology）。
4. `cargo test --workspace` 实测：178 unit + 26 e2e + 8 logsign + 6 log + 16 scan = 234 passed，0 failed。
5. 文档同步：
   - `docs/00-需求文档.md`：§3 版本范围 v0.1.1→v0.2.0 / v0.1.2→v0.3.0；§4.1 新增 v0.2.0 验收标准段；§5/§7 内部版本引用更新。
   - `docs/03-开发任务清单.md`：任务 ID 规则 v0.1.1 用 T1-N→v0.2.0 用 T2-N、v0.1.2 用 T2-N→v0.3.0 用 T3-N；v0.1.1 段头→v0.2.0（含 T2-1~T2-6 状态）、v0.1.2→v0.3.0（T3-1~T3-5）。
   - `docs/04-版本标准.md`：里程碑索引 0.1.1→0.2.0（in_progress）/ 0.1.2→0.3.0（planned）；新增「v0.2.0 验收口径」段。
   - `docs/01-页面与交互说明.md`：侧边栏 / 版本覆盖说明 / 各段 v0.1.1→v0.2.0、v0.1.2→v0.3.0。
   - `docs/02-技术设计文档.md`：模块树 / 依赖说明 v0.1.1→v0.2.0、v0.1.2→v0.3.0。
   - `docs/versions/0.2.0/规划需求.md`：标题 + 范围 + 任务清单引用 + 状态更新。
   - `docs/versions/0.3.0/规划需求.md`：标题 + 任务清单引用（T2-1~T2-5→T3-1~T3-5）+ 依赖（v0.1.1→v0.2.0）。
   - `docs/qa/versions/0.2.0/QA-审计报告.md`：新建空骨架（§0 结论 + §1-9 标题占位），由主会话 Phase 8 填充。
   - `README.md` / `README_EN.md`：功能列表补日志扫描段 + 版本表 v0.1.1→v0.2.0 / v0.1.2→v0.3.0 + 状态行 v0.2.0。
6. 版本号 bump 0.1.0 → 0.2.0：
   - `Cargo.toml`（workspace）
   - `crates/core/Cargo.toml`
   - `src-tauri/Cargo.toml`
   - `src-tauri/tauri.conf.json`
   - `frontend/package.json`
7. 物理目录重命名：
   - `docs/versions/0.1.1/` → `docs/versions/0.2.0/`
   - `docs/versions/0.1.2/` → `docs/versions/0.3.0/`
   - 清理空目录 `docs/qa/versions/0.1.1/` / `docs/qa/versions/0.1.2/`（原为占位空目录），新建 `docs/qa/versions/0.2.0/`。
