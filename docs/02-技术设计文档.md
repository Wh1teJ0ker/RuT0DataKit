# 技术设计文档

## 1. 总体架构

```
┌──────────────────────────────────────────────────────────┐
│  Tauri GUI (src-tauri + 前端)                              │
│  - 文件选择 / 类型识别 / 规则加载 / 结果展示 / 报告导出        │
│  - v0.1.0 仅 csv/xlsx 脱敏入口；v0.2.0 增 log；v0.3.0 增 pcap│
└────────────────────────┬─────────────────────────────────┘
                         │ Tauri command (serde JSON)
┌────────────────────────▼─────────────────────────────────┐
│  core crate  (ruT0-data-kit-core)                         │
│  ├─ pipeline   分发入口：detect_type → run / scan_log    │
│  ├─ rules      YAML 规则加载 + 校验器注册表 + 脱敏器注册表   │
│  ├─ readers    csv / xlsx（v0.1.0）→ log(v0.2.0) → pcap(v0.3.0)│
│  │             → sql/json + pcap/log 适配器(v0.4.0)         │
│  │             read_records(path) → Records 统一入口         │
│  ├─ validators 内置：idcard/phone/bankcard/email/mac/ip/  │
│  │               username/name/address/birth/password     │
│  ├─ maskers    内置：idcard/phone/bankcard/email/name/    │
│  │               customer_id/custom                       │
│  ├─ scan       SensitiveScan 共享 trait（v0.1.0 占位，v0.2.0+ 使用）│
│  ├─ pcap       v0.3.0：调 tshark CLI → 解析 JSON → 字段提取/│
│  │             URL 解码 / base64 重组 / 敏感扫描            │
│  ├─ log        v0.2.0：CLF 解析 → 攻击特征识别 / 敏感扫描   │
│  ├─ logsign    v0.2.0：SQLi 攻击签名引擎（6 类签名）        │
│  └─ report     统一 JSON 报告 schema + csv 导出            │
└──────────────────────────────────────────────────────────┘
```

## 2. 关键模块职责

### 2.1 rules

- `RuleSet { validators: Vec<FieldRule>, maskers: Vec<MaskRule> }`，从 YAML 反序列化。
- `Validator` trait：`fn validate(&self, value: &str) -> ValidationResult`。
- `Masker` trait：`fn mask(&self, value: &str) -> String`。
- 内置校验器注册表（`ValidatorRegistry`）：name → 工厂函数。
- 内置脱敏器注册表（`MaskerRegistry`）：同上。
- 自定义规则：v0.2.x 仅支持引用内置 validator/masker；YAML 中可声明 `regex:` 做正则兜底校验。
- 可扩展算子模型（T0-21 增）：`rules::operator` 模块定义两个通用算子枚举，作为内置 masker/validator 的统一收敛点，对外暴露稳定的可扩展 API：
  - `MaskOp`（4 种）：`Template` / `SplitTemplate` / `RegexReplace` / `ConstReplace`。配套 `MatchMode { All, First }`、`MaskOp::from_rule(name, params)` 构造器、`apply_mask_op(op, value)` 应用函数。11 个旧 masker 名（含 6 预设 `idcard_mask` / `phone_mask` / `bankcard_mask` / `email_mask` / `name_mask` / `customer_id_mask` + 富规则 `custom` / `regex_replace` / `regex_extract` / `delete` / `replace`）均收敛到这 4 种算子。
  - `ValidateOp`（3 种）：`Regex` / `Algorithm` / `RegexWithGuard`。配套 `AlgoKind` / `GuardKind`、`ValidateOp::from_rule(rule)` 构造器、`apply_validate_op(op, value)` 应用函数。8 个旧 validator 名收敛到这 3 种算子（`Algorithm` / `RegexWithGuard` 委托给既有 `IdCardValidator` / `BankCardValidator` / `PhoneValidator` / `MacValidator` 结构体，复用既有算法实现）。
  - `rules::presets` 模块提供 `lookup_mask_preset(name)` / `lookup_validate_preset(name)` 预设查表，以及 `list_mask_op_types()` / `list_validate_op_types()` 列举所有可识别名（含预设别名，保证 YAML 向后兼容）。
  - T5-1 增：`rules::presets` 提供 `list_tagged_presets(tag: &str) -> Vec<PresetEntry>`，按标签（`mask` / `validate` / `sensitive` / `search` / `sql_parse`）返回预置模板元信息。`PresetEntry { name, label, tags, params_template: Option<serde_yml::Mapping> }` **不构造 `MaskOp` / `ValidateOp` 实例**，避免与 `operator.rs` 耦合，仅供 GUI（T5-4）预填参数 / 生成规则草稿。
  - `build_masker` / `build_validator` 签名与行为不变，内部改走 `Op::from_rule → apply_*_op` 路径；旧 `Masker` / `Validator` trait、`MaskRule` / `FieldRule` / `RuleSet` 结构、`mask_pipeline*` / `validate_pipeline` 全部保持原样。

### 2.2 readers

```rust
pub trait SourceReader {
    fn detect(path: &Path) -> bool;        // 后缀 / magic
    fn read(&self, path: &Path) -> Result<Records>;
}
```

`Records` = 行流（每行 `Vec<(String, String)>` 字段名→值），csv/xlsx 都映射到它。v0.2.0/v0.3.0 的 log/pcap 不强制走该 trait（它们没有"字段名→值"结构），但复用其读取抽象思路，各自定义独立 output。

**v0.4.0 预处理归一化（T5-2）**：新增 `SqlReader` / `JsonReader`，以及把 pcap / log 模块原 output 包成 `Records` 的适配器 `PcapRecordsReader` / `LogRecordsReader`，不动 pcap / log 模块内部实现。统一入口 `read_records(path: &Path) -> Result<Records, CoreError>` 按 `detect_type` 后缀 dispatch 到对应 reader；`SourceType` 扩 `Sql` / `Json` 变体；Tauri 新增 `preprocess_file(path: String) -> Result<Value, String>` 命令，把任意支持格式统一读成 `{ headers, rows, source_type, row_count }` JSON，供 PreviewView / mask / validate / export 复用。

- `SqlReader`：`.sql` 按 `;` 拆语句（处理单/双引号字符串内分号、跳过 `--` 行注释），每条非空语句一行；headers = `["sql_text", "statement_type"]`，`statement_type` 按首关键字小写判定（select/insert/update/delete/create/alter/drop/other）。块注释、SQL `''` 转义留 v0.4.1+。
- `JsonReader`：JSON 数组对象 → headers=对象 keys 并集（按首次出现序）+ rows，缺失 cell 补 `""`；JSON 单对象 → 1 行；JSON Lines（每行一对象）→ 同数组；纯标量数组 / 字符串 → 单列 `value`。嵌套对象 / 数组最小实现 `to_string()` 成 JSON 串放入单 cell，留 v0.4.1+。
- `PcapRecordsReader`：调 `pcap::PcapReader::read` 拿 `Vec<HttpRequest>` → 8 列 Records（frame_no/src_ip/dst_ip/method/host/uri/body/user_agent），`body=None` → `""`。tshark 缺失时透传 `CoreError::DependencyMissing("tshark")`，不 panic。
- `LogRecordsReader`：调 `log::LogReader::read` 拿 `Vec<LogEntry>` → 12 列 Records（line_no/ip/timestamp/method/path/query/status/size/user_agent/decoded_path/decoded_query/decoded_ua，不含 raw），`query/size/decoded_query` 为 `None` 时 cell 为 `""`。

### 2.3 maskers（内置，v0.1.0 必交付）

| name | 规则 |
|------|------|
| idcard_mask | 前6+后4，中间8位 `*` |
| phone_mask | 前3+后4，中间4位 `*` |
| bankcard_mask | 前6+后4，中间 `*` |
| email_mask | 本地部分首末字符保留，中间 `*`；域名完整 |
| name_mask | 2字→`X*`；≥3字→`X*…*Y` |
| customer_id_mask | 首字符保留，其余 `*` |
| custom | YAML 提供 `keep_prefix / keep_suffix / mask_char / mask_min_len` 通用参数 |
| regex_replace | 富规则：`pattern` 正则 + `replacement`（支持 `$1`/`$2` 捕获组）；非法/缺失 pattern 时原值返回 |
| regex_extract | 富规则：`pattern` 正则提取首个匹配子串；无匹配返回原值 |
| delete | 富规则：对任意输入返回空串 `""`，无参数 |
| replace | 富规则：对任意输入返回 `with` 字符串；缺失 `with` 时退化为空串 |

> 富规则（`regex_replace` / `regex_extract` / `delete` / `replace`）由 v0.1.0 重构（T0-10）落地，经 `build_masker` 统一 dispatch，可与 7 个内置 masker 混挂在同一 `RuleSet.maskers` 中。

**抽象算子模型（T0-21 增）**：

- `MaskOp` 枚举 4 variant：`Template(TemplateOp)` / `SplitTemplate(SplitTemplateOp)` / `RegexReplace(RegexReplaceOp)` / `ConstReplace(ConstReplaceOp)`；配套 `MatchMode { All, First }`。
- 公共 API：`apply_mask_op(op: &MaskOp, value: &str) -> String`；`MaskOp::from_rule(name, params) -> Option<MaskOp>` 构造器；`MaskOp` impl `Masker` trait，可直接作为 `Box<dyn Masker>` 注入 pipeline。
- 预置库（`rules::presets`）：`lookup_mask_preset(name) -> Option<MaskOp>` + `list_mask_op_types() -> Vec<(op_name, label)>`（10 项 = 4 通用算子 + 6 预置别名 `idcard_mask` / `phone_mask` / `bankcard_mask` / `customer_id_mask` / `name_mask` / `email_mask`）。
- `build_masker` 内部走 `MaskOp::from_rule` 路径，签名与行为不变；旧 `MaskRule` / `RuleSet` / `mask_pipeline*` 全部保留向后兼容。
- 新增脱敏类型 = 加一个 `MaskOp` variant + 在 `rules::presets` 加一行映射，`build_masker` 自动 dispatch，无需改 pipeline 或前端调用约定。

**按列勾选脱敏 pipeline（T0-15 增）**：

```rust
pub fn mask_pipeline_columns(
    records: &Records,
    rules: &RuleSet,
    selected_columns: &HashSet<String>,
) -> Result<MaskResult, CoreError>;
```

只对同时满足「在 `selected_columns` 中」且「在 `rules.maskers[].field` 中」的字段应用 masker；未勾选的规则列原样；`selected_columns` 中未声明规则的列原样；未知名 masker 静默跳过。返回与 `mask_pipeline` 同形 `MaskResult`，`summary.by_field` 只统计实际被 masker 调用的字段。

向后兼容：旧 `mask_pipeline` / `mask_pipeline_selected`（行选择）保留不动，三者共用 `build_masker` dispatch 与 `MaskResult` 产出结构。

### 2.4 validators（内置，v0.1.0 留接口+注册表，具体实现可按需提前到 v0.1.0 或后置）

| name | 规则 |
|------|------|
| idcard | 18位，地址码+出生日期码(YYYYMMDD 有效)+顺序码+校验码(weights `[7,9,10,5,8,4,2,1,6,3,7,9,10,5,8,4,2]`, mod 11 查表 `10X98765432`) |
| phone | 11位数字；号段集合可配置（内置 CTF 虚假号段集 + 真实三大运营商号段集，由规则 YAML 选择 `prefix_set: ctf\|real`） |
| bankcard | 13..19 位数字 + Luhn 校验 |
| email | 标准邮箱正则 |
| mac | 通用 `([0-9A-Fa-f]{2}[:-]){5}[0-9A-Fa-f]{2}`；可加 `prefix` 约束 |
| ip | IPv4，禁前导零，0..255 |
| username | `^[A-Za-z0-9]+$` |
| name | 全中文 `^[\u4e00-\u9fa5]+$` |
| birth | 8位 YYYYMMDD |
| address | 全中文 + 号(1-1500) + 室(101-999) |
| password | 长度≥8 + 大写/小写/数字/符号各一 |

> 说明：v0.1.0 的脱敏 pipeline 不强依赖 validators，但 SensitiveScan 与 v0.2.0 日志扫描会用到。v0.1.0 内至少落地 idcard/phone/bankcard/email/mac/username/name 这几个被脱敏/扫描共用的校验器，其余（birth/address/password/ip）可在 v0.2.0 随日志模块一起补齐。具体由任务 T3-v0.1.0 圈定。

**数据校验 pipeline（T0-15 增，v0.1.0 起 GUI 交付）**：

```rust
pub struct ValidateResult {
    pub headers: Vec<String>,
    pub rows: Vec<Vec<String>>,
    pub valid_matrix: Vec<Vec<bool>>,
    pub summary: ValidateSummary,
}
pub struct ValidateSummary {
    pub total_rows: usize,
    pub invalid_rows: usize,
    pub by_field: HashMap<String, usize>,
}
pub fn validate_pipeline(records: &Records, rules: &RuleSet) -> Result<ValidateResult, CoreError>;
```

逐 cell 填合法矩阵：对 `rules.validators` 中每个 `FieldRule`，`build_validator` 构造校验器并对该列所有 cell 跑 `validate`，非法 cell 置 `false`。未声明 validator 的列 `valid_matrix[*][col]` 全 true；字段不在 `headers` 中或 validator 未知名且无 regex 时该规则静默跳过（不报错）。`summary.invalid_rows` = 至少有一个非法 cell 的行数；`summary.by_field` = 每个声明了 validator 的字段的非法 cell 数。

**抽象算子模型（T0-21 增）**：

- `ValidateOp` 枚举 3 variant：`Regex(RegexOp)` / `Algorithm(AlgorithmOp)` / `RegexWithGuard(RegexWithGuardOp)`；配套 `AlgoKind { IdCard, BankCard }` + `GuardKind { PhonePrefix { prefix_set }, MacPrefix { prefix } }`。
- 公共 API：`apply_validate_op(op: &ValidateOp, value: &str) -> ValidationResult`；`ValidateOp::from_rule(rule) -> Option<ValidateOp>` 构造器；`ValidateOp` impl `Validator` trait，可直接作为 `Box<dyn Validator>` 注入 pipeline。
- 预置库（`rules::presets`）：`lookup_validate_preset(name, params, message) -> Option<ValidateOp>` + `list_validate_op_types() -> Vec<(op_name, label)>`（10 项 = 3 通用算子 + 7 预置别名 `email` / `username` / `name` / `idcard` / `bankcard` / `phone` / `mac`）。`Algorithm` / `RegexWithGuard` 委托既有 `IdCardValidator` / `BankCardValidator` / `PhoneValidator` / `MacValidator` 复用算法实现。
- `build_validator` 内部优先走 `ValidateOp::from_rule`（覆盖 regex 兜底 + 7 个预置别名），未命中时回退 `ValidatorRegistry`；签名与行为不变。

### 2.5 scan（共享，v0.1.0 占位）

```rust
pub trait SensitiveScan {
    fn scan(&self, text: &str, rules: &RuleSet) -> Vec<Finding>;
}
```

v0.1.0 提供一个基于内置 validators 的 `DefaultSensitiveScan`（可空实现或最小实现），保证 trait 与类型落盘；v0.2.0 在 log 模块真正使用时补齐实现与签名。v0.1.0 不要求其被 GUI 调用。

### 2.6 report（统一 schema，v0.1.0 落地）

```rust
pub struct Report {
    pub source: String,
    pub kind: String,             // "csv_mask" | "csv_validate" | "log" | "pcap"
    pub summary: serde_json::Value,
    pub findings: Vec<Finding>,
    pub extra: serde_json::Map<String, serde_json::Value>,
}
pub struct Finding {
    pub r#type: String,           // idcard/phone/...
    pub value: String,
    pub location: Option<String>,
    pub valid: Option<bool>,
    pub context: Option<String>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub extra: Option<serde_yml::Value>,
}
```

v0.1.0 脱敏报告 `kind="csv_mask"`，summary 含 `{ total_rows, masked_rows, by_field: {field: count} }`，findings 可为空（脱敏不产出 finding，但结构统一）。

T2-9（v0.2.0）为 `Finding` 增 `extra: Option<serde_yml::Value>`（`skip_serializing_if = "Option::is_none"`，
向后兼容：无 extra 时序列化形态不变）。log_scan pipeline 的 sqli finding 把 `SignatureHit.parsed_payload`
序列化为 `serde_yml::Value` 透传进 `extra`，供前端展示「解析结果」「读取目标」等列；weak_password /
sensitive / csv_mask 场景 extra 为 `None`（序列化时不出现 `extra` key）。

### 2.7 pcap（v0.3.0）

- `PcapScanner::scan(path, rules) -> Report`
- 内部：tshark 子进程 → TSV → HttpRequest → 双重 URL 解码 → base64 重组 → 敏感扫描。
- tshark 不存在时返回 `CoreError::DependencyMissing("tshark")`，不 panic。

### 2.8 log（v0.2.0）

- 入口：自由函数 `scan_log(entries, sig_engine, scan, rules) -> Report`（`crates/core/src/pipeline/log_scan.rs`），`src-tauri/src/commands.rs::scan_log_file(path) -> {entries, report}` 为 IPC 包装。
- CLF 解析 → `LogEntry { ip, ts, method, path, query, status, ua }`。
- 攻击特征（内置签名，v0.2.0 范围）：SQLi 6 类（盲注二分 / UNION / 报错 / 时间盲注 / 恒真 / 注释符）+ 弱口令关键字 grep（16 项等值匹配）。**路径穿越 / 命令注入 / 越权**未实现，留待 v0.3.0+ 评估。
- 对 query / ua 跑敏感扫描（复用 v0.1.0 `DefaultSensitiveScan`；默认 RuleSet validators 为空时 sensitive_hits=0，字段存在兜底为 0）。

### 2.8.1 logsign（v0.2.0，SQLi 签名引擎）

- 独立模块 `crates/core/src/logsign/`，与 `rules` 解耦，不引用 `MaskOp` / `ValidateOp` / pipeline。
- `SignatureRule`（YAML 反序列化形态）：`rule_id / category / pattern / enabled / context_anchor: Vec<String> / description`。
- `SignatureHit { rule_id, category, line_no, matched_value, context, parsed_payload }`。
  `parsed_payload: Option<ParsedPayload>` 在 `scan_log_entry` 产 hit 时对命中文本段
  调 `payload_parser::parse_payload(text)` 填充，未解析到为 `None`。
- `SignatureEngine::new()` 用 `include_str!` 编译期嵌入的内置 YAML 加载 6 条 SQLi 签名：
  `sqli_blind_binary / sqli_union / sqli_error_based / sqli_time_based / sqli_tautology / sqli_comment`。
- `scan_log_entry(&LogEntry) -> Vec<SignatureHit>`：
  - 跳过 `method.is_empty() || path.is_empty()` 的行（408 超时行），不产 finding；
  - 匹配目标 = `path` + `parse_query(query)` 的所有 value（已双重 URL 解码），故 pattern 一律按 decoded 形态编写；
  - `context_anchor` 非空时要求所有锚点也在该段文本中命中才算命中（抑制 `or` / `--` 弱信号误报）；
  - 同一规则在同一文本段命中只产 1 个 `SignatureHit`。
- `loader::load_signatures_from_str(&str)` 提供运行时反序列化入口，供 T2-5 GUI 在线编辑后重新装载。
- 零新依赖：复用 core 已依赖的 `serde_yml`。

### 2.8.2 payload_parser（v0.2.0，SQLi payload 语义解析）

- 独立模块 `crates/core/src/logsign/payload_parser.rs`，对命中后的 decoded 文本做
  结构化语义解析，与签名匹配（判类别）互补。
- `ParsedPayload { attack_type, technique, read_target, char_position, compared_ascii,
  comparator, union_columns, sleep_seconds, summary }`，derive `Debug/Clone/PartialEq/Eq/Serialize`。
- `parse_payload(decoded_text: &str) -> Option<ParsedPayload>`：
  - 输入应为已解码文本（`+` 已空格、`%XX` 已解）；内部先 `to_lowercase()` 再匹配，兼容大写 SQL。
  - 6 类正则（regex crate 无 look-around，吃前缀 + 捕获组）按 blind → union → error →
    time → tautology → comment 顺序首命中返回，一条 payload 只归一类。
  - blind_boolean：`ascii(substr((<func>),N,1)) <cmp> <ascii>` → read_target/char_position/
    comparator/compared_ascii；summary「盲注二分：…」。
  - union：`union [all ]select N,N,…` → union_columns（数逗号+1）；summary「UNION 注入：取 N 列」。
  - error：`(extractvalue|updatexml)(N,concat(…, <func>))` → read_target；summary「报错注入：…」。
  - time：`(sleep|benchmark|pg_sleep)(N)` → sleep_seconds；summary「时间盲注：延时 N 秒」。
  - tautology：`<or|and> N=M` → summary「恒真绕过：N=M」。
  - comment：`(--|#|/\*)` → summary「注释符截断查询：…」。
- 与 `SignatureHit` 集成方式：`scan_log_entry` 产 hit 处直接调 `parse_payload(text)`，
  结果写入 `parsed_payload` 字段，供 T2-9 pipeline 转入 `Finding` 时复用。

### 2.8.3 v0.2.1 语义增强（T2-7 ~ T2-10）

- **`parse_query` `+` 解码语义（T2-7）**：`crates/core/src/log/mod.rs::parse_query` 按
  form-urlencoded 标准 `+`→space 再双重 `%XX` 解码（`url_decode_twice`）。
  - 历史口径（v0.2.0）：`parse_query` 不解 `+`，pattern 一律按含 `+` 形态编写；
    v0.2.1 起改为标准 form-urlencoded 解码，pattern 改按 decoded 形态编写。
  - `LogEntry` 新增 `decoded_path` / `decoded_query` / `decoded_ua` 字段：
    - `decoded_path` / `decoded_ua`：`url_decode_twice` 双重 `%XX` 解码（path / UA 段无 `+`=空格语义，不解 `+`）。
    - `decoded_query`：`join_decoded_query(parse_query(query))` 把 `(key, value)` 列表拼回
      `k=v&k2=v2` 形态，供签名引擎与 GUI 直接消费已 `+`→space + 双重 `%XX` 解码文本。
  - `parse_line` 一次性填充上述三字段，`SignatureEngine` 匹配目标改吃 `decoded_query` /
    `decoded_path`，不再每条 hit 重复解码。

- **6 类签名 pattern 扩变体（T2-9）**：`sqli_signatures.yaml` 扩展：
  - `sqli_union` 兼容 `union all select 1,2,3`（`union\s+(?:all\s+)?select`）。
  - `sqli_error_based` 兼容 `exp(~(select * from dual))` / `floor(rand(0)*2)` 变体
    （MySQL 报错注入经典 payload）。
  - 原 `extractvalue` / `updatexml` 变体保留，回归不破。

- **`Finding.extra` 透传 `ParsedPayload`（T2-9）**：
  - `crates/core/src/report.rs::Finding` 新增 `extra: Option<serde_yml::Value>`
    （`#[serde(skip_serializing_if = "Option::is_none")]`，向后兼容：无 extra 时序列化形态不变）。
  - `crates/core/src/pipeline/log_scan.rs` 把 sqli hit 的 `parsed_payload` 序列化为
    `serde_yml::Value` 透传进 `Finding.extra`，供前端展示「解析结果」「读取目标」列。
  - weak_password / sensitive / csv_mask 场景 `extra=None`（序列化时不出现 `extra` key）。

- **GUI LogView decoded 列 + 解析结果/读取目标列（T2-10）**：
  - `frontend/src/components/LogView.jsx` 原始日志表新增 `decoded_path` / `decoded_query` /
    `decoded_ua` 列。
  - findings 表新增「解析结果」（`ParsedPayload.summary`）+「读取目标」（`read_target`）列，
    读 `Finding.extra` 序列化字段渲染；weak_password / sensitive finding 该两列为空。

### 2.8.4 blind_aggregator（v0.2.2，盲注二分序列聚合还原）

- 独立模块 `crates/core/src/logsign/blind_aggregator.rs`，与 `payload_parser` 互补：
  payload_parser 只解析单条 payload 的语义类别（盲注/UNION/报错…），本模块在签名
  命中之后对一批同源（同 `read_target` + 同 `source_ip`）的二分探针按 `char_position`
  聚类，还原出被盲注读取的完整字符串（flag）。
- 输入：`BlindAggregator::collect_from_entries(&[LogEntry])` 对每条 entry 的
  `decoded_query`（None 则 `decoded_path`）整串跑自带正则，抽出
  `ascii(substr((<read_target>),<pos>,1))<cmp><thr>` 形态的探针；`body_size` 取
  `LogEntry.size`（`Option<u64>`，`None` 跳过该 entry）；`source_ip` = `LogEntry.ip`。
- 结构（derive `Serialize`，承载 `Report.extra.blind_aggregation`）：
  - `BlindProbe { read_target, char_position, threshold, body_size, source_ip, line_no }`。
  - `PositionDetail { position, decoded_char, ascii_val, true_size, probe_count, status }`
    （status ∈ `resolved` / `unresolved_all_true` / `beyond_end` / `insufficient_probes`）。
  - `AggregatedResult { read_target, decoded_string, resolved_chars, unresolved_chars,
    beyond_end_positions, probe_count, source_ips, position_details }`。
- bytes 自动聚类算法（fixture-specific 假设）：
  - 对每个 `(read_target, source_ip)` 分组：`group_true_size = min(所有探针 body_size)`
    （条件成立响应 body 更小，实测 fixture line 19-65：thr=79/103/109/111 → body=862 true，
    thr=112/115 → body=875 false）。
  - 按 `char_position` 子分组；全同 body 且 == group_true_size → `unresolved_all_true`；
    全同 body 且 != group_true_size → `beyond_end`；混合则 true 探针 = `body == true_size`
    （ascii > thr），false 探针 = `body != true_size`（ascii <= thr），
    `ascii_val = min(false_thresholds)`，自洽性校验 `max(true_thresholds) + 1 == ascii_val`，
    不符标 `insufficient_probes`。
  - 按 position 升序拼接：resolved 追加字符；unresolved/insufficient 追加 `'?'`；
    **首个 `beyond_end` 即停止拼接**（字符串末尾）。
  - 该方向是 fixture-specific 假设；若未来接入「条件成立 → body 更大」的靶机，
    需把取法从 `min` 改为 `max`（见模块注释）。
- 自带嵌套正则：regex crate 无 look-around，`read_target` 内层嵌套括号用
  `(?:[^()]|\([^()]*\))*` 吃掉单层 `(...)`（如 `database()` / `group_concat(table_name)`）；
  多层嵌套不支持，是已知简化。
- 与 `Report.extra` 集成（T2-13）：`scan_log` 末尾把 `Vec<AggregatedResult>` 序列化进
  `Report.extra` 的 `Value::Mapping({blind_aggregation: [...]})`；无盲注探针时 extra 仍为
  含空数组的 Mapping（前端统一按 sequence 读）。向后兼容：v0.2.1 `Finding.extra` 为
  `Value::Null` / `Option<serde_yml::Value>` 既有行为不破，weak_password / sensitive
  finding `extra=None`（skip_serializing_if 不出现 `extra` key）。
- GUI 集成（T2-14）：`frontend/src/components/LogView.jsx` 新增「盲注聚合结果」段 ③.5，
  读 `Report.extra.blind_aggregation` 渲染 inner Card + Text copyable 还原串 + 探针数 +
  来源 IP + 位置明细 Tooltip + 空态 Empty。

### 2.8.5 blind_aggregator 三轴优化（v0.2.3，T3-1 ~ T3-3）

- **算法优化（T3-1）**：
  - `true_size` 判定由 v0.2.2 的 `min(body_size)`（fixture-specific 假设，反向场景误判）改为
    `mode_per_position_true_size`：按 `char_position` 分组 → 每个「混合位置」取真假两簇 `body_size`
    中较小者（fixture：true 簇 body 更小）/ 较大者（反向场景：true 簇 body 更大）作为该位置 true_size
    → 跨位置众数（并列取较小者）→ 无混合位置时退化为 `min(body_size)`。修复 v0.2.2 R1 第 4 read_target
    （false 频次 737 > true 669）全局频次法误判为 875 → 全 `?` 的回归。
  - 自带正则从单层嵌套扩展到两层：`((?:[^()]|\((?:[^()]|\([^()]*\))*\))*)` 吃掉
    `where table_schema=database()` 这类两层 `(...)`；三层+不支持（regex crate 无 look-around，已知简化）。
  - `AggregatedResult` 新增 `separator_char: Option<char>`（`#[serde(skip_serializing_if)]`）：
    从 `read_target` 首个 `0x([0-9a-fA-F]{2})` 字面量解码（`0x7e`→`~`），`0xff+` 返回 `None`。
- **盲注类型扩展（T3-2）**：
  - `BlindProbe` 新增 `probe_kind: ProbeKind` 枚举（`AsciiBinary` / `Equality` / `Length`）+
    `equality_char: Option<char>`。
  - 新增 2 条正则：
    - equality：`substr\s*\(\s*\(\s*((?:[^()]|\((?:[^()]|\([^()]*\))*\))*)\s*\)\s*,\s*(\d+)\s*,\s*\d+\s*\)\s*=\s*(?:'([^'])'|char\s*\(\s*(\d+)\s*\))`
      捕获 read_target / position / 字符字面量或 char() 码点，直接解出单字符。
    - length：`length\s*\(\s*\(\s*((?:[^()]|\((?:[^()]|\([^()]*\))*\))*)\s*\)\s*\)\s*(>=?|<=?|=)\s*(\d+)`
      捕获 read_target / 比较符 / 阈值，取最小 `=` 或最大 `<` 阈值作为字符串长度。
  - 聚合分组键由 `(read_target, source_ip)` 升级为 `(read_target, source_ip, ProbeKind)` 三元组，
    避免同一 read_target 的 ascii_binary 与 length 探针串扰。
  - `AggregatedResult` 新增 `kind: Option<String>`（序列化为 `ascii_binary` / `equality` / `length`，
    `#[serde(skip_serializing_if)]`）。
  - `PositionDetail.status` 新增 `equality_resolved` / `length_resolved` 两个分支。
- **GUI 显示优化（T3-3）**：`LogView.jsx` 段 ③.5 inner Card：
  - extra = 探针类型 Tag（`kind` 着色：ascii_binary=red / equality=orange / length=blue；`r.kind` 缺失时
    前端按 `position_details[0].status` 兜底推断）+ 「已解 N/M」Tag。
  - 还原结果：`separator_char` 存在 → `Text strong` + volcano Tag 包裹分隔符高亮分段 + 独立「复制」按钮
    （`navigator.clipboard.writeText`）；否则 `Text strong copyable` 原样。
  - 位置明细由 v0.2.2 的 Tooltip + JSON.stringify 升级为 antd `Collapse`（ghost / size=small / 默认折叠）
    + 内嵌 `Table`（列 位置 / 字符 / ASCII / 探针数 / 状态 Tag 6 色着色）。
- 向后兼容：新字段 `kind` / `separator_char` 均 `#[serde(skip_serializing_if = "Option::is_none")]`，
  旧消费者读不到这两字段不影响；v0.2.2 既有 `blind_aggregation` 数组契约不破。

### 2.8.6 blind_aggregator 数据库格式还原（v0.2.4，T4-1 ~ T4-3）

- **新增 3 结构（T4-1）**：
  - `ReconstructedDatabase { schema: Option<String>, tables: Vec<ReconstructedTable>, unmatched_results: Vec<AggregatedResult> }`（`serde::Serialize`，`schema` 用 `skip_serializing_if = "Option::is_none"`）。
  - `ReconstructedTable { name: String, columns: Vec<String>, rows: Vec<ReconstructedRow>, row_data_columns: Vec<String>, column_separator: Option<char>, source_probe_count: u32 }`（`column_separator` skip_serializing_if）。
  - `ReconstructedRow { cells: Vec<Option<String>> }`（与 `columns` 等长，未 fetch 列 `None`）。
- **`reconstruct_database` 算法（T4-1）**：新增 `BlindAggregator::reconstruct_database(&[AggregatedResult]) -> ReconstructedDatabase` 关联函数，三步：
  - **Step1 分类**：对每个 `AggregatedResult` 按 `read_target` 模式分 4 类（regex crate 无 look-around，用 contains + 简单正则）：
    - `schema`：`read_target.trim() == "database()"` → `schema = Some(decoded_string)`。
    - `table_list`：`read_target.contains("group_concat(table_name)") && read_target.contains("information_schema.tables")` → 表名清单，`decoded_string` 按 `,` split。
    - `column_list`：`read_target.contains("group_concat(column_name)") && read_target.contains("information_schema.columns")` → 列名清单，`decoded_string` 按 `,` split；同时正则 `table_name\s*=\s*'([^']+)'` 抽出归属表名（无谓词按表名顺序索引对齐）。
    - `row_data`：`read_target.contains("group_concat(") && !上述两类` → 行数据；用正则 `from\s+(\w+)` 抽表名、`group_concat\s*\(([^)]*)\)` 抽列清单字符串（按 `,` split → tokens，过滤 `0xNN` hex token，剩余为 `row_data_columns`；首个 `0xNN` token 复用 `parse_separator_char` 解码为 `column_separator`）。
    - 未匹配 → 推入 `unmatched_results`。
  - **Step2 拼装**：schema 直填；对 `table_list` 解码出的每个表名 `T`：按 `table_name='T'` 谓词匹配 `column_list`（无谓词按索引对齐）→ `columns` 全量列（无列清单 → `columns = row_data_columns` 兜底）；按 `from T` 匹配 `row_data` → 解析 rows。row 解析：`decoded_string` 按 `,` split → 行字符串数组（group_concat 默认行分隔符）→ 每行按 `column_separator` split → 值数组 → 映射到 `row_data_columns → value` → 投影到 `columns` 全量列，未 fetch 列 `None`。
  - **Step3 unmatched**：未匹配的 `AggregatedResult` 原样塞 `unmatched_results`，前端兜底渲染。
  - 分类正则均 `OnceLock<Regex>` 缓存；辅助函数 `parse_table_name_predicate` / `parse_from_table` / `parse_row_data_columns` / `build_table_columns_and_rows`。8 条单测覆盖全还原 / unmatched / 缺 column_list 兜底 / 空 results / 无分隔符单列 / 3 正则单元测试。
- **pipeline 透传（T4-2）**：`scan_log` 末尾在 `BlindAggregator::collect_from_entries` + `aggregate` 之后调 `BlindAggregator::reconstruct_database(&blind_results)`，结果塞 `Report.extra.reconstructed_database` 键（与 `blind_aggregation` 并列）；`serde_yml::to_value(&reconstructed).unwrap_or(Value::Null)` 透传到前端 JSON。向后兼容 v0.2.3：`blind_aggregation` 数组契约不破，新增 `reconstructed_database` 键。e2e `log_scan_full` 新增五段断言（schema=="person" / 1 表 person_data / 7 列 / ≥2 行 / row[0].id=Some("1") / row[0].username=contains zhangsan/lisi / row_data_columns 含 id/username/idcard / column_separator=="~"）。
- **GUI 显示（T4-3）**：`LogView.jsx` 段 ③.5 之前新增段 ③.5a「还原数据库视图」Card：
  - 顶部 `Descriptions`：数据库名（`schema` `<Text code>`）+ 表数 + unmatched 数。
  - 每张 `ReconstructedTable` 一个 inner Card：title=`<Text code strong>{table.name}</Text>` + 列/行数 Tag；body=antd `Table`（`buildTableColumns` 把 `table.columns` 映射成 antd columns，title=列名，dataIndex=列名，未 fetch 单元格 `render: v => v ?? <Text type="secondary">-</Text>` 显示 `-`；`buildTableDataSource` 把 `table.rows` 映射成 dataSource，每行 object 键=列名，未 fetch 列 undefined）；extra=`column_separator` volcano Tag + `row_data_columns` Tooltip「已 fetch N/M 列」+ 探针 Tag。
  - unmatched_results 非空：`Divider` + 简版列表（read_target + decoded_string copyable）。
  - 空态：`reconstructedDb == null || tables.length === 0` → `<Empty description="无法还原为数据库结构（缺少 database()/table_name/column_name/row data 四类探针）" />`。
  - 段 ③.5 既有逐 RT inner Card（kind Tag / separator 高亮 / Collapse+Table 位置明细）全部保留，Card title 改为「原始聚合明细」作兜底，数据源 `blindAggregation` 不动。
- 向后兼容：`blind_aggregation` 数组契约保留不动；新增 `reconstructed_database` 键 + 新结构字段 `schema`（Option）/ `column_separator`（Option）均 `skip_serializing_if`，旧消费者读不到不影响。

## 3. 工作区布局

```
RuT0DataKit/
├── Cargo.toml                    # workspace
├── crates/
│   └── core/                     # ruT0-data-kit-core
│       ├── Cargo.toml
│       └── src/{lib,pipeline,rules,readers,validators,maskers,scan,report,tools}.rs
├── src-tauri/                    # Tauri GUI
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   └── src/main.rs
├── frontend/                     # Tauri 前端（v0.1.0：React 18 + Vite 5 + Ant Design 5）
├── rules/                        # 内置规则 YAML
│   └── default_mask.yaml         # v0.1.0 必备
├── tests/fixtures/samples/...
└── docs/
```

布局说明（v0.1.0 重构 + 风格优化补丁）：T0-10~T0-13 扩 crate 内模块与 src-tauri 命令、重写前端视图；T0-14 将前端从 vanilla JS 重写为 React 18 + Vite 5 + Ant Design 5 + @ant-design/icons（新增 `frontend/package.json`、`vite.config.js`、`index.html`（Vite 入口）、`src/{App.jsx,main.jsx,components/*,tauri.js,maskerDefs.js,state.js}`，删除旧 `frontend/main.js`、`frontend/styles.css`）。`tauri.conf.json` 的 build 段接 Vite：`frontendDist=../frontend/dist`、`devUrl=http://localhost:5173`、`beforeDevCommand/beforeBuildCommand = npm --prefix frontend run dev/build`。

## 3.1 关键数据流（v0.1.0 四功能重构补丁后，T0-15 ~ T0-20）

```
[ PreprocessView 内置导入入口 CSV/XLSX/SQL/JSON/PCAP/LOG ]
        │
        ▼
detect_type ──► SourceReader::read ──► Records { headers, rows }
        │       (v0.4.0: read_records 统一入口支持 csv/xlsx/sql/json/pcap/log)
        │
        ▼
load_preview / read_records (src-tauri command)
   - 取前 N 行 + headers，回填前端 state（records / headers / rows）
        │
        ├──────────────────────┬──────────────────────┬──────────────────────┐
        ▼                      ▼                      ▼                      ▼
[ MaskView ]            [ ValidateView ]       [ ExportView ]         [ RulesView ]
列勾选 selected_columns  选 validators          选数据源/列/序/格式     编辑规则 + description
+ rules.maskers         run_validate           export_records_csv     保存 YAML save_ruleset
apply_rules_cols        → state.validateResult export_records_xlsx    加载 YAML read_ruleset
→ state.maskedRows      「跳转到数据导出」       export_records_json    查看规则说明 RuleDocPanel
                                                 → 用户指定路径          （未选规则时占位提示）
```

跨视图 state：`App.jsx` 顶层 `useReducer` 常驻 `state = { view, records, rules, maskedRows, selectedColumns, validateResult, exportConfig, ... }`；`SET_VIEW` action 只切 `view` 字段，不重置其他数据，因此切回各 view 数据不丢。v0.4.0 T6-2：顶部常驻导入工具条已移除，导入仅作为 PreprocessView 内置入口存在；其他 view 在无 records 时渲染 Empty 引导用户回预处理视图导入。

ExportView 数据源说明：
- **原始数据**：直接投影 `state.rows`，不脱敏。
- **脱敏数据**：投影 `state.maskedRows`（已由 `apply_rules_cols` 产出）。
- **校验数据**：ExportView 校验源导出 = **先按 `selected_columns` 跑 `mask_pipeline_columns`，再投影列序**；即校验源仍会应用 maskers（src-tauri `exportRecordsCsv` / `exportRecordsXlsx` 内部先调 `mask_pipeline_columns`）。若需导出未脱敏原始数据，请选「原始数据」源。此为设计选择，非 bug。

要点：
- `mask_pipeline_columns` 是按列勾选脱敏入口；`mask_pipeline`（全量）与 `mask_pipeline_selected`（行选择）保留向后兼容，三者共用 `build_masker` dispatch。
- `validate_pipeline` 不改数据，只产出 `valid_matrix`；非法 cell 在前端高亮。
- `MaskRule` / `FieldRule` 均带 `description: Option<String>`（`#[serde(default)]`），RulesView 的 RuleDocPanel 读取该字段展示规则说明。
- T5-1 增：`MaskRule` / `FieldRule` 均带 `tags: Vec<String>`（`#[serde(default)]`，旧 YAML 无 tags 字段时默认空 Vec，向后兼容）。`RuleSet::by_tag(tag) -> Vec<&FieldRule>` / `RuleSet::by_tag_mask(tag) -> Vec<&MaskRule>` 按 tags 包含关系过滤规则，供 T5-4 GUI 按标签筛选、T5-7 按脱敏/校验筛规则、T5-9 SQL 解析按 `sql_parse` 标签取规则。常用标签：`mask` / `validate` / `sensitive` / `search` / `sql_parse`。
- T5-4 增：src-tauri 新增 `list_rule_tags(rules_json: Option<String>) -> Result<Vec<String>, String>`，返回预置标签（对 `mask` / `validate` / `sensitive` / `search` / `sql_parse` 调 `list_tagged_presets` 取非空集合）∪ 当前 ruleset 中 `maskers` / `validators` 实际出现过的 tag 的并集，排序后返回；`rules_json` 缺失 / 空串 / 反序列化失败时退化为仅预置标签（不抛错）。前端 `RulesView` 顶部「按标签过滤」Select 与 `RuleDrawer` 的 tags `Select(mode="tags")` 共用此下拉源。`RulesView` 列表每条规则的 tags 列用 antd Tag 渲染，颜色按 tag 字符串哈希取 antd preset colors 取模（稳定映射）。前端 state 新增 `rulesTagFilter: Option<String>` + `SET_RULES_TAG_FILTER` action。
- 规则既可来自 YAML（`read_ruleset` / `load_ruleset`），也可由前端编辑后以 JSON 透传后端（`apply_rules_cols` / `run_validate` 直接接 `RuleSet`），不再强制走 YAML 文件。
- `summary.masked_rows` 只统计被脱敏的行；`by_field` 只统计 masker 被调用的 cell 数。

**UI 重构后数据流（T0-21~T0-26）**：

- RulesView 试运行：前端把当前规则（`MaskRule` / `FieldRule`）+ 当前数据首行 value 透传到 `preview_mask_rule` / `preview_validate_rule`（src-tauri 命令）；命令内部走 `MaskOp::from_rule` / `ValidateOp::from_rule` 构造算子 → 调 `apply_mask_op` / `apply_validate_op` → 返回 `{ input, output }`（脱敏）或 `{ input, valid, message }`（校验）；前端 Drawer 内卡片展开结果，不写 state、不改实际数据。
- MaskView / ValidateView 四段垂直：「原始数据 → 表头-规则映射 → 预览 → 操作面板」；操作面板「应用」按钮触发 `apply_rules_cols`（脱敏）/ `run_validate`（校验）→ reducer `SET_MASKED` / `SET_VALIDATE` 把结果回填 `state.maskedRows` / `state.validateResult` → 预览表 rerender；未点应用前预览表为空。MaskView「导出」按钮调 `SET_VIEW("export")` 跳转 ExportView（不直接导出）。
- ExportView 单一 Table：`columnOrder` / `exportColumns` / `exportFormat` 三段 state 常驻 reducer（切走再切回不丢）；Table 表头 checkbox 勾选列、上下移按钮调列序、单元格预览按当前列序+勾选投影数据；「导出」按钮按 `columnOrder ∩ exportColumns` + `exportFormat` 调 `export_records_csv` / `export_records_xlsx` / `export_records_json`，src-tauri 内部按数据源类型 dispatch（原始数据=直接投影 rows，脱敏数据=投影 maskedRows，校验数据=先 `mask_pipeline_columns` 再投影）。v0.6.0 T5-8 增 `export_records_json`：与 csv/xlsx 同签名同流程，把每行按 `column_order` 映射为 `HashMap<String, String>`（headers 做 key），用 `serde_json` 序列化为 JSON 数组写盘（每行扁平对象，value 全为字符串，不做嵌套）。数据源 Select 的 `exportSource` 值扩为 `masked` / `validate` / `records`，旧值 `raw` 保留为 `records` 别名（ExportView 内部归一）。

### 2.9 v0.3.0 流量包处理切片（pcap）

新增 `crates/core/src/pcap/` 模块（reader / decoder / scanner 三子模块）：

- `pcap/reader.rs::HttpRequest`（`#[derive(Debug, Clone, serde::Serialize)]`）字段：`frame_no: u32` / `src_ip: String` / `dst_ip: String` / `method: String` / `host: String` / `uri: String` / `body: Option<String>` / `user_agent: String`。
- `pcap/reader.rs::PcapReader::read(path: &Path) -> Result<Vec<HttpRequest>, CoreError>`：
  - 探测：`Command::new("tshark").arg("--version")`，失败 → `CoreError::DependencyMissing("tshark")`，不 panic。
  - 主命令：`tshark -r <path> -Y "http.request" -T fields -e frame.number -e ip.src -e ip.dst -e http.request.method -e http.host -e http.request.uri -e http.file_data -e http.user_agent -E separator=\t -E occurrence=f`。
  - 解析：stdout 按行 split → 每行按 `\t` split 8 字段（<7 字段跳过）；`http.file_data` 是 hex 串，手写 `hex_to_bytes(s: &str) -> Option<Vec<u8>>`（trim → empty 返回 `Some(Vec::new())` → 奇数长度 `None` → 非 hex 字符 `None` → 每 2 char 一字节）→ `String::from_utf8_lossy`。
- `pcap/decoder.rs`：
  - `decode_url_twice(input: &str) -> String`：独立实现（不依赖 log 模块，避免循环依赖）。
  - `try_decode_base64_field(value: &str) -> Option<String>`：要求 `length ≥ 4` + `length % 4 == 0` + charset ⊂ `[A-Za-z0-9+/=]`；手写 `b64_decode`（不引入 `base64` crate）；UTF-8 only（GBK 留 v0.3.1+）；`printable_ratio ≥ 0.8` 防误伤（如 16 位银行卡解码后多为非可打印字节被拒）。
  - `extract_decoded_fields(body: &str) -> Vec<(String, String)>`：三降级——① `serde_json::from_str` 解析为 Object → 每字段值递归 `try_decode_base64_field`；② JSON 解析失败再降级 `form-urlencoded`（`&` split → `=` split → `decode_url_twice` → `try_decode_base64_field`）；③ 仍失败则整体当单字段 `_body`。
  - `reassemble_base64(field_values: &[String], _param: &str) -> Option<String>`：规则化兜底（fixture 每 POST body 单独 base64 不需要，保留接口供 CTF 分块场景）。
- `pcap/scanner.rs::PcapScanner::scan(path: &Path, scan: &DefaultSensitiveScan, rules: &RuleSet) -> Result<Report, CoreError>`：
  - 主循环：对每个 `HttpRequest` 拼装 `scan_text = decode_url_twice(uri)` + body 经 `extract_decoded_fields` 解出的非空字段值（累加 `decoded_fragments`）→ `scan.scan(&scan_text, rules)` → 每个 `Finding` 填 `location = Some("frame:{frame_no}")` + `context = Some("{method} {host} -> {src_ip}")` + `extra = None` → 累加 `sensitive_hits` + `ip_counts: HashMap<String, u64>`。
  - summary `serde_yml::Mapping`：`total_requests` / `sensitive_hits` / `decoded_fragments` / `top_src_ips`（按 hits 倒序取前 5，每项 `{ip, hits}` mapping）。
  - 返回 `Report { source, kind: "pcap_scan", summary: Value::Mapping, findings, extra: Value::Null }`。
- `pipeline/pcap_scan.rs::scan_pcap`：薄包装透传 `PcapScanner::scan`，`pipeline/mod.rs` 导出。
- src-tauri `commands.rs::scan_pcap_file(path: String) -> Result<Value, String>`：`PcapReader::read` → `DefaultSensitiveScan::new()` + `pcap_sensitive_ruleset()`（独立 helper，返回 idcard/phone/name validators RuleSet，不污染 mask view 的 `load_default_mask_ruleset`）→ `scan_pcap` → `serde_json::to_value(&report)` + `serde_json::to_value(&requests)` → `Ok(json!({ "entries": ..., "report": ... }))`；`main.rs` `generate_handler!` 注册（共 21 命令）。
- 前端 `PcapView.jsx` 四段布局：① 导入 `.pcap/.pcapng`（`select_file` → `detect_source_type === "pcap"` → `scanPcapFile`）；② 原始 HTTP 表（pageSize 50）；③ 扫描按钮 + summary Descriptions + top_src_ips volcano Tag；④ Findings 表（type Tag 按 `TAG_COLOR_BY_TYPE` 着色）。tshark 缺失：`msg.includes("tshark") || msg.includes("DependencyMissing")` → `message.error("流量分析需要系统 tshark，请先安装 Wireshark CLI (brew install wireshark)")`。state 加 `pcapEntries` / `pcapReport` / `pcapLoading`；`Sidebar.jsx` 删除 pcap `disabled: true`。
- 向后兼容：`kind="pcap_scan"` 新增，旧 `kind`（`csv_mask` / `log_scan`）不破；`extra: Value::Null`；Finding/Report schema 不动。

### 2.10 v0.4.0 tools 模块（正则解析后端，T5-11）

新增 `crates/core/src/tools/` 模块（与 T5-9 的 `sql_parse` 同属 tools 工具集，二者独立），含两个子模块：

- `tools/regex_explain.rs::RegexTokenDesc { token, kind, description, position }`（`kind` ∈ `literal` / `char_class` / `quantifier` / `anchor` / `group` / `backref` / `assertion` / `escape` / `unsupported`）+ `explain_regex(pattern: &str) -> Result<Vec<RegexTokenDesc>, CoreError>`。
  - **不**实现正则引擎：用 `regex::Regex::new` 做合法性校验（非法返回 `CoreError::InvalidInput`，不 panic）；token 解释用手写逐字符扫描 + 状态机。
  - 覆盖：字面量、字符类 `[...]` + `\d \w \s` 等、量词 `* + ? {n,m}`（含 lazy `*?` / possessive `++` 后缀合并进同一 token）、锚点 `^ $ \b \B`、分组 `(...)` / `(?:...)` / `(?P<name>...)` / 内联标志 `(?i)`、反向引用 `\1`（`regex` crate 引擎本身不支持反向引用，会触发 `backreferences are not supported` 错误；`explain_regex` 把这类错误视为合法输入继续解释，其他错误才返回 `Err`）、转义 `\. \\` `\xHH` `\uHHHH`。
  - PCRE 专属 look-around（`(?=...)` / `(?!...)` / `(?<=...)` / `(?<!...)`）标 `unsupported` 并给出说明；由于 `regex::Regex::new` 会拒绝，整体返回 `Err`（用户拿不到 token 列表，但不会 panic）。
- `tools/regex_template.rs`：预置模板库 email / phone_cn / idcard_cn / ipv4 / url / sql_injection_bool / sql_injection_union / mac。`TemplateMeta { name, description, params_schema: Vec<TemplateParam> }`（`TemplateParam { key, description, required, default }`）+ `generate_regex(template_name, params: &HashMap<String,String>) -> Result<String, CoreError>` + `list_regex_templates() -> Vec<TemplateMeta>`。模板生成后再用 `regex::Regex::new` 兜底校验。
  - `email` 模板固定生成 `^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$`；`url` 模板支持 `protocol` 参数（`http` / `https` / `both`，默认 `both`）。
- `tools/mod.rs` re-export `explain_regex` / `generate_regex` / `list_regex_templates` / `RegexTokenDesc` / `TemplateMeta`；`lib.rs` 增 `pub mod tools;`（T5-9 若先落地 `sql_parse`，则保留其声明并追加本任务的两个子模块）。
- Tauri 新增三命令：`explain_regex(pattern: String) -> Result<Vec<RegexTokenDesc>, String>` / `generate_regex(template_name: String, params: HashMap<String,String>) -> Result<String, String>` / `list_regex_templates() -> Result<Vec<TemplateMeta>, String>`，并在 `main.rs` 的 `invoke_handler` 注册。下游 T5-12 GUI 调用这三个命令实现「解析 Tab + 模板 Tab」。

### 2.11 v0.4.0 7 界面架构性重构（T5-1 ~ T5-13）

本节补齐 v0.4.0 新增/扩展模块的职责边界，与 §2.1 ~ §2.10 既有描述互补，不替换既有内容。

#### 2.11.1 readers（§2.2 扩展，T5-2）

§2.2 已落 SqlReader / JsonReader / PcapRecordsReader / LogRecordsReader 四个适配器，本节补充语义约束与 e2e 验收点：

- `SqlReader`：
  - 拆分算法按 `;` 切片，单引号 `'...'` / 双引号 `"..."` 字符串内的 `;` 不切；`--` 至行尾的注释整行跳过；空切片丢弃。
  - `statement_type` 由语句首非空白 token 小写归类：`select` / `insert` / `update` / `delete` / `create` / `alter` / `drop` / `other`。
  - e2e（`preprocess_6_sources`）：6 语句 fixture（含 `'bob; jr'` 字符串内分号）必须读出 6 行，`statement_type` 集合 ⊇ `{select, insert, update, drop, create}`，headers == `["sql_text", "statement_type"]`。
- `JsonReader`：
  - JSON 数组对象 → headers=对象 keys 并集（按首次出现序）+ rows；缺失 cell 补 `""`。
  - JSON 单对象 → 1 行；JSON Lines（每行一对象）→ 同数组语义；纯标量数组 / 字符串 → 单列 `value`。
  - 嵌套对象 / 数组最小实现：`to_string()` 成 JSON 串放入单 cell（v0.4.1+ 再做展平）。
  - e2e：3 对象 union keys fixture（id/name/email/phone/active）必须读出 3 行，headers ⊇ `{id, name, email}`。
- `PcapRecordsReader`：透传 `CoreError::DependencyMissing("tshark")`，不 panic；tshark 可用时输出 8 列 Records（frame_no/src_ip/dst_ip/method/host/uri/body/user_agent）。`#[ignore]` 集成测试 `preprocess_pcap_source_readable` 覆盖。
- `LogRecordsReader`：调 `log::LogReader::read` → 12 列 Records（line_no/ip/timestamp/method/path/query/status/size/user_agent/decoded_path/decoded_query/decoded_ua），不依赖 tshark。e2e `preprocess_log_source_readable` 覆盖（rows.len() ≥ 100）。
- `read_records(path)` 统一入口按 `detect_type` 后缀 dispatch，`SourceType` 枚举扩 `Sql` / `Json` 变体；src-tauri `preprocess_file(path) -> Result<Value, String>` 命令把任意支持格式统一读成 `{ headers, rows, source_type, row_count }` JSON。

#### 2.11.2 search（新增模块，T5-3）

新增 `crates/core/src/search/` 模块（独立于 §2.5 scan），承担「3 模式搜索 + 倒排索引」职责：

- `search/mod.rs` 导出 `SearchIndex` / `SearchQuery` / `SearchMode` / `SearchHit` / `search_records` / `search_keyword` / `search_regex` / `search_exact_field`。
- `SearchIndex::build(records: &Records) -> SearchIndex`：
  - 倒排索引 `token → (row_idx, col_idx)`，token 由 cell 值按非字母数字字符切分后小写化得到。
  - 大文件基线（e2e `search_big_file`）：100k 行 × 10 列、稀疏 `needle`（仅 r%1000==0 行含 `needle`）→ build < 5s，`search_keyword(&index, &["needle"], Or)` < 200ms。
- `SearchQuery` serde tag enum（`#[serde(tag="kind", rename_all="snake_case")]`）：
  - `Keyword { terms: Vec<String>, mode: SearchMode }`：mode ∈ `Or` / `And`，对 terms 做倒排命中合并。
  - `Regex { pattern: String }`：`regex::Regex::new` 预编译，逐 cell `is_match`，非法 pattern 返回 `CoreError::InvalidInput`。
  - `ExactField { field: String, value: String }`：按列名定位 + 精确相等匹配，避免全表扫描。
- `search_records(records, query) -> Vec<SearchHit>`：统一入口，内部按 `SearchQuery` 变体 dispatch 到 `search_keyword` / `search_regex` / `search_exact_field`；返回 `SearchHit { row_idx, col_idx, value, snippet }`，`snippet` 为命中 cell 值（不做高亮，高亮由 GUI `<mark>` 渲染）。
- Tauri 命令 `search_records(records: Value, query: Value) -> Result<Value, String>` 透传 JSON，GUI `SearchView.jsx` 3-mode `Radio.Group` 切换 + 命中 Table `<mark>` 高亮 + 「跳转脱敏 / 跳转校验 / 跳转导出」按钮写入 `state.filteredRowIndices`（命中行的 row_idx 集合）。

#### 2.11.3 tools/sql_parse（T5-9，与 §2.10 regex 同属 tools 工具集）

§2.10 已描述 `tools/regex_*`，本节补充同模块下的 `tools/sql_parse.rs`：

- `parse_sqls(inputs: &[SqlParseInput]) -> Result<SqlParseResult, CoreError>`：主入口，返回 `SqlParseResult { probes, aggregated, reconstructed, parsed_payloads }`。
- `extract_blind_probe(input: &SqlParseInput) -> Result<Option<BlindProbe>, CoreError>`：从单条 `(sql, response_body_size, source_ip)` 三元组提取盲注探针；`ProbeKind` 枚举（`#[serde(rename_all="snake_case")]`）= `AsciiBinary` / `Equality` / `Length`，覆盖 v0.2.2 ~ v0.2.3 的三轴盲注。
- `BlindAggregator`（复用 §2.8.4 ~ §2.8.6 既有实现）：把 `Vec<BlindProbe>` 聚合成 `Vec<BlindAggregatedResult>`，再交叉关联还原成 `ReconstructedDatabase`。
- `ParsedPayload` 结构（T5-9 新增，`#[derive(serde::Serialize)]`）：字段 `attack_type` / `technique` / `read_target` / `char_position` / `compared_ascii` / `comparator` / `union_columns` / `sleep_seconds` / `summary`，由 `payload_parser`（§2.8.2）扩展产出，供 GUI `SqlParseTool.jsx` 渲染「payload 语义解析」Table。
- `ReconstructedTable.rows: Vec<ReconstructedRow{cells: Vec<Option<String>>}>`：未 fetch 列 `None`，GUI 渲染为 `-`。
- e2e `sql_parse_tool_full`：4 read_target 探针序列（database() → `"person"`、group_concat(table_name) → `"person_data"`、group_concat(column_name) → 7 列、group_concat(id,0x7e,username,0x7e,idcard) → `"1~zhangsan~IDCARD1,2~lisi~IDCARD2"`）→ `reconstructed.schema == Some("person")`、`tables[0].name == "person_data"`、`columns == ["id","username","password","sex","birth","idcard","phone"]`、`rows[0].cells[0] == Some("1")`。
- Tauri 命令 `parse_sql_tool(inputs: Vec<SqlParseInput>) -> Result<SqlParseResult, String>`；GUI `SqlParseTool.jsx` 渲染 Descriptions + 每表 Card + unmatched + probes Table + parsed_payloads Table。

#### 2.11.4 rules tags 多标签（§2.1 扩展，T5-1）

§2.1 已描述 `rules::presets::list_tagged_presets`，本节补充 `RuleSet` / `FieldRule` / `MaskRule` 的 tags 字段与按标签查询语义：

- `FieldRule` 增 `tags: Vec<String>` 字段（serde 默认空 vec，YAML 向后兼容）；`MaskRule` 同。
- `RuleSet::by_tag(tag: &str) -> Vec<&FieldRule>`：返回 `tags` 包含 `tag` 的 validators 子集；`RuleSet::by_tag_mask(tag: &str) -> Vec<&MaskRule>` 同语义对 maskers。
- 约定标签词表：`mask` / `validate` / `sensitive` / `search` / `sql_parse`，与 `list_tagged_presets` 的 tag 入参一致；无标签规则视为「通用」，`by_tag` 不返回但 mask/validate pipeline 仍会兜底应用（MaskView `effectiveMaskers` 取 `by_tag_mask("mask")` + 无标签兼容）。
- e2e `rules_multi_tag`：MaskRule tags=`[mask, sensitive]` → `by_tag_mask("mask").len()==1` 且 `by_tag_mask("sensitive").len()==1`；FieldRule tags=`[validate, sensitive]` → `by_tag("validate").len()==1` 且 `by_tag("sensitive").len()==1`；负例 `by_tag("nonexistent").is_empty()`。
- GUI `RulesView.jsx` 多标签编辑：Drawer 内 antd `Select mode="tags"` 编辑 tags 数组，保存回 YAML；RulesView 列表按 tag 过滤。

#### 2.11.5 前端 7 界面同步（T5-4 ~ T5-12）

- `PreprocessView.jsx`（T5-4）：`select_file` → `detect_source_type` → `preprocess_file` → `state.records` + 4 跳转按钮（搜索/脱敏/校验/导出）。
- `RulesView.jsx`（T5-5）：多标签 Drawer 编辑 + tag 过滤列表。
- `SearchView.jsx`（T5-6）：3-mode `Radio.Group`（keyword/regex/exact_field）+ 命中 Table `<mark>` 高亮 + 跳转按钮写 `state.filteredRowIndices`。
- `MaskView.jsx`（T5-7）：4 段垂直布局 + 「仅搜索命中行」`Switch` 消费 `state.filteredRowIndices`（`effectiveRows` 过滤后喂预览/应用）。
- `ValidateView.jsx`（T5-8）：4 段垂直布局，validate-tag 过滤规则子集。
- `ExportView.jsx`（T5-10）：单 Table + 「仅导出搜索命中行」`Switch`（默认 on），`onlyFiltered && hasFiltered` 时把 `selectedRowIndices = [...state.filteredRowIndices].sort()` 透传后端。
- `ToolsView.jsx`（T5-12）：`Tabs` 容器，`SqlParseTool`（Descriptions + 每表 Card + unmatched + probes + parsed_payloads Table）+ `RegexTool`（解析 Tab + 模板 Tab）。
- v0.2.0/v0.3.0 的 LogView / PcapView 保留（向后兼容），不在 v0.4.0 的 7 界面主流程中。

## 4. 依赖策略

- `serde` / `serde_yaml`（或 `serde_yml`）/ `csv`（core 用 `calamine` 读 xlsx；src-tauri 用 `csv = "1"` 写 CSV 导出）/ `calamine`(xlsx 读) / `regex` / `base64`(v0.3.0) / `url`(v0.3.0) / `tempfile`(v0.3.0) / `thiserror`。
- `rust_xlsxwriter = "0"`（src-tauri 侧 XLSX 导出，T0-16 增）。
- pcap（v0.3.0）：调用 `tshark` 子进程，不直接依赖 libpcap。
- Tauri：v2 稳定版（`tauri = "2"` + `tauri-plugin-dialog = "2"`，环境无 v1）。前端基于 React 18 + Vite 5 + Ant Design 5 + @ant-design/icons；`js-yaml@5.2.1`（frontend 侧 YAML 序列化/反序列化，T0-19 增，用于 RulesView 保存/加载 YAML）。
- 公共算子 API（T0-21 增）：core 暴露 `apply_mask_op(op, value) -> String` / `apply_validate_op(op, value) -> ValidationResult` / `MaskOp::from_rule` / `ValidateOp::from_rule` / 预置库 `lookup_mask_preset` / `lookup_validate_preset` / `list_mask_op_types` / `list_validate_op_types`，作为 src-tauri 试运行命令（`preview_mask_rule` / `preview_validate_rule` / `list_mask_op_types` / `list_validate_op_types`，T0-22 增）与未来 CLI / 测试的统一入口。
- 不引入 Python、不引入 `pyo3`。

## 5. 错误处理

- 所有公共 API 返回 `Result<T, CoreError>`，`CoreError` 用 `thiserror` 派生。
- v0.1.0 即定义 `DependencyMissing(String)` 变体（v0.3.0 tshark 用，提前定义）。

## 6. 测试策略

- 单元测试：每个 masker 一条 `#[test]`，覆盖 spec.pdf 给的正反例。
- 集成测试：`tests/` 下用 fixtures 跑端到端，断言 masked.csv 关键字段。
- v0.2.0/v0.3.0 的 pcap 集成测试在 tshark 不可用时 `#[ignore]`。
