# 03 — 开发任务清单（v1.0.0 框架阶段 + v1.1.0 能力阶段 + v1.1.1 能力阶段）

> 本清单服务于 [`00-需求文档.md`](./00-需求文档.md) 定义的 MVP 边界。v1.0.0 搭建纯框架 shell（四区布局 + Sheet/Tab 工作台 + SQLite 全量持久 + Tauri updater 签名 + AI 占位 + 设置入口）。v1.1.0 在此之上落地数据处理原型三件套（脱敏/校验/提取）+ 规则管理基础结构（3 条姓名规则 + DB 持久化）。v1.1.1 在此之上落地撤销/重做 + 列操作 + 搜索 + commands 层测试。后续版本在本清单基础上扩展。

## 1. 任务 DAG 总览

```
T1 工作区脚手架
 ├─ T2 四区布局 shell ─────────────┐
 ├─ T4 SQLite 持久层 ──┐            │
 │                     ├─ T5 导入流 │
 T3 Sheet/Tab + Table ─┘            │
 ├─ T6 Tauri updater 插件           │
 ├─ T7 AI 占位 IPC 契约             ├─ T9 双语 README + docs + QA
 └─ T8 设置页 + 7 项操作占位提示 ───┘
```

依赖关系说明：

- T1 为所有任务前置（脚手架未就绪则无法编译）。
- T2、T3、T4、T6、T7 在 T1 完成后可并行启动。
- T5 依赖 T3（工作台 UI）+ T4（持久层）。
- T8 依赖 T2（布局壳）+ T6（设置页需要 updater 检查能力）。
- T9 依赖全部前序任务 verified_complete 后做端到端文档与 QA 收口。

## 2. 任务详细定义

每任务字段：目标 / 前置依赖 / 实现要点 / 交付物 / 验收标准 / 验证方式。

---

### T1 工作区脚手架

**目标**：从零建立 Cargo workspace + 前端工程 + src-tauri Tauri v2 工程，版本号统一为 `1.0.0`，本地 `cargo tauri dev` 可启动空白窗口。

**前置依赖**：无。

**实现要点**：
- 根 `Cargo.toml`：workspace members = `crates/core`、`src-tauri`，统一 `edition = "2021"`。
- `crates/core/Cargo.toml`：`ruT0-data-kit-core`，v1.0.0 仅骨架（lib.rs + 空模块 `datasource`/`processor`/`model` + trait 占位），不引入业务依赖。
- `src-tauri/Cargo.toml`：`ruT0-data-kit`，依赖 `tauri = "2"`、`tauri-plugin-dialog`、`tauri-plugin-updater`、`rusqlite = { version = "0.32", features = ["bundled"] }`、`serde`/`serde_json`；path dep `../crates/core`。
- `src-tauri/tauri.conf.json`：`productName = "RuT0DataKit"`，`version = "1.0.0"`，`identifier = "com.rut0.datakit"`，`beforeBuildCommand = "pnpm --prefix ../frontend run build"`，`devUrl = "http://localhost:5173"`，`frontendDist = "../frontend/dist"`。
- `frontend/`：Vite 5 + React 18 + antd 5 + @ant-design/icons，`pnpm` 为包管理器；`package.json` 版本同步 `1.0.0`。
- 前端入口仅渲染一个空 antd Layout 占位，确认 Tauri ↔ 前端 dev 链路通。

**交付物**：
- 根 `Cargo.toml` + `Cargo.lock`
- `crates/core/`（骨架）
- `src-tauri/`（Tauri v2 工程骨架，可编译）
- `frontend/`（Vite + React + antd 骨架）
- `rust-toolchain.toml`（固定 Rust 版本，不用 `stable` 浮动 channel）

**验收标准**：
- `cargo check --workspace` 通过。
- `pnpm --prefix frontend install --frozen-lockfile` 后 `pnpm --prefix frontend build` 通过。
- `cargo tauri dev` 可启动空白窗口，前端 antd Layout 渲染正常。
- `tauri.conf.json`、`Cargo.toml`（根 + core + src-tauri）、`package.json` 版本号一致为 `1.0.0`。

**验证方式**：
```sh
cargo check --workspace
pnpm --prefix frontend install --frozen-lockfile && pnpm --prefix frontend build
cargo tauri dev  # 手动确认窗口启动
```

---

### T2 四区布局 shell

**目标**：实现四区布局——上方工具栏（左组数据操作 + 右组能力按钮）/ 左侧动态能力面板 / 中央 Workbench 占位 / 右侧 AI 面板占位，按 [`01-页面与交互说明.md`](./01-页面与交互说明.md) §1 的占比与分区落地。

**前置依赖**：T1。

**实现要点**：
- antd `Layout`：`Header`（工具栏）+ `Sider`（能力面板）+ `Content`（Workbench）+ 右侧固定宽度面板（AI 占位）。
- 工具栏左组 6 项：导入 / 导出 / 格式 / 撤销 / 列操作 / 运行；v1.0.0 仅「导入」可用，其余点击 `message.info("...能力 v1.1+")`。
- 工具栏右组 4 项能力按钮：脱敏 / 校验 / 提取 / 规则管理；点击后 dispatch `SET_ACTIVE_CAPABILITY` 切换左侧能力面板，面板内展示占位提示「…能力 v1.1+ 释放」。左右组之间用竖线 Divider 视觉分隔。
- 左侧能力面板：由 `activeCapability` state 驱动，切换 MaskPanel / ValidatePanel / ExtractPanel / RulesPanel。未点任何能力按钮时显示空态占位。底部常驻「⚙ 设置」齿轮按钮。
- 中央 Workbench 占位：空状态提示「导入数据后在此展示工作台」。
- 右侧 AI 面板占位：展示「AI 能力 v1.1+ 释放」+ IPC 契约说明文案。
- 响应式：窗口最小宽度 1024px，右侧 AI 面板可折叠隐藏。

**交付物**：
- `frontend/src/components/layout/TopToolbar.jsx`（含左组 + 右组 + Divider）
- `frontend/src/components/layout/SidePanel.jsx`（动态面板容器 + 设置齿轮）
- `frontend/src/components/panels/{MaskPanel,ValidatePanel,ExtractPanel,RulesPanel}.jsx`（占位）
- `frontend/src/components/Workbench.jsx`（占位）
- `frontend/src/components/AiPanel.jsx`（占位）
- `frontend/src/App.jsx`（组合四区）
- `frontend/src/state.js`（reducer 骨架含 `activeCapability` 字段 + `SET_ACTIVE_CAPABILITY` action）

**验收标准**：
- 四区可见且占比符合 01 文档定义。
- 工具栏左组 6 项 + 右组 4 项全部渲染，左组禁用项点击弹 `v1.1+` 提示。
- 右组能力按钮点击后左侧面板切换到对应能力面板，同一时刻最多一个能力按钮高亮激活。
- 左侧面板底部「⚙ 设置」始终可见。
- 右侧 AI 面板可折叠。
- 布局在 1024×768 下无溢出。

**验证方式**：`cargo tauri dev`，手动核验四区、能力按钮切换面板、点击提示。

---

### T3 Sheet/Tab 多页 + antd Table

**目标**：实现中央 Workbench 的 Sheet/Tab 多页 + antd Table（选区 / 列选 / 列序 / 状态高亮），v1.0.0 状态高亮仅 `default`。

**前置依赖**：T1。

**实现要点**：
- Sheet/Tab：新建 / 切换 / 关闭 / 重命名（双击 Tab 标题进入编辑）。
- antd Table：`rowSelection`（复选 + 框选区间选择）、`columnCheckbox`（列可见性）、列拖拽排序（`react-resizable` 或 antd `Table.resizeColumn`）。
- 状态高亮：行 `className` 枚举 `default/invalid/masked/hit`，v1.0.0 仅 `default` 样式，其余样式占位但不触发。
- 分页：默认 50 行/页，`LIMIT/OFFSET` 由后端 `get_sheet_data` 提供（T5 落地，T3 先用 mock 数据跑通交互）。

**交付物**：
- `frontend/src/components/SheetTabs.jsx`
- `frontend/src/components/DataTable.jsx`
- `frontend/src/state.js`（reducer 骨架：sheets[] / activeSheetId / selection / columnOrder / columnVisibility）

**验收标准**：
- 可新建多个 Sheet Tab 并切换、关闭、重命名。
- Table 支持行复选 + 区间选择、列 checkbox 显隐、列拖拽排序。
- 分页控件渲染正常（mock 数据）。

**验证方式**：`cargo tauri dev`，mock 数据手动核验交互。

---

### T4 SQLite 持久层

**目标**：建立 rusqlite（bundled）持久层，落地 5 表 schema + 初始化迁移 + 管理器，DB 文件位于 `app_config_dir/ruT0datakit.db`。

**前置依赖**：T1。

**实现要点**：
- `src-tauri/src/db/mod.rs`：`DbManager` 持有 `Mutex<Connection>`，`new(app_config_dir)` 初始化或打开 DB。
- 5 表 schema（见 [`02-技术设计文档.md`](./02-技术设计文档.md) §3）：`sessions` / `sheets` / `cells` / `operations` / `app_settings` + 3 个索引。
- 迁移：首次启动建表，后续启动校验 schema 版本（`app_settings` 写 `schema_version`），不兼容则备份旧 DB 后重建。
- 大表策略：`cells` 全量写入；前端分页查询用 `LIMIT/OFFSET`，默认 50/页。
- 事务：导入写 `cells` 用单事务批量插入，失败回滚。

**交付物**：
- `src-tauri/src/db/mod.rs`（DbManager + schema + 迁移）
- `src-tauri/src/db/schema.sql`（建表 + 索引 DDL，供嵌入或 include_str!）

**验收标准**：
- 首次启动生成 DB 文件，5 表 3 索引存在（`sqlite3 .schema` 核验）。
- 重复启动不重复建表，`schema_version` 正确。
- 插入 5 万行 `cells` 后查询分页响应 < 200ms。

**验证方式**：
```sh
# 启动后定位 DB 文件
sqlite3 ~/Library/Application\ Support/com.rut0.datakit/ruT0datakit.db ".schema"
cargo test -p ruT0-data-kit db::  # 单元测试覆盖建表/迁移/分页
```

---

### T5 导入流

**目标**：打通「选文件 → 识别 → 新建 Sheet → 写 SQLite → 渲染 Table」全链路，v1.0.0 支持 CSV 与 XLSX。

**前置依赖**：T3（工作台 UI）、T4（持久层）。

**实现要点**：
- 文件选择：`tauri-plugin-dialog` `open` 过滤 `.csv,.xlsx`。
- 识别：CSV 用 `csv` crate；XLSX 用 `calamine`。首行作为表头。
- 新建 Session + Sheet：写 `sessions`（source_path/source_type/row_count）+ `sheets`（name=文件名，position 自增）。
- 写 cells：事务批量 `INSERT INTO cells`；大表分批 commit（每 5000 行）。
- 操作日志：写一条 `operations`（kind=`import`，params_json 记录路径与行数）。
- 返回 `ImportResult { sessionId, sheetId, rowCount, columns }`，前端新建 Tab + 首页数据 `get_sheet_data`。

**交付物**：
- `crates/core/src/datasource/mod.rs`（CSV/XLSX 解析 trait + 实现）
- `src-tauri/src/commands.rs`：`import_file(path) -> ImportResult`、`get_sheet_data(sheet_id, page, page_size) -> PageData`
- 前端 `frontend/src/tauri.js`：`invoke('import_file', ...)` 封装

**验收标准**：
- 导入 1000 行 CSV：Sheet 新建、Table 渲染首页 50 行、分页可翻。
- 导入 XLSX 同上。
- 重启应用后通过「打开历史 Session」可重新加载该 Sheet 全量数据（验证持久化）。
- `operations` 表有对应 `import` 记录。

**验证方式**：`cargo tauri dev`，准备样例 CSV/XLSX 手动导入 + 重启核验持久化 + `sqlite3` 核验 operations 记录。

---

### T6 Tauri updater 插件

**目标**：集成 Tauri updater 插件（带 Ed25519 签名），提供「检查更新 / 安装更新」命令，updater JSON 托管于 GitHub Release。

**前置依赖**：T1。

**实现要点**：
- 生成签名密钥：`tauri signer generate -w ~/.tauri/ruT0datakit.key`（本地保存私钥，pubkey 填入 `tauri.conf.json`）。
- `tauri.conf.json` `plugins.updater`：`pubkey` 填入；`endpoints` 指向 GitHub Release latest 的 `latest.json` URL；`windows.installMode = "passive"`。
- GitHub Secrets：`TAURI_SIGNING_PRIVATE_KEY`、`TAURI_SIGNING_PRIVATE_KEY_PASSWORD`（CI 构建时注入，本地 dev 不需要）。
- 命令：`check_update() -> UpdateStatus { available: bool, version: Option<String>, notes: Option<String> }`；`install_update()` 委托 `tauri-plugin-updater`。
- 安全边界：updater 仅由插件发起 HTTPS 请求拉版本元数据 + 签名产物，不传用户数据；签名校验失败拒绝安装。

**交付物**：
- `src-tauri/src/updater.rs`（check/install 命令封装）
- `src-tauri/tauri.conf.json` updater 配置
- 密钥生成操作记录（不落盘私钥，仅记录 pubkey 与操作步骤到 `docs/versions/1.0.0/`）

**验收标准**：
- `check_update` 在无新版本时返回 `available=false`。
- 配置一个测试 updater JSON 指向更高版本，`check_update` 返回 `available=true, version=<新版本>`。
- 签名验证失败时 `install_update` 拒绝安装并报错。
- 无网络时 `check_update` 静默降级返回 `available=false`，不弹错。

**验证方式**：`cargo tauri dev`，本地起一个静态服务托管 `latest.json` 模拟检查；手动构造篡改签名验证拒绝。

---

### T7 AI 占位 IPC 契约

**目标**：落地 `ai_suggest` / `invoke_ai_op` 空实现 IPC 契约，为 v1.1+ 真实 AI 能力预留接口。

**前置依赖**：T1。

**实现要点**：
- `ai_suggest(context: AiContext) -> Result<AiSuggestion, String>`：返回 `Err("ai_suggest not implemented until v1.1+")`。
- `invoke_ai_op(op: String, params: Value) -> Result<Value, String>`：返回 `Err("invoke_ai_op not implemented until v1.1+")`。
- 类型定义见 02 文档 §4，所有嵌套 struct 必须 `#[serde(rename_all = "camelCase")]`。
- 前端 AI 面板调用 `ai_suggest` 时 catch 错误并展示「AI 能力 v1.1+ 释放」文案，不崩溃。

**交付物**：
- `src-tauri/src/commands.rs`：两个命令 + 类型定义
- `frontend/src/tauri.js`：`aiSuggest` / `invokeAiOp` 封装

**验收标准**：
- 前端调用 `aiSuggest` 得到 `v1.1+` 错误文案，UI 不崩溃。
- `invokeAiOp` 任意 op 均返回 `v1.1+` 错误。
- 类型定义中嵌套 struct 均带 `rename_all = "camelCase"`。

**验证方式**：`cargo tauri dev`，前端 AI 面板触发调用核验文案；`grep -n 'rename_all' src-tauri/src/commands.rs` 核验。

---

### T8 设置页 + 能力面板占位提示

**目标**：落地设置页（updater 检查 / tshark 路径占位 / DB 路径展示 / 关于）+ 确认 4 项能力面板占位提示统一。

**前置依赖**：T2（布局壳 + 能力面板容器）、T6（updater 检查能力）。

**实现要点**：
- 设置页组件：`UpdateCard`（按钮触发 `check_update` + 展示结果）、`TsharkPathCard`（输入框占位，v1.0.0 不持久化，提示「v1.3+ 生效」）、`DbPathCard`（只读展示 DB 路径 + 「在 Finder 中显示」按钮）、`AboutCard`（版本号 + 链接）。
- 4 项能力面板（脱敏/校验/提取/规则管理）占位提示统一文案模板「【能力名】能力 v1.1+ 释放」。
- 设置页通过左侧能力面板底部「⚙ 设置」入口切换显示（Workbench 区域被设置页覆盖）。
- 设置页返回时恢复上次激活的能力面板（`activeCapability` 保持）。

**交付物**：
- `frontend/src/components/settings/SettingsView.jsx`
- `frontend/src/components/settings/cards/{UpdateCard,TsharkPathCard,DbPathCard,AboutCard}.jsx`

**验收标准**：
- 设置页 4 个卡片渲染正常。
- 「检查更新」按钮触发 `check_update`，结果正确展示。
- DB 路径展示与实际 DB 文件路径一致，「在 Finder 中显示」可打开。
- 4 项能力面板点击后展示统一 `v1.1+` 占位提示。
- 设置页返回后恢复上次激活的能力面板。

**验证方式**：`cargo tauri dev`，手动核验设置页与能力面板提示。

---

### T9 双语 README + docs 收口 + QA

**目标**：完成 v1.0.0 文档体系最终收口与 Release QA 审计，状态推进至 `qa_passed`。

**前置依赖**：T1~T8 全部 `verified_complete`。

**实现要点**：
- 同步 `docs/versions/1.0.0/更新日志.md` 进度表（全任务 `verified_complete`）。
- 完成 `docs/qa/versions/1.0.0/QA-审计报告.md` 5 维度审计（功能 / 回归 / 构建 / 安全 / 文档），结论 `qa_passed`。
- 校验 README 双语与当前实现一致（Quick Start 命令实测可复现）。
- 校验 docs 间无术语/范围冲突，无未实现的 `v1.1+` 能力被误写成 v1.0.0 已交付。

**交付物**：
- `docs/versions/1.0.0/更新日志.md`（终态）
- `docs/qa/versions/1.0.0/QA-审计报告.md`（`qa_passed`）
- `README.md` / `README_EN.md`（与实现一致）

**验收标准**：
- 更新日志进度表与实际任务状态一致。
- QA 报告 5 维度全部通过，无 major/critical 问题。
- README Quick Start 在干净环境可复现。
- 文档间无冲突。

**验证方式**：人工审计 + 干净环境 Quick Start 复现 + 文档冲突扫描。

---

## 3. 阶段里程碑

| 阶段 | 任务 | 里程碑 | 状态口径 |
|---|---|---|---|
| 1 脚手架 | T1 | 工程可编译可启动 | `done_e2e`（T1 单任务） |
| 2 框架并行 | T2 T3 T4 T6 T7 | 四区 + 工作台 + 持久层 + updater + AI 占位各自落地 | 各任务 `verified_complete` |
| 3 链路打通 | T5 T8 | 导入全链路 + 设置页可用 | 各任务 `verified_complete` |
| 4 收口 | T9 | 文档 + QA 通过 | `qa_passed` |

## 4. 风险点

- **rusqlite bundled 编译耗时**：首次编译 SQLite C 源较慢，CI 需启用缓存。
- **Tauri updater 签名密钥管理**：私钥不落盘仓库，CI 通过 secret 注入；本地 dev 不签名也能跑。
- **大表导入性能**：5 万行以上需分批 commit + 前端分页，避免一次性渲染卡死。
- **serde camelCase 漏加**：v0.8.0 踩过的坑，v1.0.0 所有嵌套 struct 必须显式 `rename_all`，CI 加 grep 检查。

---

## 5. v1.1.0 任务详细定义（T15~T21）

> v1.1.0 在 v1.0.0 shell 之上落地脱敏/校验/提取三件套 + 规则管理基础结构。每任务字段同 §2。

---

### T15 core processor 模块

**目标**：在 `crates/core` 新增 `processor` 模块（纯逻辑引擎），定义 `Masker` / `Validator` / `Extractor` trait 与原型实现，以及 `Rule` / `RuleKind` / `RuleRegistry` 规则管理结构与 3 条姓名内置规则。

**前置依赖**：T1（脚手架）。

**实现要点**：
- `processor/rules.rs`：`Rule`（serde camelCase）+ `RuleKind`（lowercase 序列化 + `from_str_lowercase`）+ `RuleRegistry::with_defaults()` 注册 3 条姓名规则（`name-validate` / `name-mask` / `name-extract`）。
- `processor/validator.rs`：`Validator` trait + `RegexValidator`（`pattern` 正则校验单值）。
- `processor/masker.rs`：`Masker` trait + `SimpleMasker`（规则带 replacement → 整值替换；kind=Mask replacement=None → 保留首字符 + 其余 `*`；无规则 → 首尾保留）。
- `processor/extractor.rs`：`Extractor` trait + `PiiExtractor`（内置手机/邮箱/身份证 3 正则，`Default` 不 panic）。
- `lib.rs` re-export `processor` 模块。

**交付物**：`crates/core/src/processor/{mod,rules,validator,masker,extractor}.rs`

**验收标准**：
- `cargo test -p ruT0-data-kit-core` 全绿（含 processor 单测）。
- `with_defaults()` 断言 3 条规则；`RuleKind` round-trip 序列化测试通过。
- 姓名脱敏测试覆盖：「张三」→「张*」、「张三丰」→「张**」、「欧阳修」→「欧**」、「赵」→「*」、「」→「」。

**验证方式**：`cargo test -p ruT0-data-kit-core`

---

### T16 DB 扩展（rules 表 + SCHEMA_VERSION=2）

**目标**：DB schema 升级到 v2，新增 `rules` 表 + `idx_rules_kind` 索引，落地 Rule CRUD 方法与启动 seed 逻辑。

**前置依赖**：T4（持久层）、T15（`Rule`/`RuleKind` 类型）。

**实现要点**：
- `schema.rs`：`SCHEMA_VERSION = 2`；`SCHEMA_DDL` 新增 `rules` 表（id/name/kind/field/pattern/replacement/enabled/description）+ `idx_rules_kind`。
- `migrate.rs`：新增 `migrate_v1_to_v2`（`IF NOT EXISTS` 幂等增量迁移，不触发备份重建）；`migrate()` 分支：v1→v2 增量、v2 幂等、其他不兼容 → 备份重建。
- `db/mod.rs`：新增 `query_column_cells`（排除表头行 row_idx=0）+ `find_col_idx`（列名→col_idx）+ `upsert_rule` / `list_rules` / `get_rule` / `set_rule_enabled` / `count_rules` / `update_rule_params`（事务包裹两条 UPDATE）/ `seed_builtin_rules`（DB 为空时 seed 3 条）+ `row_to_rule`（DB 行↔`Rule` 映射）。
- `Rule`/`RuleKind` 通过 `use ruT0_data_kit_core::processor::rules::{Rule, RuleKind}` 引入。

**交付物**：`src-tauri/src/db/{schema,migrate,mod}.rs` 扩展

**验收标准**：
- `cargo test -p ruT0-data-kit db::` 全绿（含 v1→v2 迁移测试）。
- v1 DB → migrate → v2 + `rules` 表存在。
- Rule CRUD round-trip 测试通过。
- `seed_builtin_rules` 在空 DB 写入 3 条规则，非空 DB 不重复写入。

**验证方式**：`cargo test -p ruT0-data-kit db::`

---

### T17 Tauri commands（6 个 IPC）

**目标**：Tauri 命令层新增 6 个 processor IPC，规则改由 DB 单一真源，删除内存 `RuleState`。

**前置依赖**：T15（core processor）、T16（DB Rule CRUD）。

**实现要点**：
- `commands/processor.rs`：`mask_column` / `validate_column` / `extract_column` / `list_rules` / `toggle_rule` / `update_rule_params`，全部从 `DbManager` 读 rule，返回 `Result<T, String>`（`.map_err(|e| e.to_string())`）。
- `lib.rs`：删除 `RuleState(Mutex<RuleRegistry>)` import + `app.manage()`；setup 中 `DbManager::new` → `seed_builtin_rules()`；`generate_handler!` 注册 6 命令。

**交付物**：`src-tauri/src/commands/processor.rs` + `src-tauri/src/lib.rs`

**验收标准**：
- `cargo clippy --workspace -- -D warnings` 全绿。
- 6 命令注册可调用。
- 规则从 DB 读取，重启后保留。

**验证方式**：`cargo clippy --workspace -- -D warnings` + `cargo tauri dev` 手动核验

---

### T18 版本号升级

**目标**：版本号从 `1.0.0` 升级到 `1.1.0`，四处一致。

**前置依赖**：T1。

**实现要点**：`tauri.conf.json` / 根 `Cargo.toml` / `crates/core/Cargo.toml` / `src-tauri/Cargo.toml` / `frontend/package.json` / `frontend/src/constants.js` 同步 `1.1.0`。

**交付物**：版本号字段同步

**验收标准**：6 处版本号一致为 `1.1.0`。

**验证方式**：`grep -rn "1.1.0" Cargo.toml crates/core/Cargo.toml src-tauri/Cargo.toml src-tauri/tauri.conf.json frontend/package.json frontend/src/constants.js`

---

### T19 前端 state + IPC 封装

**目标**：前端 state 扩展行状态高亮 + 6 个 IPC 封装。

**前置依赖**：T17（IPC 命令）。

**实现要点**：
- `state/reducer.js`：`ACTION.APPLY_ROW_STATUSES` + case（按 `rowKey` 更新 `row.status` + `statusHighlights`）。
- `state`：`applyRowStatuses` dispatcher + `toRowObjects` 增加 `_rowIdx`（`(page-1)*pageSize + i + 1`，与后端 `rowIdx` 对齐）。
- `tauri.js`：`maskColumn` / `validateColumn` / `extractColumn` / `listRules` / `toggleRule` / `updateRuleParams` 6 个 invoke 封装。

**交付物**：`frontend/src/state/reducer.js` + `frontend/src/tauri.js`

**验收标准**：`pnpm build` 通过；6 个 IPC 封装可调用。

**验证方式**：`pnpm --prefix frontend build`

---

### T20 前端能力面板（4 面板真实 UI）

**目标**：4 个能力面板从占位提示升级为真实 UI，支持行状态高亮（`masked` / `invalid` / `hit`）；RulesPanel 改为 Workbench 主区两栏布局，无新增规则入口。

**前置依赖**：T19（state + IPC）、T3（DataTable 行高亮）。

**实现要点**：
- `MaskPanel`：选列 → 执行脱敏 → 回写 DB + 重新拉取当前页 + 行高亮 `masked`。
- `ValidatePanel`：选列 + 选规则（`kind==="validate"`）→ 执行校验 → 不通过行高亮 `invalid`。
- `ExtractPanel`：选列 → 执行提取 → 命中行高亮 `hit` + 底部 PII 列表。
- `RulesPanel`（Workbench 主区两栏）：左侧 240px antd List（规则名 + kind Tag + 启用 Tag）+ 右侧详情 Card（规则信息 + 可填参数 pattern/replacement + Save → `updateRuleParams` + 内联测试 TextArea）；**无新增规则入口**。
- `App.jsx`：`activeCapability === "rules"` → 渲染 `<RulesPanel/>` 占据 Workbench 主区；其余走原 `<SidePanel/> + <Workbench/>`。
- `SidePanel`：`PANELS` map 删除 `rules` 条目。

**交付物**：`frontend/src/components/panels/{MaskPanel,ValidatePanel,ExtractPanel,RulesPanel}.jsx` + `App.jsx` + `SidePanel.jsx`

**验收标准**：`pnpm build` 通过；4 面板真实 UI 可用；RulesPanel 两栏布局 + 无新增规则入口。

**验证方式**：`pnpm --prefix frontend build` + `cargo tauri dev` 手动核验

---

### T21 文档收口

**目标**：v1.1.0 文档体系最终收口，与实现一致。

**前置依赖**：T15~T20 全部 `verified_complete`。

**实现要点**：
- `docs/versions/1.1.0/规划需求.md`：范围（3 条姓名规则 + DB 持久化 + RulesPanel 两栏 + 无新增规则能力）。
- `docs/versions/1.1.0/更新日志.md`：进度表 T15~T21 + 验证摘要。
- `docs/versions/1.1.0/RELEASE-NOTES.md`：用户面向描述 + 已知限制。
- `docs/02-技术设计文档.md`：processor 模块 + DB schema（SCHEMA_VERSION=2 + rules 表）段落同步。

**交付物**：`docs/versions/1.1.0/` 三件套 + `docs/02-技术设计文档.md`

**验收标准**：文档间无冲突；与实现一致。

**验证方式**：人工审计 + 文档冲突扫描

---

## 6. v1.1.0 阶段里程碑

| 阶段 | 任务 | 里程碑 | 状态口径 |
|---|---|---|---|
| 1 core | T15 | processor 模块纯逻辑引擎 | `verified_complete` |
| 2 DB | T16 | rules 表 + SCHEMA_VERSION=2 | `verified_complete` |
| 3 命令 | T17 | 6 IPC + DB 单一真源 | `verified_complete` |
| 4 版本号 | T18 | 6 处一致 1.1.0 | `verified_complete` |
| 5 前端 state | T19 | 行状态高亮 + 6 IPC 封装 | `verified_complete` |
| 6 前端面板 | T20 | 4 面板真实 UI + RulesPanel 两栏 | `verified_complete` |
| 7 收口 | T21 | 文档 + QA 推进 | `verified_complete` → `qa_passed`（tag `v1.1.0` 已发布） |

---

## 7. v1.1.1 任务详细定义（T29~T36）

> v1.1.1 在 v1.1.0 之上落地撤销/重做 + 列操作 + 搜索 + commands 层测试。任务 DAG 详见 [`handoff/TASK-BOARD.md`](../handoff/TASK-BOARD.md) §1。

| ID | 标题 | depends_on | 状态 | 验收要点 |
|---|---|---|---|---|
| T29 | DB schema v2→v3（before_snapshot + idx_cells_sheet_col + 8 方法） | — | `verified_complete` | SCHEMA_VERSION=3 + 8 方法全参数化 SQL + 39 tests |
| T30 | undo/redo 后端命令（undo/redo/list_undoable_operations + mask_column 快照改造） | T29 | `verified_complete` | mask before_snapshot 非空 + undo/redo 回写正确 + 5 commands 层 tests |
| T31 | 搜索后端命令（search_cells + replace_all） | T29 | `verified_complete` | 关键字 LIKE + 正则预筛精匹配 + 分页 + replace_all 单事务快照撤销 + 15 tests |
| T32 | 列操作后端命令（parse_column_as_json + replace_in_column） | T29 | `verified_complete` | JSON 列展开新 sheet（不入撤销栈）+ replace_in_column 列内替换快照撤销 + 7 tests |
| T33 | 前端 IPC + state 扩展（7 IPC + 5 ACTION + factory searchHits） | T30,T31,T32 | `verified_complete` | undo/redo 不传 sheetId + APPLY_SEARCH_HITS rowKey 构造 + pnpm build 通过 |
| T34 | 前端 UI（撤销工具栏 + 搜索栏 + 列操作面板 + 单元格高亮） | T33 | `verified_complete` | mark 高亮 + 全局替换 Modal + ColumnOpsPanel JSON 解析新 Tab + pnpm build 通过（3 minor 非阻塞） |
| T35 | 版本号升级 1.1.0→1.1.1（6 处一致） | T34 | `verified_complete` | 4 文件改动 + workspace=true 继承 + cargo check + pnpm build 通过 |
| T36 | 文档收口 + 全量验证 + Release QA | T35 | `verified_complete` | 三件套 + QA-审计报告 + 02 设计文档同步 + E1~E8 + qa_passed |

## 8. v1.1.1 阶段里程碑

| 阶段 | 任务 | 里程碑 | 状态口径 |
|---|---|---|---|
| 1 DB 地基 | T29 | schema v3 + 8 方法 | `verified_complete` |
| 2 后端命令 | T30/T31/T32 | undo/redo + 搜索 + 列操作（并行） | `verified_complete` |
| 3 前端 IPC+state | T33 | 7 IPC + 5 ACTION | `verified_complete` |
| 4 前端 UI | T34 | 撤销工具栏 + 搜索栏 + ColumnOpsPanel | `verified_complete` |
| 5 版本号 | T35 | 6 处一致 1.1.1 | `verified_complete` |
| 6 收口 | T36 | 文档 + QA 推进 | `verified_complete` → `qa_passed` |
